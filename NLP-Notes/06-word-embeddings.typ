= 预训练词向量

== NLP技术的四次范式变迁

#text(fill: red, weight: "bold")[自然语言处理（NLP）的发展经历了四次重要的范式变迁，每一次都标志着方法论的根本转变：]

#table《
  columns: 《auto, auto, auto》,
  《阶段》, 《时期》, 《特点》,
  《小规模专家系统》, 《1950-1990》, 《基于人工规则和语言学知识》,
  《语料库统计模型》, 《1990-2010》, 《基于统计特征和概率模型》,
  《深度学习模型》, 《2010-2017》, 《神经网络端到端学习》,
  《大规模预训练模型》, 《2018-至今》, 《预训练+微调范式》,
》

- 小规模专家系统《1950-1990》：依赖语言学家手工编写规则，如语法分析器、词典匹配等。优点是精确可控，缺点是覆盖面窄、维护成本高。
- 语料库统计模型《1990-2010》：利用大规模语料进行统计学习，如 N-gram 语言模型、隐马尔可夫模型《HMM》、条件随机场《CRF》 等。特征工程是核心。
- 深度学习模型《2010-2017》：以 Word2Vec 为起点，神经网络开始主导 NLP。RNN、CNN、Seq2Seq 等架构相继出现，实现了端到端学习。
- 大规模预训练模型《2018-至今》：以 BERT、GPT 为代表，采用 预训练+微调 范式，在大规模无标注语料上预训练，再在下游任务上微调，取得了 SOTA 效果。

== 传统词表示方法

=== One-Hot 表示

#text(fill: red, weight: "bold")[One-Hot 表示是最直观的词表示方法：]

- 假设词表大小为 V，每个词对应一个 V 维向量
- 该词对应位置为 1，其余位置为 0

例如词表 ={"猫", "狗", "鸟"}：
"猫" -› 《1, 0, 0》
"狗" -› 《0, 1, 0》
"鸟" -› 《0, 0, 1》

缺点：
- 高维稀疏：维度随词表增长，通常为几万到几十万维
- 无法表达语义相似性：任意两个不同词的向量正交，内积为零 angle.l "猫", "狗" angle.r = 0
- 存在 维数灾难 问题

=== 分布式表示

#text(fill: red, weight: "bold")[分布式表示（Distributed Representation）的核心思想：]

#quote《词的含义由它周围的词来决定。》 — J.R. Firth, 1957

- 将词映射到低维稠密向量《通常 50-300 维》
- 语义相近的词在向量空间中距离更近
- 稠密向量的每个维度都承载语义信息
- 可通过向量运算捕捉词之间的关系：vec《"国王"》 - vec《"男"》 + vec《"女"》 approx vec《"女王"》

== 预训练静态词向量

静态词向量：训练完成后，每个词对应一个 固定 的向量，不随上下文变化。

=== Word2Vec

Word2Vec《Mikolov et al., 2013》是静态词向量的里程碑，包含两种模型：

=== CBOW（Continuous Bag-of-Words）模型

- 目标：根据 上下文词 预测 中心词
- 将上下文词的向量 取平均 后，通过投影层预测中心词
- 适合 高频词，训练速度较快

"输入" -› "上下文向量平均" -› "投影层" -› "预测中心词"

=== Skip-gram 模型

- 目标：根据 中心词 预测 上下文词
- 将中心词的向量投影后，预测周围窗口内的每个上下文词
- 在 低频词 上表现更好

"输入中心词" -› "投影层" -› "预测上下文词"

#table《
  columns: 《auto, auto, auto》,
  《对比项》, 《CBOW》, 《Skip-gram》,
  《预测方向》, 《上下文 -› 中心词》, 《中心词 -› 上下文》,
  《训练速度》, 《快》, 《慢》,
  《低频词效果》, 《较差》, 《较好》,
  《适用场景》, 《大语料高频词》, 《小语料含低频词》,
》

=== GloVe（Global Vectors）

GloVe《Pennington et al., 2014》结合了 全局共现统计 和 局部上下文 的优点：

- 构建全局 词-词共现矩阵 X，其中 X《i j》 表示词 i 和词 j 在窗口内共现的次数
- 目标函数：J = sum《i,j=1》V f《X《i j》》 《witop hat《w》j + bi + hat《b》j - log X《i j》》2
- f《X《i j》》 为 加权函数，防止高频词权重过大
- 优势：能捕获 全局语料统计信息，在大语料上效率较高

=== FastText

FastText《Bojanowski et al., 2016》在 Word2Vec 基础上引入了 子词信息《subword》：

- 将每个词分解为 字符 n-gram《如 "apple" -› "［ap", "app", "ppl", "ple", "le］"》
- 词的向量 = 所有 n-gram 向量之和
- 优势：
  - 能处理 未登录词《OOV》
  - 对 形态变化《如 run/running/ran》有更好的捕捉能力
  - 在 拼写变体 和 稀有词 上表现优异

== 预训练动态词向量（上下文相关词向量）

#text(fill: red, weight: "bold")[动态词向量：同一个词在不同上下文中获得不同的向量表示。]

=== ELMo（Embeddings from Language Models）

ELMo《Peters et al., 2018》：

- 基于 双向 LSTM《BiLSTM》的语言模型
- 多层输出进行 加权组合：
  ELMok = gamma sum《j=0》J sj h《k j》
- 下层捕捉 语法 信息，上层捕捉 语义 信息
- 是 预训练动态词向量 的开山之作

=== BERT（Bidirectional Encoder Representations from Transformers）

BERT《Devlin et al., 2019》：

- 基于 Transformer 编码器 架构
- 使用 MLM《Masked Language Model》 作为预训练任务：随机遮盖部分 token，预测被遮盖的词
- 使用 NSP《Next Sentence Prediction》 任务判断两个句子是否连续
- 采用 双向注意力机制，同时编码左右上下文
- 通过 微调《fine-tuning》适配下游任务

=== GPT（Generative Pre-trained Transformer）

GPT《Radford et al., 2018》：

- 基于 Transformer 解码器 架构
- 使用 自回归语言模型《Autoregressive LM》：根据前文预测下一个词
- 采用 单向注意力《causal attention》，只能看到左侧上下文
- GPT-2/GPT-3 展示了 大规模自回归模型 的 few-shot 甚至 zero-shot 能力

=== 静态 vs 动态词向量对比

#table《
  columns: 《auto, auto, auto》,
  《对比项》, 《静态词向量》, 《动态词向量》,
  《代表模型》, 《Word2Vec, GloVe, FastText》, 《ELMo, BERT, GPT》,
  《上下文感知》, 《否，一词一向量》, 《是，一词多向量》,
  《架构》, 《浅层》, 《深层神经网络》,
  《OOV处理》, 《FastText部分支持》, 《完全支持》,
  《训练成本》, 《低》, 《非常高》,
  《适用场景》, 《资源受限/简单任务》, 《复杂任务/SOTA》,
》

== 词向量的应用

预训练词向量在各类 NLP 任务中广泛应用：

- 文本分类：将文本中的词向量聚合《平均/加权/Pooling》，输入分类器
- 情感分析：捕捉词语的褒贬语义倾向，如 vec《"优秀"》 - vec《"差劲"》
- 命名实体识别《NER》：利用词向量和上下文信息识别实体边界和类型
- 机器翻译：词向量作为 Seq2Seq 模型的输入表示
- 问答系统：通过向量匹配计算问题和答案的相似度
- 信息检索：将查询和文档映射到向量空间进行相似度排序

== 词向量的评测

词向量的质量需要通过标准任务进行评估：

=== 词相似度任务

- 计算模型生成的词对相似度与 人工评分 之间的 斯皮尔曼等级相关系数
- 常用数据集：
  - WordSim-353：353 对词的相似度评分
  - Rare Words《RW》：包含稀有词对
  - SimLex-999：更强调 同义性 而非一般相关性

=== 类比任务

- 经典的 词类比《Word Analogy》任务：
  vec《"国王"》 - vec《"男"》 + vec《"女"》 = vec《"女王"》
- 常用数据集：
  - Google Analogy Test Set：语义和语法类比《如 国家-首都、形容词比较级、动词时态》
  - BATS《Bigger Analogy Test Set》：更全面的类比数据集
- 评估指标：类比准确率《Analogy Accuracy》

=== 下游任务评测

- 将词向量作为下游模型的输入特征，通过任务性能间接评估词向量质量
- 常见下游任务：情感分析准确率、NER F1值、文本分类准确率 等

== 习题要点

- 掌握 One-Hot 与 分布式表示 的区别，理解为什么分布式表示优于 One-Hot。
- 熟练掌握 CBOW 和 Skip-gram 的训练目标、优缺点及适用场景，能写出各自的损失函数。
- 理解 GloVe 的全局共现矩阵方法，掌握其目标函数及加权函数 f《X《i j》》 的作用。
- 掌握 FastText 子词机制，理解其处理 OOV 词的原理。
- 区分 静态词向量 与 动态词向量，理解上下文相关表示的重要性。
- 对比 BERT《MLM + 双向注意力》与 GPT《自回归 + 单向注意力》的预训练目标和架构差异。
- 理解 ELMo 的多层加权组合机制。
- 掌握词向量评测的 词相似度 和 类比任务 方法，了解常用评测数据集及指标。
- 能举例说明词向量在 文本分类、NER、情感分析 等任务中的具体应用方式。
