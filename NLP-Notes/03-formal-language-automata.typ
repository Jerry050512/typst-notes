= 第3章：形式语言与自动机

== 1. 形式语言的定义

*形式语言*（Formal Language）是按一定规律构成的句子和符号串的有限或无限集合（Chomsky 定义）。

形式语言理论的核心思想：
- 用*数学方法*研究语言的结构和规律
- 关注语言的*形式特征*（符号组合规则），而非语义
- 为自然语言处理提供*形式化基础*

形式语言的基本构成：
- *字母表*（Alphabet）：有限的非空符号集合，记为 $Sigma$
- *字符串*（String / Word）：字母表中符号的有限序列，记为 $w$
- *语言*（Language）：字符串集合的某个子集 $L subseteq Sigma^*$

== 2. 图的基本概念

图是形式语言与自动机理论中的基础数据结构。

*无向图*：$G = (N, E)$
- $N$：节点（顶点）的有限集合
- $E$：边的集合，每条边是 $N$ 中两个节点的无序对

*有向图*：$G = (N, E)$
- $E$ 中的每条边是有序对 $(n_i, n_j)$，表示从 $n_i$ 到 $n_j$ 的有向边

相关概念：
- *路径*（Path）：一系列边的序列，连接一系列节点
- *连通*（Connected）：任意两节点间存在路径
- *环/回路*（Cycle）：起点与终点相同的路径

== 3. 字母表与字符串

=== 字母表 $Sigma$

字母表是*有限的非空符号集合*。

示例：
- $Sigma = {a, b, c}$（英文字母表）
- $Sigma = {0, 1}$（二进制字母表）
- $Sigma = {我, 你, 他, 是, 的}$（汉字片段表）

=== 字符串 $w$

字符串是字母表中符号的*有限序列*。

- $w = a_1 a_2 ... a_n$，其中 $a_i in Sigma$
- $|w|$ 表示字符串 $w$ 的*长度*
- $|w| = n$ 表示 $w$ 由 $n$ 个符号组成

=== 空串 $epsilon$

*空串*（Empty String）是长度为 0 的字符串，记为 $epsilon$（也可记为 $lambda$）。

性质：
- $|epsilon| = 0$
- 对任意字符串 $w$，有 $w epsilon = epsilon w = w$

=== 连接运算（Concatenation）

两个字符串 $w_1 = a_1 ... a_m$ 和 $w_2 = b_1 ... b_n$ 的*连接*：

$ w_1 w_2 = a_1 ... a_m b_1 ... b_n $

连接运算满足：
- *结合律*：$(w_1 w_2) w_3 = w_1 (w_2 w_3)$
- *单位元*：$w epsilon = epsilon w = w$
- *不满足交换律*：$w_1 w_2 != w_2 w_1$（一般情况）

其他重要概念：
- $Sigma^*$：字母表 $Sigma$ 上所有字符串的集合（含 $epsilon$）
- $Sigma^+$：$Sigma^* - {epsilon}$，即所有非空字符串的集合
- *子串*（Substring）：字符串中连续的一段
- *前缀*（Prefix）与*后缀*（Suffix）

== 4. 文法类型（Chomsky 分层）

*文法*（Grammar）是语言的形式化表示，定义了从符号生成字符串的规则。

一个文法 $G$ 是一个四元组：$G = (V_N, V_T, P, S)$
- $V_N$：*非终结符*（Non-terminal）集合 — 中间变量
- $V_T$：*终结符*（Terminal）集合 — 语言中的实际符号
- $P$：*产生式规则*（Production Rules）集合
- $S$：*开始符号*（Start Symbol），$S in V_N$

关键约束：$V_N inter V_T = emptyset$

=== 四种文法类型

#highlight[0型文法 — 无限制文法（Unrestricted Grammar）]

- 产生式形式：$alpha -> beta$，其中 $alpha in (V_N union V_T)^+$，$beta in (V_N union V_T)^*$
- 唯一限制：$alpha$ 不能为空串
- 对应自动机：*图灵机*（Turing Machine）
- 表达能力：最强，可描述递归可枚举语言

#highlight[1型文法 — 上下文有关文法（Context-Sensitive Grammar, CSG）]

- 产生式形式：$alpha A beta -> alpha gamma beta$
- 要求：$|alpha A beta| <= |alpha gamma beta|$，即 $|gamma| >= 1$（非收缩性）
- 含义：$A$ 在上下文 $alpha, beta$ 中才能被替换为 $gamma$
- 对应自动机：*线性有界自动机*（Linear Bounded Automaton）
- 对应语言：上下文有关语言

#highlight[2型文法 — 上下文无关文法（Context-Free Grammar, CFG）]

- 产生式形式：$A -> gamma$
- 要求：$A in V_N$（单个非终结符），$gamma in (V_N union V_T)^*$
- 含义：$A$ 的替换*不依赖上下文*
- 对应自动机：*下推自动机*（Pushdown Automaton）
- 对应语言：上下文无关语言

#highlight[3型文法 — 正则文法（Regular Grammar）]

- 产生式形式（右线性）：$A -> wB$ 或 $A -> w$
- 产生式形式（左线性）：$A -> Bw$ 或 $A -> w$
- 其中 $A, B in V_N$，$w in V_T^*$
- 对应自动机：*有限自动机*（Finite Automaton）
- 对应语言：正则语言

=== 文法类型对比

#table(
  columns: 5,
  [类型], [文法名称], [产生式形式], [对应自动机], [语言类],
  [0型], [无限制文法], [$alpha -> beta$], [图灵机], [递归可枚举语言],
  [1型], [上下文有关文法], [$alpha A beta -> alpha gamma beta$], [线性有界自动机], [上下文有关语言],
  [2型], [上下文无关文法], [$A -> gamma$], [下推自动机], [上下文无关语言],
  [3型], [正则文法], [$A -> wB$ 或 $A -> w$], [有限自动机], [正则语言],
)

=== 包含关系

四种文法类型之间存在*严格包含*关系：

$ "正则语言" subset "上下文无关语言" subset "上下文有关语言" subset "递归可枚举语言" $

即：3型 $subset$ 2型 $subset$ 1型 $subset$ 0型

*重要考点*：判断给定文法属于哪一型，关键是看产生式左部是否满足对应条件。

== 5. CFG 派生树（短语结构分析）

*派生树*（Derivation Tree / Parse Tree）是上下文无关文法推导过程的图形化表示。

=== 派生树的基本概念

派生树 $T = (V, E)$：
- 每个*节点* $v in V$ 标注一个符号 $l(v) in V_N union V_T union {epsilon}$
- *根节点*标注开始符号 $S$
- *内部节点*标注非终结符（$A in V_N$）
- *叶节点*标注终结符（$a in V_T$）或 $epsilon$

派生树的*短语*分析：
- *简单短语*（Simple Phrase / 直接短语）：由某个非终结符一步推导出的子串
- *短语*（Phrase）：由某个非终结符推导出的子串
- *句柄*（Handle）：最左边的简单短语，是归约操作的对象

=== 最左推导与最右推导

- *最左推导*（Leftmost Derivation）：每一步替换最左边的非终结符
- *最右推导*（Rightmost Derivation / 规范推导）：每一步替换最右边的非终结符
- *规范归约*（Canonical Reduction）：最右推导的逆过程

=== 文法的二义性

如果一个句子存在*两棵不同的派生树*（或两个不同的最左推导），则该文法是*二义的*（Ambiguous）。

注意：二义性是文法的性质，不是语言的性质。可能存在等价的无二义文法。

== 6. 自动机

自动机是形式语言的*识别模型*，与文法（生成模型）互为对偶。

=== DFA — 确定性有限自动机

*DFA*（Deterministic Finite Automaton）是一个五元组：$M = (Q, Sigma, delta, q_0, F)$

- $Q$：有限状态集合
- $Sigma$：输入字母表
- $delta$：状态转移函数，$delta: Q times Sigma -> Q$（确定性：每个状态-输入对应唯一下一个状态）
- $q_0$：初始状态，$q_0 in Q$
- $F$：接受状态（终态）集合，$F subseteq Q$

DFA 的特点：
- 每个状态对每个输入符号有*且仅有一条*出边
- *无 $epsilon$ 转移*
- 从初始状态出发，读完输入字符串后，若停在 $F$ 中的状态，则接受该字符串

=== NFA — 非确定性有限自动机

*NFA*（Nondeterministic Finite Automaton）是一个五元组：$M = (Q, Sigma, delta, q_0, F)$

- $Q, Sigma, q_0, F$ 含义同 DFA
- $delta$：状态转移函数，$delta: Q times (Sigma union {epsilon}) -> 2^Q$

NFA 的特点：
- 同一状态对同一输入符号可以有*多个*不同的转移（非确定性）
- 允许 *$epsilon$ 转移*（不需要输入符号的状态跳转）
- 只要*存在一条*路径到达接受状态，就接受该字符串

=== DFA 与 NFA 的等价性

#highlight[*重要定理*]：DFA 和 NFA 等价，即它们能识别的语言类完全相同 — 都是*正则语言*。

- 子集构造法（Subset Construction）：可将 NFA 转化为等价的 DFA
- 转化后 DFA 的状态数最多为 $2^{|Q|}$（NFA 状态数的幂集）

=== 自动机与文法类型的对应关系

#table(
  columns: 3,
  [文法类型], [语言类], [识别自动机],
  [3型（正则文法）], [正则语言], [DFA / NFA],
  [2型（上下文无关文法）], [上下文无关语言], [下推自动机 PDA],
  [1型（上下文有关文法）], [上下文有关语言], [线性有界自动机 LBA],
  [0型（无限制文法）], [递归可枚举语言], [图灵机 TM],
)

== 7. 正则表达式与有限自动机的等价性

*正则表达式*（Regular Expression, RE）是描述正则语言的另一种形式化方法。

=== 正则表达式的基本运算

给定字母表 $Sigma$，正则表达式定义如下：
- $emptyset$：表示空集
- $epsilon$：表示仅含空串的集合 ${epsilon}$
- $a$（$a in Sigma$）：表示单字符集合 ${a}$
- 若 $r$ 和 $s$ 是正则表达式，则：
  - *连接*（Concatenation）：$r s$ — $L(r) L(s)$
  - *并*（Union）：$(r | s)$ — $L(r) union L(s)$
  - *Kleene 闭包*（Kleene Star）：$(r)^*$ — $L(r)^*$

=== 等价性定理

#highlight[*Kleene 定理*]：正则表达式、DFA、NFA 三者等价。

即：对于任意正则表达式 $r$，存在 DFA（和 NFA）$M$ 使得 $L(r) = L(M)$；反之亦然。

转化方法：
- NFA → DFA：*子集构造法*
- DFA → 正则表达式：*状态消除法*
- 正则表达式 → NFA：*Thompson 构造法*（基于正则表达式结构递归构造）

=== 正则语言的封闭性

正则语言在以下运算下封闭：
- 并、交、补运算
- 连接运算
- Kleene 闭包
- 反转运算
- 商运算

== 8. 形式语言在 NLP 中的应用

=== 词法分析（Lexical Analysis）

*词法分析器*（Lexer）的核心技术基于正则表达式和有限自动机：

- *Token 定义*：用正则表达式定义词法单元（关键字、标识符、运算符等）
- *模式匹配*：将正则表达式编译为 DFA，对输入字符串进行高效扫描
- 时间复杂度：$O(n)$，$n$ 为输入字符串长度

示例 — 标识符的正则表达式：
$ [a-zA-Z_][a-zA-Z0-9_]* $

=== 拼写检查（Spell Checking）

基于形式语言的拼写检查方法：

- *编辑距离*（Edit Distance）：计算两个字符串之间的最小编辑操作数（插入、删除、替换）
- *有限状态转录机*（Finite State Transducer, FST）：将拼写错误模式映射到正确单词
- *N-gram 模型*：基于统计的语言模型，与正则语言理论结合

=== 其他应用

- *句法分析*：基于 CFG 的短语结构分析（如 CKY 算法）
- *命名实体识别*：可用正则表达式辅助
- *形态分析*：基于 FST 的词形还原
- *编译器前端*：词法分析 + 语法分析的理论基础

== 习题要点

1. *文法的类型判断*：给定文法 $G$，判断其属于 Chomsky 分层中的哪一型。关键是检查产生式的形式是否满足对应类型的约束条件。注意：如果文法同时满足多个类型的约束，则按最小类型归类。

2. *DFA/NFA 的设计*：根据语言描述，设计能识别该语言的 DFA 或 NFA。注意状态的定义和转移函数的完整性。

3. *NFA 到 DFA 的转化*：掌握*子集构造法*，将给定的 NFA 转化为等价的 DFA。注意 $epsilon$-闭包的计算。

4. *正则表达式 ↔ 自动机的相互转化*：掌握 Thompson 构造法（RE → NFA）、子集构造法（NFA → DFA）、状态消除法（DFA → RE）。

5. *CFG 的派生树与二义性*：给定 CFG 和句子，画出所有派生树，判断文法是否二义。寻找最左推导和最右推导。

6. *Chomsky 分层的包含关系*：理解四种文法类型之间的严格包含关系，能举出属于某类型但不属于更小类型的语言例子（如 ${a^n b^n | n >= 0}$ 是 CFL 但不是正则语言）。

7. *派生树中的短语与句柄*：在给定派生树中标注简单短语、短语和句柄。

8. *有限自动机的化简*：掌握 DFA 的*最小化*算法（等价状态合并）。
