= 第8章：语言模型

== 1. 语言模型的定义

*语言模型（Language Model）* 是刻画真实语言中词与词之间联系规律的模型。

给定一个词序列 $w_1, w_2, ..., w_n$，语言模型的目标是计算该序列出现的概率：

$ P(w_1, w_2, ..., w_n) = P(w_1) P(w_2 | w_1) P(w_3 | w_1, w_2) ... P(w_n | w_1, ..., w_(n-1)) $

利用*链式法则（Chain Rule）*，将联合概率分解为一系列条件概率的乘积。

语言模型的核心任务：在给定上文的情况下，预测下一个词的概率分布。

== 2. N-gram 模型

N-gram 模型基于*马尔可夫假设*：当前词的概率仅依赖于前 $n-1$ 个词。

=== 2.1 常见 N-gram 模型

- *Unigram（一元模型）*：每个词独立出现

  $ P(w_i) = (count(w_i)) / N $

  其中 $N$ 为语料库总词数。

- *Bigram（二元模型）*：当前词仅依赖前一个词

  $ P(w_i | w_(i-1)) = (count(w_(i-1), w_i)) / (count(w_(i-1))) $

- *Trigram（三元模型）*：当前词依赖前两个词

  $ P(w_i | w_(i-2), w_(i-1)) = (count(w_(i-2), w_(i-1), w_i)) / (count(w_(i-2), w_(i-1))) $

=== 2.2 参数估计：最大似然估计（MLE）

*最大似然估计（MLE）* 是最基本的参数估计方法：

$ P_MLE(w_i | w_(i-1)) = (count(w_(i-1), w_i)) / (count(w_(i-1))) $

- $count(w_(i-1), w_i)$：bigram $(w_(i-1), w_i)$ 在训练语料中出现的次数
- $count(w_(i-1))$：词 $w_(i-1)$ 在训练语料中出现的次数

同理，对于 trigram：

$ P_MLE(w_i | w_(i-2), w_(i-1)) = (count(w_(i-2), w_(i-1), w_i)) / (count(w_(i-2), w_(i-1))) $

MLE 的缺点：如果某个 n-gram 在训练集中未出现，则其概率估计为 *0*，导致整个序列概率为 0。

== 3. 数据稀疏问题与平滑方法

=== 3.1 数据稀疏问题

在训练语料中，许多合法的 n-gram 可能从未出现，导致 MLE 估计概率为 0。这就是*数据稀疏问题*。

语料库不可能覆盖所有可能的词组合，尤其是高阶 n-gram 的稀疏问题更为严重。

=== 3.2 平滑方法（Smoothing）

平滑方法的核心思想：将部分概率质量从未出现的事件中"借"给已出现的事件。

==== 3.2.1 加一平滑（Laplace Smoothing）

最简单的平滑方法，对每个 n-gram 的计数加 1：

$ P_Laplace(w_i | w_(i-1)) = (count(w_(i-1), w_i) + 1) / (count(w_(i-1)) + V) $

其中 $V$ 为词汇表大小。

优点：实现简单。缺点：对高阶 n-gram 的修正过大，效果有限。

==== 3.2.2 古德-图灵估计（Good-Turing Estimation）

基本思想：用*出现 $r+1$ 次*的 n-gram 数量来估计*出现 $r$ 次*的 n-gram 的真实频率。

$ r^(*) = (r + 1) dot (N_(r+1)) / N_r $

其中 $N_r$ 为恰好出现 $r$ 次的 n-gram 数量。

将未出现事件（$r=0$）的概率质量设为 $N_1 / N$。

==== 3.2.3 回退法（Back-off）

基本思想：当高阶 n-gram 未出现时，*回退*到低阶 n-gram 进行估计。

$ P_BO(w_i | w_(i-1)) = cases(
  (count(w_(i-1), w_i) - d) / (count(w_(i-1)), "if count(w_(i-1), w_i) > k",
  alpha(w_(i-1)) dot P_BO(w_i), "otherwise"
)

其中 $d$ 为折扣因子，$k$ 为阈值，$alpha$ 为回退权重。

==== 3.2.4 插值法（Interpolation）

基本思想：将不同阶的 n-gram 模型进行*线性组合*。

$ P_interp(w_i | w_(i-2), w_(i-1)) = lambda_1 P(w_i | w_(i-2), w_(i-1)) + lambda_2 P(w_i | w_(i-1)) + lambda_3 P(w_i) $

其中 $lambda_1 + lambda_2 + lambda_3 = 1$，且 $lambda_i >= 0$。

优点：各阶模型互补，不会出现概率为 0 的情况。

==== 3.2.5 Kneser-Ney 平滑

目前*最常用且效果最好*的平滑方法，是对回退法的改进。

核心思想：低阶模型的概率不应仅基于该词出现的频率，而应基于该词*作为新延续*的能力。

$ P_KN(w_i | w_(i-1)) = max(count(w_(i-1), w_i) - d, 0) / (count(w_(i-1))) + lambda(w_(i-1)) dot P_continuation(w_i) $

其中*延续概率（continuation probability）*：

$ P_continuation(w_i) = (|{w : count(w, w_i) > 0}|) / (|{(w_j, w_k) : count(w_j, w_k) > 0}|) $

即：有多少个不同的前一个词 $w$ 与 $w_i$ 共现过，除以所有 bigram 类型的数量。

=== 3.3 平滑方法对比

#table(
  columns: 5,
  [平滑方法], [核心思想], [优点], [缺点], [适用场景],
  [加一平滑(Laplace)], [所有计数加1], [实现简单], [修正过大，效果差], [教学演示],
  [古德-图灵(Good-Turing)], [用r+1的频次估计r], [理论优雅], [高频r估计不稳定], [小样本估计],
  [回退法(Back-off)], [高阶缺失则回退到低阶], [利用了多阶信息], [阈值选择困难], [通用场景],
  [插值法(Interpolation)], [多阶模型线性组合], [不会出现概率为0], [参数需调优], [通用场景],
  [Kneser-Ney], [改进的回退+延续概率], [效果最好，最常用], [实现较复杂], [大规模应用首选],
)

== 4. 神经网络语言模型

=== 4.1 基本思想

传统 N-gram 模型的局限：
- 只能捕捉固定窗口内的依赖关系
- 无法泛化到未出现的 n-gram
- 高维稀疏

*神经网络语言模型（NNLM）* 用神经网络代替条件概率模型，通过*分布式词语表示（Distributed Representation）* 来解决上述问题。

=== 4.2 分布式词语表示

将每个词映射为一个低维（通常 50\~300 维）的*稠密向量*（词向量/Word Embedding）。

- 语义相似的词在向量空间中距离更近
- 通过神经网络学习得到
- 是*连续空间*中的表示，天然具有泛化能力

=== 4.3 前馈神经网络语言模型（Feed-Forward NNLM）

Bengio 等人于 2003 年提出的前馈神经网络语言模型结构：

1. 输入层：将前 $n-1$ 个词的词向量拼接
2. 隐藏层：通过非线性激活函数（如 $tanh$）计算
3. 输出层：通过 Softmax 输出下一个词的概率分布

$ P(w_i | w_(i-n+1), ..., w_(i-1)) = softmax(W dot h + b) $

其中 $h = tanh(W_1 dot [e_(i-n+1); ...; e_(i-1)] + b_1)$，$e_j$ 为词 $w_j$ 的词向量。

优点：能自动学习词语之间的语义关系。缺点：训练速度慢，固定窗口大小。

=== 4.4 RNN 语言模型

*循环神经网络（RNN）* 语言模型可以处理任意长度的上下文：

$ h_t = f(W_h h_(t-1) + W_e e_t + b) $

$ P(w_(t+1) | w_1, ..., w_t) = softmax(W_o h_t + b_o) $

优点：理论上可以捕捉*任意长距离依赖*，不受窗口大小限制。

缺点：存在*梯度消失/爆炸*问题，难以学习长距离依赖。

改进方案：*LSTM*（Long Short-Term Memory）和 *GRU*（Gated Recurrent Unit）通过门控机制解决了长距离依赖问题。

== 5. 语言模型的评价

=== 5.1 困惑度（Perplexity）

*困惑度*是评价语言模型最核心的指标，衡量模型对测试数据的"惊讶程度"。

$ PP(W) = P(w_1, w_2, ..., w_N)^(-1/N) = 2^(-1/N sum_(i=1)^N log_2 P(w_i | w_1, ..., w_(i-1))) $

等价形式（以 $e$ 为底）：

$ PP(W) = e^(-1/N sum_(i=1)^N ln P(w_i | w_1, ..., w_(i-1))) $

- $N$：测试集总词数
- $P(w_i | w_1, ..., w_(i-1))$：模型给出的条件概率

*困惑度越低，模型越好*（表示模型对测试数据越"确定"）。

直观理解：困惑度表示模型在预测下一个词时的*平均可选词数*。例如 PP=100 表示模型平均在每个位置面临约 100 个等概率的选择。

=== 5.2 交叉熵（Cross Entropy）

交叉熵与困惑度等价，只是以*对数*形式表示：

$ H(W) = -1/N sum_(i=1)^N log_2 P(w_i | w_1, ..., w_(i-1)) $

关系：$ PP = 2^(H(W)) $

交叉熵越小，模型越好。

== 6. 语言模型的应用

- *语音识别*：语言模型帮助从多个候选词序列中选择最可能的序列。

  例如，"recognize speech" vs "wreck a nice beach"，语言模型选择概率更高的。

- *机器翻译*：语言模型确保生成的目标语言句子通顺自然。

  翻译模型提供候选翻译，语言模型评估译文的流畅度。

- *文本生成*：语言模型根据上文自动生成后续文本。

  包括自动写作、对话系统、代码补全等。

- *拼写纠错*：利用语言模型判断候选纠正是否合理。

- *信息检索*：用于查询扩展和相关性排序。

== 习题要点

本章是*计算题的重点章节*，需熟练掌握以下题型：

. *N-gram 概率计算*：给定语料库，计算 unigram、bigram、trigram 的 MLE 概率。注意分母的选择（unigram 除以总词数，bigram 除以前一词的计数）。

. *加一平滑计算*：掌握 Laplace 平滑公式的应用，特别注意词汇表大小 $V$ 在分母中的使用。

  $ P_Laplace(w_i | w_(i-1)) = (count(w_(i-1), w_i) + 1) / (count(w_(i-1)) + V) $

. *古德-图灵估计计算*：给定各频次 $N_r$ 的值，计算调整后的频次 $r^(*)$。

  $ r^(*) = (r + 1) dot (N_(r+1)) / N_r $

. *困惑度计算*：给出测试序列中每个词的条件概率，计算语言模型的困惑度。

  $ PP = 2^(-1/N sum_(i=1)^N log_2 P(w_i)) $

  计算步骤：(1) 对每个概率取 $log_2$；(2) 求平均；(3) 以 2 为底取指数的相反数。

. *交叉熵计算*：与困惑度等价，注意 $H = -1/N sum log_2 P(w_i)$。

. *Kneser-Ney 平滑计算*：理解延续概率的概念，掌握折扣因子 $d$ 和回退权重 $lambda$ 的作用。

. *对比分析题*：比较不同平滑方法的优缺点，理解各自适用的场景。特别注意 Kneser-Ney 平滑为什么效果最好。

*考试提示*：
- 计算题务必注意*对数的底数*（通常以 2 为底）
- 困惑度和交叉熵可以相互转换
- 加一平滑中 $V$ 是*词汇表大小*（不同词的个数），不是语料总词数
- 注意区分 $count(w_(i-1))$（unigram 计数）和 $count(w_(i-1), w_i)$（bigram 计数）
