// NLP 期末复习笔记 - 第11章：课后作业与习题精选
// 来源：NLP作业合并.txt + 课程PPT习题

= 第11章 课后作业与习题精选

== 11.1 课后作业一：绪论

=== 题目1：什么是NLP？NLP与AI的关系？

*来源：第一次课后作业 — 绪论*

*解题思路：* 从定义出发，厘清NLP在AI中的位置。

* *自然语言处理（NLP）*：利用计算机对自然语言进行处理、分析和理解的学科。
* *核心目标*：让计算机能够"读懂"和"生成"人类语言。

*NLP与AI的关系：*

#[
  #set text(size: 10pt)
  #table(
    columns: 2,
    [AI分支], [说明],
    [感知智能], [计算机视觉、语音识别等],
    [认知智能 ← NLP], [语言理解与生成，是认知智能的核心],
    [行为智能], [机器人、智能驾驶等],
  )
]

*NLP是AI皇冠上的明珠* —— 语言是人类思维的核心载体，NLP是最具挑战性的AI方向之一。

=== 题目2：NLP研究内容的层次划分

*来源：第一次课后作业 — 绪论*

*解题思路：* 从底层到高层，NLP通常分为五个层次：

1. *语音分析（Phonetic Analysis）*：语音层面，音素、音节的分析
2. *词法分析（Morphological/Lexical Analysis）*：分词、词性标注、命名实体识别
3. *句法分析（Syntactic Analysis）*：短语结构分析、依存句法分析
4. *语义分析（Semantic Analysis）*：词义消歧、语义角色标注、语义依存
5. *语用分析（Pragmatic Analysis）*：篇章理解、指代消解、机器翻译、问答系统

*记忆口诀：* *语音 → 词法 → 句法 → 语义 → 语用*（从低到高）

=== 题目3：NLP过程的主要困难

*来源：第一次课后作业 — 绪论*

*解题思路：* 自然语言与形式化语言的本质差异导致以下困难：

* *歧义性（Ambiguity）*：
  - 词法歧义："他背着总经理和副总经理偷偷出去"
  - 句法歧义："咬死了猎人的狗"
  - 语义歧义："苹果"（水果 vs 公司）
  - 指代歧义："小明告诉小红他考上了"
* *未知性*：新词、新用法不断涌现（如网络用语）
* *形式化困难*：自然语言规则难以用精确的形式化方法描述
* *知识依赖*：需要大量的世界知识和常识

=== 题目4：NLP基本方法的分类

*来源：第一次课后作业 — 绪论*

*解题思路：* 两大类方法的对比：

#[
  #set text(size: 10pt)
  #table(
    columns: 3,
    [方法类别], [核心思想], [典型应用],
    [*基于规则/知识的方法*], [人工编写规则和词典], [早期机器翻译、正则分词],
    [*基于统计/机器学习的方法*], [从数据中学习规律], [HMM词性标注、N-gram语言模型],
    [*基于深度学习的方法*], [端到端神经网络], [BERT、GPT、Seq2Seq],
  )
]

*发展趋势：* 规则方法 → 统计方法 → 深度学习方法（三者融合使用）

== 11.2 课后作业二：词法分析

=== 题目5：基于字分类法的分词方法原理

*来源：第二次课后作业 — 词法分析*

*解题思路：* 将分词问题转化为*字标注*问题。

*核心思想：* 对每个字在词中的位置进行标注，常用 *BIES标注集*：

#[
  #set text(size: 10pt)
  #table(
    columns: 4,
    [标签], [含义], [说明], [示例（"大学"）],
    [B], [Begin], [词首字], [大→B],
    [I], [Inside], [词中字], [（单字词无）],
    [E], [End], [词尾字], [学→E],
    [S], [Single], [单字词], [是→S],
  )
]

*关键步骤：*

1. 将分词任务转化为*序列标注*任务
2. 用标注语料训练分类器（如HMM、CRF、BiLSTM-CRF）
3. 对新句子进行字级别的标注
4. 根据BIES标签还原分词结果

*示例：* "研究生命" → B,E,B,E（研究/生命）或 B,I,E,S（研究生/命）

=== 题目6：命名实体识别（NER）概念

*来源：第二次课后作业 — 词法分析*

*解题思路：* 理解NER的定义、类别和方法。

*定义：* 从文本中识别出具有特定意义的实体，主要分为：

#[
  #set text(size: 10pt)
  #table(
    columns: 2,
    [实体类型], [示例],
    [人名（PER）], [张三、 Barack Obama],
    [地名（LOC）], [北京、太平洋],
    [机构名（ORG）], [北京大学、联合国],
    [时间（TIME）], [2024年、上午八点],
    [数字（NUM）], [3.14、一百],
  )
]

*常用方法：*

* 基于规则：词典匹配 + 模式匹配
* 基于统计模型：HMM、CRF
* 基于深度学习：BiLSTM-CRF、BERT-CRF

*关键步骤：* 定义实体类型 → 标注训练数据 → 训练序列标注模型 → 解码预测

=== 题目7：北大版词性标注集的基本词性代码

*来源：第二次课后作业 — 词法分析*

*解题思路：* 熟记常用词性标注代码。

#[
  #set text(size: 10pt)
  #table(
    columns: 3,
    [代码], [词性], [说明],
    [n], [名词], [一般名词],
    [nr], [人名], [专有名词-人],
    [ns], [地名], [专有名词-地],
    [nt], [机构名], [专有名词-机构],
    [v], [动词], [一般动词],
    [a], [形容词], [一般形容词],
    [d], [副词], [一般副词],
    [m], [数词], [数量词],
    [q], [量词], [单位量词],
    [r], [代词], [人称/指示代词],
    [p], [介词], [在、对、为],
    [c], [连词], [和、与、或],
    [u], [助词], [的、地、得、了],
    [w], [标点], [句号、逗号等],
  )
]

*记忆重点：* nr（人名）、ns（地名）、nt（机构名）是考试中高频考点。

=== 题目8：利用HMM建立词性标注器

*来源：第二次课后作业 — 词法分析*

*解题思路：* 将词性标注建模为*隐马尔可夫模型*的解码问题。

*HMM建模思路：*

* *观测序列 $O$*：句子中的每个词（可观测）
* *状态序列 $S$*：每个词的词性标注（隐状态）
* *目标*：给定观测序列 $O = (o_1, o_2, ..., o_T)$，找到最可能的词性标注序列 $S^* = (s_1, s_2, ..., s_T)$

*公式：*

$ S^* = argmax_S  P(S \| O) = argmax_S  P(O \| S) dot P(S) $

*关键步骤：*

1. 从标注语料中统计*转移概率*（词性→词性）和*发射概率*（词性→词）
2. 对新句子使用*维特比算法*（Viterbi）求解最优词性序列
3. 回溯得到最优标注路径

=== 题目9：正向/反向最大匹配法分词练习

*来源：第二次课后作业 — 词法分析*

*解题思路：* 理解双向最大匹配的消歧策略。

*正向最大匹配（FMM）：*

* 从左到右，每次取词典中最长的匹配词
* 窗口大小 = 词典中最长词的长度

*逆向最大匹配（BMM）：*

* 从右到左，每次取词典中最长的匹配词

*双向最大匹配消歧规则：*

1. 若FMM和BMM分词结果*相同* → 直接输出
2. 若结果*不同* → 选择*单字词更少*的分词结果
3. 若单字词数量也相同 → 通常选择FMM结果

*练习示例：*

假设词典 = {研究, 研究生, 生命, 命, 的, 起源}

句子："研究生命的起源"

*FMM过程：* "研究"（匹配）→ "生命"（匹配）→ "的"（匹配）→ "起源"（匹配）

结果：研究 / 生命 / 的 / 起源（4词，0单字词）

*BMM过程：* "起源"（匹配）→ "的"（匹配）→ "生命"（匹配）→ "研究"（匹配）

结果：研究 / 生命 / 的 / 起源（4词，0单字词）

→ 两者一致，输出：研究 / 生命 / 的 / 起源

== 11.3 HMM相关习题

=== 题目10：盒子和球模型

*来源：HMM章节习题*

*解题思路：* 盒子和球模型是理解HMM的经典类比。

*模型描述：*

* 有多个*盒子*（隐状态 $q_i$），每个盒子中有不同颜色的*球*（观测值 $v_k$）
* 盒子之间按照*转移概率* $a_(i j)$ 随机切换
* 从当前盒子中按*发射概率* $b_j(k)$ 随机抽取一个球
* 只看到抽出的*球的颜色*（观测序列），看不到*来自哪个盒子*（隐状态）

*对应关系：*

#[
  #set text(size: 10pt)
  #table(
    columns: 2,
    [盒子和球模型], [HMM对应],
    [盒子], [隐状态 $q_i$],
    [球的颜色], [观测值 $v_k$],
    [盒子间切换规则], [状态转移概率 $A$],
    [抽球规则], [观测概率 $B$],
    [初始盒子选择], [初始状态概率 $pi$],
  )
]

=== 题目11：HMM各符号含义（Q/V/A/B/π）

*来源：HMM章节 — 基本问题*

*解题思路：* 熟记HMM五元组各符号的定义。

$ lambda = (Q, V, A, B, pi) $

#[
  #set text(size: 10pt)
  #table(
    columns: 3,
    [符号], [名称], [含义],
    [$Q = {q_1, q_2, ..., q_N}$], [状态集合], [所有可能的状态，共 $N$ 个],
    [$V = {v_1, v_2, ..., v_M}$], [观测集合], [所有可能的观测值，共 $M$ 个],
    [$A = [a_(i j)\_(N times N)$], [状态转移概率矩阵], [$a_(i j) = P(s_(t+1) = q_j \| s_t = q_i)$],
    [$B = [b_j(k)\_(N times M)$], [观测概率矩阵], [$b_j(k) = P(o_t = v_k \| s_t = q_j)$],
    [$pi = (pi_1, pi_2, ..., pi_N)$], [初始状态概率向量], [$pi_i = P(s_1 = q_i)$],
  )
]

*关键公式：*

* 转移概率约束：$ sum_(j=1)^N a_(i j) = 1 $
* 发射概率约束：$ sum_(k=1)^M b_j(k) = 1 $
* 初始概率约束：$ sum_(i=1)^N pi_i = 1 $

=== 题目12：前向算法计算

*来源：HMM章节 — 评估问题*

*解题思路：* 前向算法通过*递推*高效计算 $P(O \| lambda)$。

*定义前向概率：*

$ alpha_t(i) = P(o_1, o_2, ..., o_t, s_t = q_i \| lambda) $

*递推公式：*

* *初始化*（$t = 1$）：$ alpha_1(i) = pi_i dot b_i(o_1) $
* *递推*（$t = 2, ..., T$）：$ alpha_t(j) = [sum_(i=1)^N alpha_(t-1)(i) dot a_(i j)] dot b_j(o_t) $
* *终止*：$ P(O \| lambda) = sum_(i=1)^N alpha_T(i) $

*计算步骤：*

1. 计算 $t=1$ 时刻所有状态的 $alpha_1(i)$
2. 从 $t=2$ 到 $t=T$，利用前一步的 $alpha$ 递推计算
3. 对所有 $alpha_T(i)$ 求和得到最终概率

*复杂度：* $O(N^2 T)$（远优于暴力枚举的 $O(N^T T)$）

=== 题目13：后向算法计算

*来源：HMM章节 — 评估问题*

*解题思路：* 后向算法从后往前递推，与前向算法对称。

*定义后向概率：*

$ beta_t(i) = P(o_(t+1), o_(t+2), ..., o_T \| s_t = q_i, lambda) $

*递推公式：*

* *初始化*（$t = T$）：$ beta_T(i) = 1 $
* *递推*（$t = T-1, ..., 1$）：$ beta_t(i) = sum_(j=1)^N a_(i j) dot b_j(o_(t+1)) dot beta_(t+1)(j) $

*关系：* $ P(O \| lambda) = sum_(i=1)^N pi_i dot b_i(o_1) dot beta_1(i) $

=== 题目14：维特比算法求最优路径

*来源：HMM章节 — 解码问题*

*解题思路：* 维特比算法用*动态规划*求最优状态序列。

*定义：*

$ delta_t(i) = max_(s_1,...,s_(t-1)) P(s_1,...,s_(t-1), s_t = q_i, o_1,...,o_t \| lambda) $

$ Psi_t(i) = argmax_(1 <= j <= N) [delta_(t-1)(j) dot a_(j i)] $

*递推公式：*

* *初始化*（$t = 1$）：$ delta_1(i) = pi_i dot b_i(o_1) $，$ Psi_1(i) = 0 $
* *递推*（$t = 2, ..., T$）：
  - $ delta_t(j) = max_(1 <= i <= N) [delta_(t-1)(i) dot a_(i j)] dot b_j(o_t) $
  - $ Psi_t(j) = argmax_(1 <= i <= N) [delta_(t-1)(i) dot a_(i j)] $
* *终止*：
  - $ P^* = max_(1 <= i <= N) [delta_T(i)] $
  - $ s_T^* = argmax_(1 <= i <= N) [delta_T(i)] $
* *回溯*（$t = T-1, ..., 1$）：$ s_t^* = Psi_(t+1)(s_(t+1)^*) $

*关键理解：* $delta$ 记录到当前时刻的最优路径概率，$Psi$ 记录最优路径的前驱状态。

=== 题目15：Baum-Welch算法中γ和ξ的含义

*来源：HMM章节 — 学习问题*

*解题思路：* Baum-Welch是EM算法在HMM上的应用，用于参数估计。

*$gamma$（状态概率）：*

$ gamma_t(i) = P(s_t = q_i \| O, lambda) $

*含义：* 在给定观测序列和模型参数的条件下，$t$ 时刻处于状态 $q_i$ 的概率。

*计算公式：*

$ gamma_t(i) = (alpha_t(i) dot beta_t(i)) / P(O \| lambda) $

*$xi$（转移概率）：*

$ xi_t(i,j) = P(s_t = q_i, s_(t+1) = q_j \| O, lambda) $

*含义：* 在给定观测序列和模型参数的条件下，$t$ 时刻处于状态 $q_i$ 且 $t+1$ 时刻处于状态 $q_j$ 的概率。

*计算公式：*

$ xi_t(i,j) = (alpha_t(i) dot a_(i j) dot b_j(o_(t+1)) dot beta_(t+1)(j)) / P(O \| lambda) $

*用途（参数重估计）：*

$ hat(pi)_i = gamma_1(i) $

$ hat(a)_(i j) = (sum_(t=1)^(T-1) xi_t(i,j)) / (sum_(t=1)^(T-1) gamma_t(i)) $

$ hat(b)_j(k) = (sum_(t=1, o_t=v_k)^T gamma_t(j)) / (sum_(t=1)^T gamma_t(j)) $

=== 题目16：维特比算法中δ和Ψ的含义

*来源：HMM章节 — 解码问题*

*解题思路：* 理解动态规划中两个核心变量的物理意义。

*$delta_t(i)$：*

* *含义*：在 $t$ 时刻以状态 $q_i$ 结尾的所有*部分路径*中，概率最大的那条路径的概率值。
* *物理意义*：到时刻 $t$ 为止，"最优的局部解"。

*$Psi_t(i)$：*

* *含义*：在 $t$ 时刻以状态 $q_i$ 结尾的最优部分路径中，$t-1$ 时刻的状态。
* *物理意义*：记录最优路径的*前驱节点*，用于最终回溯。

*类比：* $delta$ 相当于 Dijkstra 算法中的距离数组，$Psi$ 相当于前驱节点数组。

== 11.4 语言模型相关习题

=== 题目17：N-gram概率计算

*来源：语言模型章节*

*解题思路：* 利用马尔可夫假设简化联合概率。

*N-gram模型公式：*

$ P(w_1, w_2, ..., w_n) = prod_(i=1)^n P(w_i \| w_1, ..., w_(i-1)) $

*马尔可夫假设（近似）：*

$ P(w_i \| w_1, ..., w_(i-1)) approx P(w_i \| w_(i-N+1), ..., w_(i-1)) $

*常见N-gram：*

#[
  #set text(size: 10pt)
  #table(
    columns: 3,
    [名称], [N值], [条件概率],
    [Unigram], [1], [$P(w_i)$],
    [Bigram], [2], [$P(w_i \| w_(i-1))$],
    [Trigram], [3], [$P(w_i \| w_(i-2), w_(i-1))$],
  )
]

*概率估计（最大似然）：*

$ P(w_i \| w_(i-1)) = C(w_(i-1), w_i) / C(w_(i-1)) $

其中 $C$ 为语料中的出现次数。

=== 题目18：困惑度（Perplexity）计算

*来源：语言模型章节*

*解题思路：* 困惑度是评价语言模型好坏的核心指标。

*定义：*

$ PP(W) = P(w_1, w_2, ..., w_N)^(-1/N) $

等价形式（以2为底的对数）：

$ log_2 PP(W) = -(1/N) sum_(i=1)^N log_2 P(w_i \| w_1, ..., w_(i-1)) $

*关键理解：*

* 困惑度越小 → 模型越好（对测试集的"困惑"程度越低）
* 困惑度可以理解为模型在每个位置上*平均可选择的词数*
* $PP = 1$ 表示完美预测，$PP = |V|$ 表示完全随机猜测

*Bigram困惑度：*

$ PP(W) = 2^(-(1/N) sum_(i=1)^N log_2 P(w_i \| w_(i-1))) $

=== 题目19：平滑方法应用

*来源：语言模型章节*

*解题思路：* 解决零概率问题（未登录词/N-gram）。

*（1）加一平滑（Laplace Smoothing）：*

$ P_Laplace(w_i \| w_(i-1)) = (C(w_(i-1), w_i) + 1) / (C(w_(i-1)) + |V|) $

*（2）加k平滑（Lidstone Smoothing）：*

$ P_Lidstone(w_i \| w_(i-1)) = (C(w_(i-1), w_i) + k) / (C(w_(i-1)) + k dot |V|) $

*（3）Good-Turing平滑：*

* 核心思想：用出现 $r+1$ 次的N-gram数量来估计出现 $r$ 次的N-gram的概率
* $r^* = (r+1) dot N_(r+1) / N_r$
* 将"节省"下来的概率分配给未出现的N-gram

*（4）回退法（Back-off）：*

$
P_backoff(w_i \| w_(i-1)) =
 cases(
   C(w_(i-1), w_i) > 0: P_ML(w_i \| w_(i-1)),
   C(w_(i-1), w_i) = 0: alpha(w_(i-1)) dot P_backoff(w_i),
 )
$

*（5）插值法（Interpolation）：*

$ P_interp(w_i \| w_(i-2), w_(i-1)) = lambda_1 P(w_i) + lambda_2 P(w_i \| w_(i-1)) + lambda_3 P(w_i \| w_(i-2), w_(i-1)) $

其中 $lambda_1 + lambda_2 + lambda_3 = 1$

*关键点：* 平滑的本质是*概率重新分配* —— 从高频事件"借"一些概率给低频和未出现事件。

== 11.5 信息检索相关习题

=== 题目20：TF-IDF计算

*来源：信息检索章节*

*解题思路：* TF-IDF衡量词在文档中的重要性。

*公式：*

$ TF-IDF(t, d, D) = TF(t, d) dot IDF(t, D) $

*其中：*

$ TF(t, d) = (词 t 在文档 d 中的出现次数) / (文档 d 的总词数) $

或（对数形式）：$ TF(t, d) = 1 + log(词 t 在文档 d 中的出现次数) $

$ IDF(t, D) = log(N / (1 + n_t)) $

其中 $N$ = 文档总数，$n_t$ = 包含词 $t$ 的文档数

*关键理解：*

* *TF*：词在当前文档中出现越频繁越重要 → *局部重要性*
* *IDF*：词在越少的文档中出现越重要 → *全局区分度*
* 常见词的IDF低（如"的"、"是"），专业术语的IDF高

*计算步骤：*

1. 统计每个词的词频（TF）
2. 统计包含每个词的文档数（DF）
3. 计算IDF
4. 计算TF-IDF = TF × IDF

=== 题目21：余弦相似度计算

*来源：信息检索章节*

*解题思路：* 将文档表示为向量，计算向量间的夹角余弦。

*公式：*

$ "cosine"(d_1, d_2) = (vec(d_1) dot vec(d_2)) / (|vec(d_1)| dot |vec(d_2)|) $

$ = (sum_(i=1)^n w_(1i) dot w_(2i)) / (sqrt(sum_(i=1)^n w_(1i)^2) dot sqrt(sum_(i=1)^n w_(2i)^2)) $

其中 $w_(ji)$ 为文档 $d_j$ 中第 $i$ 个词的TF-IDF权重。

*关键步骤：*

1. 构建词汇表（确定向量维度 $n$）
2. 将每个文档转换为TF-IDF向量
3. 计算两个向量的内积
4. 分别计算两个向量的模长
5. 内积除以模长乘积

*取值范围：* $[-1, 1]$，对于非负权重（TF-IDF），范围为 $[0, 1]$

*意义：* 值越接近1，文档越相似；值越接近0，文档越不相似。

=== 题目22：准确率/召回率/F1计算

*来源：信息检索章节*

*解题思路：* 理解混淆矩阵，掌握P/R/F1的计算。

*混淆矩阵：*

#[
  #set text(size: 10pt)
  #table(
    columns: 3,
    [], [预测相关], [预测不相关],
    [*实际相关*], [TP（真正例）], [FN（假反例）],
    [*实际不相关*], [FP（假正例）], [TN（真反例）],
  )
]

*公式：*

$ "Precision" (P) = TP / (TP + FP) $

→ *检索结果中有多少是真正相关的*

$ "Recall" (R) = TP / (TP + FN) $

→ *所有相关的文档中检索出了多少*

$ F_1 = 2 dot P dot R / (P + R) $

→ *P和R的调和平均*

*关键理解：*

* P和R是一对矛盾：提高阈值→P升高但R降低；降低阈值→R升高但P降低
* F1综合考虑P和R，避免只看一个指标
- 若只看P，可以只返回最相关的1篇，R会很低
- 若只看R，可以全部返回，P会很低

*扩展：$F_beta = (1 + beta^2) dot P dot R / (beta^2 dot P + R)$

* $beta > 1$：偏重召回率
* $beta < 1$：偏重准确率
* $beta = 1$：标准F1

== 11.6 综合复习要点

*核心公式速查：*

#[
  #set text(size: 10pt)
  #table(
    columns: 2,
    [知识点], [核心公式],
    [HMM评估], [$P(O\|\lambda) = \sum_{i=1}^N \alpha_T(i)$],
    [前向算法], [$\alpha_t(j) = [\sum_{i=1}^N \alpha_{t-1}(i) \cdot a_{ij}] \cdot b_j(o_t)$],
    [后向算法], [$\beta_t(i) = \sum_{j=1}^N a_{ij} \cdot b_j(o_{t+1}) \cdot \beta_{t+1}(j)$],
    [维特比], [$\delta_t(j) = \max_i [\delta_{t-1}(i) \cdot a_{ij}] \cdot b_j(o_t)$],
    [N-gram], [$P(w_i|w_{i-1}) = C(w_{i-1},w_i)/C(w_{i-1})$],
    [困惑度], [$PP(W) = P(w_1,...,w_N)^{-1/N}$],
    [TF-IDF], [$TF\text{-}IDF = TF(t,d) \cdot \log(N/n_t)$],
    [余弦相似度], [$\cos(d_1,d_2) = \vec{d_1}\cdot\vec{d_2} / (|\vec{d_1}|\cdot|\vec{d_2}|)$],
    [F1分数], [$F_1 = 2PR/(P+R)$],
  )
]

*高频考点总结：*

1. ⭐ HMM的*三个基本问题*：评估、解码、学习
2. ⭐ *BIES标注*与分词的对应关系
3. ⭐ *维特比*与*前向算法*的区别：max vs sum
4. ⭐ *平滑方法*的本质：概率重新分配
5. ⭐ *P/R/F1*的权衡关系
6. ⭐ *TF-IDF*中IDF的作用：降低常见词的权重