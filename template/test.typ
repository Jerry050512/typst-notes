// ============================================================
// NovaForge 模板测试文件
// 用于验证 preamble.typ 中所有组件的正确性
// ============================================================

#import "conf.typ": *

#show: conf.with(
  title: "NovaForge 模板组件测试",
  authors: (
    (
      name: "测试用户",
      affiliation: "测试机构",
      email: "test@example.com"
    ),
  )
)

= 组件测试总览

本文档用于测试 NovaForge 模板的所有组件，确保颜色、布局和功能正常工作。

== 行内标记测试

这是一段普通文字，包含 #key[关键术语] 的标记。

这是 #super-note[真题标注] 的效果，通常用于标记历年真题。

这是 #hint[提示文字] 的效果，用于辅助说明。

这是 #warn[警告文字] 的效果，用于标记易错点。

== 知识点标题栏测试

#knowtitle[第一个知识点：卷积神经网络基础]

这是知识点的内容部分，应该在标题栏下方正常显示。

#knowtitle[第二个知识点：反向传播算法]

可以有多个知识点标题栏，每个都应该有正确的背景色和文字颜色。

== 公式框测试

#formula[
  $ y = f(W x + b) $
]

#formula[
  $ "Loss" = 1/N sum_(i=1)^N (y_i - hat(y)_i)^2 $
]

公式框应该居中显示，带有边框和合适的内边距。

== 年份标签测试

#yearlabel("2023") 这是2023年的内容

#yearlabel("2024", info: "期末") 这是2024年期末的内容

#yearlabel("2025", info: "考研") 这是2025年考研的内容

= 环境组件测试

== 真题环境测试

#examenv(title: "卷积神经网络的感受野计算")[
  已知一个卷积神经网络包含以下层：
  - 输入层：224×224
  - Conv1：3×3 卷积，stride=1
  - Pool1：2×2 池化，stride=2
  - Conv2：3×3 卷积，stride=1

  请计算 Conv2 输出特征图中一个像素点在原始输入图像上的感受野大小。

  *解答：*

  使用公式 $r_n = r_(n-1) + (k_n - 1) times product_(i=1)^(n-1) s_i$

  - 初始：$r_0 = 1$
  - Conv1 后：$r_1 = 1 + (3-1) times 1 = 3$
  - Pool1 后：$r_2 = 3 + (2-1) times 1 = 4$
  - Conv2 后：$r_3 = 4 + (3-1) times 2 = 8$

  因此感受野为 8×8。
]

== 练习环境测试（留空版）

#pracenv(title: "BatchNorm 的作用", id: "1")[
  请简述 Batch Normalization 的三个主要作用，并说明为什么它能加速训练。

  #v(4em)
]

#pracenv(title: "dropout 概率计算", id: "2")[
  在训练时使用 dropout=0.5，测试时不使用 dropout。如果某层神经元输出为 $bold(x)$，请写出训练和测试时的输出表达式。

  #v(3em)
]

== 答案条目测试

#answer("1")[
  Batch Normalization 的三个主要作用：
  1. *减少内部协变量偏移*：使每层输入分布稳定
  2. *允许更大的学习率*：梯度更稳定
  3. *正则化效果*：减少对其他正则化方法的依赖

  加速训练的原因：通过标准化使得梯度更加稳定，避免梯度消失或爆炸。
]

#answer("2")[
  训练时：$bold(y) = bold(x) dot.circle bold(m) / 0.5$，其中 $bold(m)$ 是 Bernoulli(0.5) 掩码

  测试时：$bold(y) = bold(x)$（已在训练时缩放）
]

= 辅助框体测试

== 警告框测试

#warnbox[
  注意：卷积层的参数量计算公式为 $(k times k times C_"in" + 1) times C_"out"$，其中 +1 是偏置项，很多同学容易遗漏！
]

#warnbox[
  常见错误：混淆感受野和特征图大小。感受野指的是输入图像上的范围，特征图大小指的是输出的空间维度。
]

== 提示框测试

#infobox[
  提示：理解反向传播时，可以从计算图的角度思考，每个节点都是一个函数，边表示数据流动。
]

#infobox[
  技巧：在手算卷积输出大小时，记住公式 $n_"out" = floor((n_"in" + 2p - k) / s) + 1$
]

== 方法速通块测试

#methodblock[
  *快速判断过拟合的三步法：*

  1. 观察训练集和验证集的损失曲线
  2. 如果训练集损失持续下降，验证集损失上升 → 过拟合
  3. 解决方案：增加数据、正则化（L2/dropout）、减小模型复杂度
]

== 直觉理解框测试

#intuition[
  为什么池化层可以提供平移不变性？想象一只猫在图像中稍微移动了几个像素，池化层会从邻域中选择最大值（max pooling），所以即使位置稍有变化，选出的特征也基本相同。
]

#intuition[
  ResNet 的跳跃连接本质上是在问："如果不做变换会怎样？" 这给网络提供了一个"什么都不做"的选项，避免了退化问题。
]

= 章节笔记专用组件测试

== 7步学习框架测试

#stepbox("1", "概念定义")[
  *卷积神经网络（CNN）*：一种专门用于处理网格结构数据（如图像）的神经网络，通过卷积层、池化层和全连接层的组合来提取特征。
]

#stepbox("2", "核心要素")[
  - *卷积核（Filter）*：可学习的权重矩阵
  - *步长（Stride）*：卷积核移动的间隔
  - *填充（Padding）*：在输入边缘添加的像素
  - *特征图（Feature Map）*：卷积操作的输出
]

#stepbox("3", "工作原理")[
  卷积操作通过在输入上滑动卷积核，计算局部区域的加权和。每个卷积核学习识别一种特定的局部模式（如边缘、纹理）。多个卷积核并行工作，提取多个特征通道。
]

#stepbox("4", "直觉理解")[
  想象你在用一个"模式检测器"扫描图像。这个检测器（卷积核）每次看一小块区域，判断这里是否有它要找的模式。如果匹配度高，输出值就大；不匹配就小。
]

#stepbox("5", "典型例题")[

  #example(title: "卷积输出大小计算")[
    输入：$32 times 32 times 3$，卷积核：$5 times 5$，stride=1，padding=2，输出通道数=16

    求：输出特征图的尺寸和参数量

    *解：*

    输出空间大小：$floor((32 + 2 times 2 - 5)/1) + 1 = 32$

    输出尺寸：$32 times 32 times 16$

    参数量：$(5 times 5 times 3 + 1) times 16 = 1216$
  ]
]

#stepbox("6", "巩固练习")[

  #exercise(title: "多层卷积计算")[
    设计一个两层卷积网络：
    - 输入：$64 times 64 times 3$
    - Conv1：$3 times 3$，stride=1，padding=1，32通道
    - Pool1：$2 times 2$，stride=2
    - Conv2：$3 times 3$，stride=1，padding=1，64通道

    计算每层输出尺寸和总参数量。

    *解答：*

    Conv1 输出：$64 times 64 times 32$

    Pool1 输出：$32 times 32 times 32$

    Conv2 输出：$32 times 32 times 64$

    参数量：Conv1: $(3 times 3 times 3 + 1) times 32 = 896$，Conv2: $(3 times 3 times 32 + 1) times 64 = 18496$，总计 19392
  ]
]

#stepbox("7", "易错点")[
  #warnbox[
    易错点1：忘记在参数量计算中加上偏置项（每个输出通道一个偏置）

    易错点2：混淆卷积核数量和输出通道数（它们是相同的）

    易错点3：池化层改变空间维度但不改变通道数
  ]
]

= 综合测试场景

== 模拟复习笔记页面

#knowtitle[损失函数与优化器]

在深度学习中，损失函数用于衡量模型预测与真实值的差距。

#formula[
  $ "MSE" = 1/N sum_(i=1)^N (y_i - hat(y)_i)^2 $
]

#yearlabel("2023", info: "期末") 考察了交叉熵损失函数的推导

#examenv(title: "交叉熵损失函数的数学形式")[
  对于二分类问题，交叉熵损失函数的形式是什么？请写出公式并说明其含义。

  *解答：*

  #formula[
    $ L = -(y log(hat(y)) + (1-y) log(1-hat(y))) $
  ]

  其中 $y in {0,1}$ 是真实标签，$hat(y)$ 是模型预测的概率。当预测正确时损失小，预测错误时损失大。
]

#infobox[
  记忆技巧：交叉熵 = 真实分布与预测分布的"距离"，KL散度的特例。
]

#methodblock[
  *选择损失函数的原则：*

  - 回归问题 → MSE 或 MAE
  - 二分类 → Binary Cross-Entropy
  - 多分类 → Categorical Cross-Entropy
  - 目标检测 → 组合损失（分类 + 定位）
]

#warnbox[
  使用交叉熵时，#key[必须确保模型输出经过 softmax]，否则数值可能不稳定！
]

== 练习区域

#pracenv(title: "优化器对比", id: "3")[
  请比较 SGD、Momentum、Adam 三种优化器的特点和适用场景。

  #v(5em)
]

#answer("3")[
  - *SGD*：最基础，易陷入局部最优，需要精心调整学习率
  - *Momentum*：利用历史梯度动量，加速收敛，减少震荡
  - *Adam*：自适应学习率，结合 Momentum 和 RMSprop，广泛适用，但可能在某些任务上泛化性不如 SGD

  适用场景：简单问题用 SGD，需要快速收敛用 Adam，追求最优泛化性能可尝试 SGD + 精心调参
]

= 测试总结

#infobox[
  本测试文件涵盖了 NovaForge 模板的所有主要组件。如果所有组件都正确显示，说明模板工作正常。
]

#methodblock[
  *使用模板的建议：*

  1. 先确定笔记类型（章节笔记/期末复习/考研）
  2. 选择合适的组件（7步框架/真题环境/练习环境）
  3. 保持统一的颜色主题和排版风格
  4. 合理使用行内标记和辅助框体
]
