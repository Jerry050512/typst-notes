#import "../template/preamble.typ": *

= 词法分析

#knowtitle[词法分析是NLP流水线的入口，常考分词、NER、POS、未登录词，以及“把任务转成序列标注”的思想。]

#intuition[
  计算机看到的是连续字符，而后续句法和语义分析需要“词”作为基本单位。词法分析就是先把原始文本整理成词序列，并给词贴上词性、实体类别等标签。
]

== 定义与任务

/ 词法分析 (Lexical/Morphological Analysis): 从输入字符串中识别词语边界、词形、词性和实体等词级语言信息的处理过程。

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*任务*], [*目标*], [*例子*]),
  [汉语分词], [确定词边界], [自然语言处理 → 自然语言 / 处理],
  [词性标注 POS], [给词标注语法类别], [研究/v 生命/n],
  [命名实体识别 NER], [识别人名、地名、机构名等], [杭州电子科技大学/ORG],
  [未登录词识别], [识别词典中没有的新词或专名], [大模型、双减、元宇宙],
)


== 汉语分词的三大困难

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*困难*], [*含义*], [*例子/提醒*]),
  [分词规范不明确], [什么应作为独立词缺少统一标准], [大白菜、小媳妇、白花等边界可能有争议],
  [切分歧义普遍], [同一串字可有多种合法切分], [交集型：乒乓球拍卖完了；组合型：马上],
  [未登录词频繁], [词表未收录的新词、专名、术语], [影响最大的是人名、地名、机构名等专有名词],
)

== 汉语分词主要方法总览

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*方法*], [*核心思想*], [*优缺点*]),
  [最大匹配法], [基于词典，每次取最长词], [简单高效；不能识别未登录词],
  [最短路径法], [在分词图中选择词数最少的路径], [体现“词语数最少”启发式；仍依赖词典],
  [最大概率法], [用 N-gram 等模型选择概率最大的切分], [能利用统计信息；需要语料估计概率],
  [字分类法], [把每个字标成 B/I/E/S], [可识别新词；本质是序列标注],
)


== 基于词典的分词方法\*

=== 正向最大匹配 FMM

#methodblock[
  *输入*：待分词字符串、词典、最大词长 $L$。

  1. 从句子左端开始，取当前位置起长度不超过 $L$ 的最长候选串。
  2. 若候选串在词典中，则输出该词，并移动到该词之后。
  3. 若不在词典中，则候选串长度减 1 继续匹配。
  4. 若长度减到 1 仍不匹配，则按单字切分。
  5. 重复直到句子结束。
]

=== 反向最大匹配 BMM 与双向匹配

#methodblock[
  *BMM*：从右向左做最大匹配。很多汉语歧义中，BMM 的错误率通常低于 FMM。

  *双向匹配*：
  1. 分别运行 FMM 和 BMM。
  2. 若结果一致，直接采用。
  3. 若不一致，优先选词数较少者。
  4. 若词数相同，优先选单字词较少者。
  5. 仍相同则通常选 BMM。
]

#warnbox[

  *课后习题*：给定词典和句子，分别用 FMM 与 BMM 分词。

  手工分词题要写出每一步匹配结果，不要只给最终答案。题目给词典时，必须严格按词典切分，不能凭语感加词。
]

== 基于字分类的分词\*

/ 基于字分类法: 将分词转化为字级别序列标注问题，对每个字标注其在词中的位置。

#table(
  columns: (auto, auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*标签*], [*英文*], [*含义*], [*例子*]),
  [B], [Begin], [词首字], [“研究”中的”研”],
  [I/M], [Inside/Middle], [词中字], [“自动化”中的”动”],
  [E], [End], [词尾字], [“研究”中的”究”],
  [S], [Single], [单字词], [“我”],
)

#example(title: [BIES 标注])[
  “我爱自然语言处理” 可标为：我/S 爱/S 自/B 然/I 语/I 言/E 处/B 理/E。
]

#warnbox[

  *课后习题*：简述基于字分类法的分词方法原理。

  答题要点：将分词转化为字序列标注任务，对每个字标注 B/I/E/S，表示词首/词中/词尾/单字词。可识别未登录词，本质是序列标注问题，常用 HMM、CRF、BiLSTM-CRF 等模型。
]

== 命名实体识别 NER\*

/ 命名实体识别 (Named Entity Recognition, NER): 识别文本中对实体的#key[命名性指称]，具体来讲就是识别文本中的人名、地名、机构名、时间名等专有名词。

#intuition[
  对同一个实体，可以有三种指称方式：
  - #key[命名性指称]：专有名词，如"刘国梁"
  - #key[名词性指称]：描述性名词短语，如"中国乒乓球男队总教练"
  - #key[代词性指称]：代词，如"他"

  NER 主要关注命名性指称的识别。
]

常见实体类型：人名 PER、地名 LOC、机构名 ORG、时间 TIME、数值 NUM。

#warnbox[

  *课后习题*：简述命名实体识别的概念。

  答题要点：NER 是识别文本中对实体的命名性指称，主要识别人名、地名、机构名、时间名等专有名词。需要区分命名性指称、名词性指称和代词性指称三种类型。
]

=== NER 的主要方法

*基于规则的方法*：通过人工构建规则库（姓氏用字规则、识别规则等），结合优先级策略（高置信度规则优先、最长匹配优先、细粒度优先）进行实体识别。

*基于统计的方法*：把 NER 抽象成字序列标注任务，标注 BIO 或 BIOES，常用 CRF 模型。

*基于深度学习的方法*：word embedding + RNN/LSTM/BiLSTM/BERT，端到端学习，无需人工设计特征。

== POS 标注与 HMM 建模\*

#methodblock[
  *HMM 词性标注建模*：
  1. 观测序列 $W = (w_1, w_2, dots, w_n)$：句子中的词。
  2. 隐状态序列 $T = (t_1, t_2, dots, t_n)$：词性标签。
  3. 初始概率：$P(t_1)$。
  4. 转移概率：$P(t_i | t_(i-1))$。
  5. 发射概率：$P(w_i | t_i)$。
  6. 用 Viterbi 算法搜索最可能的词性序列。
]

#warnbox[

  *课后习题*：简述如何利用 HMM 建立词性标注器。

  答题要点：将词性标注建模为 HMM 问题。观测序列是句子中的词，隐状态序列是词性标签。需要初始概率、转移概率（前一个词性到当前词性）、发射概率（词性到词）。使用 Viterbi 算法解码最优词性序列。
]

== HMM vs CRF vs 深度学习

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else if calc.odd(row) { white } else { rgb("#f8f9fa") },
  table.header([*方法*], [*优点*], [*缺点*]),
  [HMM], [简单、可解释、解码快], [独立性假设强，特征有限],
  [CRF], [可融合丰富上下文特征], [训练复杂],
  [BiLSTM-CRF], [上下文建模强], [依赖标注数据],
  [BERT类模型], [处理OOV和语义上下文强], [计算成本高],
)

== 未登录词 OOV

/ 未登录词 (Out-of-Vocabulary, OOV): 词典中不存在但文本中出现的词，如新词、专名、缩略语和网络词。

处理策略：将分词转为字级序列标注；用 HMM、CRF、BiLSTM-CRF 或 BERT 学习上下文模式；与 NER 结合识别人名、地名、机构名。


== 北大词性标注基本代码\*

#table(
  columns: (auto, auto, auto, auto),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 5pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*代码*], [*词性*], [*代码*], [*词性*]),
  [n], [名词], [v], [动词],
  [a], [形容词], [d], [副词],
  [m], [数词], [q], [量词],
  [r], [代词], [p], [介词],
  [c], [连词], [u], [助词],
  [t], [时间词], [s], [处所词],
  [f], [方位词], [b], [区别词],
  [i], [成语], [l], [习用语],
  [j], [简称略语], [o], [拟声词],
  [e], [叹词], [y], [语气词],
  [z], [状态词], [x], [非语素字],
  [nr], [人名], [ns], [地名],
  [nt], [机构名], [w], [标点],
)

#warnbox[

  *课后习题*：列举北大版词性标注集的基本词性代码。

  答题要点：n-名词、v-动词、a-形容词、d-副词、m-数词、q-量词、r-代词、p-介词、c-连词、u-助词、nr-人名、ns-地名、nt-机构名、w-标点等。
]

#example(title: [最大匹配分词示例])[
  词典：自动化研究所、自动化、研究所、自动、研究、取得、成就、非常、常人、能干、干到。

  句子：自动化研究所取得的成就非常人能干到。

  - FMM：自动化研究所 / 取得 / 的 / 成就 / 非常 / 人 / 能干 / 到。
  - BMM：自动化研究所 / 取得 / 的 / 成就 / 非 / 常人 / 能 / 干到。

  说明：”的、人、到、非、能”不在词典中时按单字切分。题目要求分别用 FMM/BMM 时，不要自行做双向消歧后只给一个结果。
]

== 本章高频题

#exercise(title: [词法分析])[
  1. 简述基于字分类法的分词方法原理。
  2. 简述命名实体识别的概念及常见实体类别。
  3. 列举北大版词性标注集中常见代码：n、v、a、d、m、q、r、p、c、u。
  4. 如何利用 HMM 建立词性标注器？
  5. 给定词典和句子，分别用 FMM 与 BMM 分词。
]
