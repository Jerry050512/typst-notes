#import "../template/preamble.typ": *

= 形式语言与自动机

#knowtitle[本章是句法分析的理论基础。考试重点是文法四元组、Chomsky层级、CFG派生树，以及自动机和文法类型的对应关系。]

== 基本概念

/ 字母表 ($Sigma$): 符号的非空有限集合。
/ 字符串 ($w$): 由字母表中符号组成的有限序列。
/ 空串 ($epsilon$): 长度为 0 的字符串。
/ 语言 ($L$): 某个字母表上字符串的集合。

语言描述三种方式：穷举法、文法/产生式系统、自动机法。#key[文法偏生成结构，自动机偏识别合法性]。


== 文法四元组\*

#formula[$ G = (N, Sigma, P, S) $]

其中：
- $N$：非终结符集合；
- $Sigma$：终结符集合，且 $N inter Sigma = emptyset$；
- $P$：产生式规则集合；
- $S$：开始符号，$S in N$。

#warnbox[

  *课后习题*：文法四元组各部分含义。

  文法定义题必须写完整四元组。不要把终结符和非终结符混在一起。$N$ 是非终结符集合，$Sigma$ 是终结符集合，$P$ 是产生式规则，$S$ 是开始符号。
]

== Chomsky 文法层级\*

#table(
  columns: (auto, auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else if calc.odd(row) { white } else { rgb("#f8f9fa") },
  table.header([*类型*], [*名称*], [*产生式限制*], [*对应自动机*]),
  [0型], [无限制文法], [左部至少含一个非终结符], [图灵机],
  [1型], [上下文相关文法], [长度不减，形如 $alpha A beta -> alpha gamma beta$], [线性有界自动机],
  [2型], [上下文无关文法 CFG], [左部为单个非终结符，$A -> gamma$], [下推自动机],
  [3型], [正则文法], [右线性或左线性规则], [有限自动机],
)

#key[包含关系]：正则语言 ⊂ 上下文无关语言 ⊂ 上下文相关语言 ⊂ 递归可枚举语言。

#warnbox[

  *课后习题*：1型文法对应哪种自动机？各类文法的产生式限制是什么？

  答题要点：
  - 0型 → 图灵机
  - 1型（上下文相关） → #key[线性有界自动机]
  - 2型（上下文无关 CFG） → #key[下推自动机]
  - 3型（正则文法） → #key[有限自动机]

  判断文法类型看产生式规则：2型左部必须是单个非终结符，3型必须是右线性或左线性。
]

== CFG 与派生树\*

/ 上下文无关文法 (Context-Free Grammar, CFG): 每条产生式左部都是单个非终结符的文法，常用于描述短语结构。

#methodblock[
  *画派生树步骤*：
  1. 根节点写开始符号 $S$。
  2. 每次选择一个非终结符，根据产生式展开。
  3. 直到所有叶节点都是终结符。
  4. 从左到右读叶节点，检查是否等于目标句子。
]

#warnbox[

  *课后习题*：给定 CFG，画出句子的派生树。

  同一个句子若能对应多棵不同派生树，说明文法存在歧义。句法分析题中要注意是否要求列出多种结构。
]

#example(title: [派生树示例])[
  设文法 $G$：$S -> A B$；$A -> A a | b B$；$B -> a | S b$

  句子形式：$b a a b a a b$

  从 $S$ 开始，逐步展开非终结符：
  - $S -> A B$
  - $A -> b B -> b a$（得到第一个 $b a$）
  - $B$ 展开后续部分...

  最终叶节点从左到右读出应该等于目标句子。
]


== 推导与句子

/ 句型: 从开始符号 $S$ 可推导出的符号串。
/ 句子: 不含非终结符的句型。
/ 文法生成的语言: $L(G)$ 是文法 $G$ 能生成的全部句子的集合。

#warnbox[判断”文法是否有歧义”看同一句子是否存在两棵不同语法树，不能只看是否有两种推导顺序。]


== 有限自动机W\*

/ 有限自动机 (Finite Automaton, FA): 用有限状态和状态转移识别正则语言的计算模型。

#key[DFA 五元组定义]：$M = (Sigma, Q, delta, q_0, F)$

其中：
- $Sigma$：#key[输入符号的有穷集合]
- $Q$：状态的有限集合
- $delta$：状态转移函数，$delta: Q times Sigma -> Q$
- $q_0$：初始状态，$q_0 in Q$
- $F$：终止状态集合，$F subset.eq Q$

#warnbox[

  *课后习题*：确定的有限自动机 M 五元组中 $Sigma$ 代表什么？

  答题要点：$Sigma$ 代表#key[输入符号的有穷集合]。$Q$ 是状态集合，$delta$ 是转移函数，$q_0$ 是初始状态，$F$ 是终止状态集合。
]

DFA 与 NFA 区别：
- DFA：每个状态对每个输入符号恰有一个确定转移；
- NFA：允许多个可能转移，也可有 $epsilon$ 转移；
- 二者识别能力等价，都识别正则语言。

== 正则文法与自动机转换\*

#key[正则文法与 FA 构造口诀]：右线性规则 $A -> a B$ 对应从状态 $A$ 读入 $a$ 到状态 $B$；$A -> a$ 对应读入 $a$ 到终态。

#methodblock[
  *右线性正则文法转自动机步骤*：

  1. 每个非终结符对应一个状态
  2. 规则 $A -> a B$ 转为：状态 $A$ 读入 $a$ 转移到状态 $B$
  3. 规则 $A -> a$ 转为：状态 $A$ 读入 $a$ 转移到终态
  4. 开始符号对应初始状态
]

#example(title: [正则文法转自动机])[
  右线性正则文法 $G$：
  - $S -> a A$
  - $A -> b B$
  - $B -> a | b B$

  转换为状态转换图：
  - 初始状态：$S$
  - $S$ 读入 $a$ 转到 $A$
  - $A$ 读入 $b$ 转到 $B$
  - $B$ 读入 $a$ 转到终态（接受状态）
  - $B$ 读入 $b$ 转到 $B$（自环）

  状态转换图：$S ->^a A ->^b B ->^a ["终态"]$，$B$ 有 $b$ 的自环。
]

#warnbox[
  
  *课后习题*：将右线性正则文法转换为对应自动机的状态转换图。

  答题要点：非终结符对应状态，$A -> a B$ 对应从 $A$ 读 $a$ 到 $B$，$A -> a$ 对应从 $A$ 读 $a$ 到终态。画图时标清初始状态和终态。
]


== 自动机类型总结

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 5pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*自动机*], [*形式*], [*识别语言*]),
  [有限自动机 FA], [$M=(Sigma,Q,delta,q_0,F)$], [正则语言（3型文法）],
  [下推自动机 PDA], [比 FA 多一个栈], [上下文无关语言（2型文法）],
  [线性有界自动机 LBA], [受输入长度线性限制的图灵机], [上下文相关语言（1型文法）],
  [图灵机 TM], [无限纸带 + 读写头], [递归可枚举语言（0型文法）],
)


== 本章高频题

#exercise(title: [形式语言与自动机])[
  1. 写出文法四元组 $G=(N, Sigma, P, S)$ 并解释各部分含义。
  2. 比较 0/1/2/3 型文法及其对应自动机。
  3. 说明 DFA 五元组 $(Sigma, Q, delta, q_0, F)$ 各部分含义。
  4. 给定 CFG，画出句子的派生树并判断是否有歧义。
  5. 将右线性正则文法转换为自动机状态转换图。
]
