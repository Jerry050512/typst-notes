#import "../template/preamble.typ": *

= 形式语言与自动机

#knowtitle[本章是句法分析的理论基础。考试重点是文法四元组、Chomsky层级、CFG派生树，以及自动机和文法类型的对应关系。]

== 基本概念

/ 字母表 ($Sigma$): 符号的非空有限集合。
/ 字符串 ($w$): 由字母表中符号组成的有限序列。
/ 空串 ($epsilon$): 长度为 0 的字符串。
/ 语言 ($L$): 某个字母表上字符串的集合。


== 字符串与语言运算

- 字符串长度：$|w|$ 表示 $w$ 中符号个数，$|epsilon|=0$。
- 连接：$x y$ 表示把字符串 $y$ 接在 $x$ 后面。
- 幂：$x^0=epsilon$，$x^n = x x^(n-1)$。
- 闭包：$V^*$ 表示字母表 $V$ 上所有字符串集合，包括空串；$V^+$ 不包括空串。

语言描述三种方式：穷举法、文法/产生式系统、自动机法。#key[文法偏生成结构，自动机偏识别合法性]。


== 文法四元组

#formula[$ G = (N, Sigma, P, S) $]

其中：
- $N$：非终结符集合；
- $Sigma$：终结符集合，且 $N inter Sigma = emptyset$；
- $P$：产生式规则集合；
- $S$：开始符号，$S in N$。

#warnbox[文法定义题必须写完整四元组。不要把终结符和非终结符混在一起。]

== Chomsky 文法层级

#table(
  columns: (auto, auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else if calc.odd(row) { white } else { rgb("#f8f9fa") },
  table.header([*类型*], [*名称*], [*产生式限制*], [*对应自动机*]),
  [0型], [无限制文法], [左部至少含一个非终结符], [图灵机],
  [1型], [上下文相关文法], [长度不减，形如 $alpha A beta -> alpha gamma beta$], [线性有界自动机],
  [2型], [上下文无关文法 CFG], [左部为单个非终结符，$A -> gamma$], [下推自动机],
  [3型], [正则文法], [右线性或左线性规则], [有限自动机],
)

#key[包含关系]：正则语言 ⊂ 上下文无关语言 ⊂ 上下文相关语言 ⊂ 递归可枚举语言。

== CFG 与派生树

/ 上下文无关文法 (Context-Free Grammar, CFG): 每条产生式左部都是单个非终结符的文法，常用于描述短语结构。

#methodblock[
  *画派生树步骤*：
  1. 根节点写开始符号 $S$。
  2. 每次选择一个非终结符，根据产生式展开。
  3. 直到所有叶节点都是终结符。
  4. 从左到右读叶节点，检查是否等于目标句子。
]

#warnbox[同一个句子若能对应多棵不同派生树，说明文法存在歧义。句法分析题中要注意是否要求列出多种结构。]


== 推导与句子

/ 直接推导: 若产生式 $A -> beta$ 可把 $alpha A gamma$ 改写为 $alpha beta gamma$，记为 $alpha A gamma => alpha beta gamma$。
/ 最左推导: 每一步都改写最左边的非终结符。
/ 最右推导: 每一步都改写最右边的非终结符，又称规范推导。
/ 句型: 从开始符号 $S$ 可推导出的符号串。
/ 句子: 不含非终结符的句型。
/ 文法生成的语言: $L(G)$ 是文法 $G$ 能生成的全部句子的集合。

#warnbox[判断“文法是否有歧义”看同一句子是否存在两棵不同语法树，不能只看是否有两种推导顺序。]


== 自动机

=== 有限自动机 FA

/ 有限自动机: 用有限状态和状态转移识别正则语言的计算模型。

DFA 与 NFA 区别：
- DFA：完整定义下，每个状态对每个输入符号恰有一个确定转移；允许缺省转移时称为部分 DFA；
- NFA：允许多个可能转移，也可有 $epsilon$ 转移；
- 二者识别能力等价，都识别正则语言。

=== 下推自动机 PDA

/ 下推自动机: 有一个栈作为辅助存储的自动机，可识别上下文无关语言。

#intuition[有限自动机只有“当前状态”，记忆能力有限；PDA 多了一个栈，因此可以处理括号匹配、嵌套结构等 CFG 语言。]


=== 自动机形式定义速记

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 5pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*自动机*], [*形式*], [*识别语言*]),
  [DFA], [$M=(Sigma,Q,delta,q_0,F)$，$delta: Q times Sigma -> Q$], [正则语言],
  [PDA], [$M=(Sigma,Q,Gamma,delta,q_0,Z_0,F)$，比 FA 多一个栈], [上下文无关语言],
  [LBA], [受输入长度线性限制的图灵机], [上下文相关语言],
  [TM], [无限纸带 + 读写头 + 状态控制], [递归可枚举语言],
)

#key[正则文法与 FA 构造口诀]：右线性规则 $A -> a B$ 对应从状态 $A$ 读入 $a$ 到状态 $B$；$A -> a$ 对应读入 $a$ 到终态。


== 本章高频题

#exercise(title: [形式语言与自动机])[
  1. 写出文法四元组 $G=(N, Sigma, P, S)$ 并解释各部分含义。
  2. 比较 0/1/2/3 型文法及其对应自动机。
  3. 给定 CFG，画出句子的派生树并判断是否有歧义。
  4. 说明有限自动机与正则文法的关系。
]
