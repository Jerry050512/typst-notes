#import "../template/preamble.typ": *

= 句法分析

#knowtitle[句法分析常考三类概念比较：句法结构分析、浅层句法分析、依存句法分析；也可能考CFG、PCFG、CYK算法和句法树。]

#intuition[
  词法分析解决"有哪些词"，句法分析解决"这些词如何组成句子"。它要回答：谁修饰谁？哪个短语是主语？哪个词支配哪个词？
]

== 定义与类型\*

/ 句法分析 (Syntactic Parsing): 在句子层面对词序列进行形式化分析，确定句子的结构或词语之间的依存关系。

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*类型*], [*目标*], [*输出*]),
  [句法结构分析], [分析完整短语结构], [成分句法树],
  [浅层句法分析], [识别局部短语块，不构造完整树], [NP/VP等短语块],
  [依存句法分析], [分析词与词之间的支配关系], [依存弧和依存标签],
)

#warnbox[

  *课后习题*：比较说明句法结构分析、浅层句法分析、依存句法分析的概念。

  答题要点：
  - *句法结构分析*：使用 CFG 等文法，将句子分解为层级短语结构（如 S、NP、VP），输出完整的成分句法树
  - *浅层句法分析*：只识别局部短语块（如 Base NP、VG、PP），不构造完整句法树，不处理远距离依赖
  - *依存句法分析*：分析词与词之间的支配和依存关系，输出依存树，强调中心词—依存词关系
]

== 短语结构分析\*

/ 短语结构分析: 用 CFG 等文法把句子分解为层级短语结构。

常见符号：
- *S*：句子（Sentence）
- *NP*：名词短语（Noun Phrase）
- *VP*：动词短语（Verb Phrase）
- *PP*：介词短语（Prepositional Phrase）
- *ADJP*：形容词短语
- *ADVP*：副词短语

#example(title: [短语结构分析示例])[
  句子："学生 在 教室 学习"

  句法树：
  ```
  S
  ├─ NP (学生)
  └─ VP
     ├─ PP
     │  ├─ P (在)
     │  └─ NP (教室)
     └─ V (学习)
  ```
]

#warnbox[

  *课后习题*：解释术语 S、VP、NP、PP 的含义。

  答题要点：
  - *S*：句子，整个句子的根节点
  - *VP*：动词短语，以动词为中心的短语
  - *NP*：名词短语，以名词为中心的短语
  - *PP*：介词短语，以介词为中心的短语
]

== 依存句法分析\*

/ 依存句法分析: 认为句子的核心是词与词之间的依存关系；除根节点外，每个词通常依附于一个中心词。

常见依存关系：
#table(
  columns: (auto, auto, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*缩写*], [*中文*], [*说明*]),
  [SBV], [主谓关系], [主语依存于谓词],
  [VOB], [动宾关系], [宾语依存于动词],
  [POB], [介宾关系], [介词宾语依存于介词],
  [ATT], [定中关系], [修饰名词的定语],
  [ADV], [状中关系], [修饰谓词的状语],
  [CMP], [动补关系], [补语补充说明动词],
  [DE], ["的"字结构], [连接定语和中心词],
)

#example(title: [依存句法分析示例])[
  句子："学生 在 教室 学习"

  依存关系：
  - "学生" --SBV--> "学习"（主谓）
  - "在" --ADV--> "学习"（状中）
  - "教室" --POB--> "在"（介宾）
  - 根节点："学习"
]

#warnbox[

  *课后习题*：解释术语 SBV、VOB、ATT、DE 的含义。

  答题要点：
  - *SBV*：主谓关系（Subject-Verb），主语依存于谓词
  - *VOB*：动宾关系（Verb-Object），宾语依存于动词
  - *ATT*：定中关系（Attribute），修饰名词的定语
  - *DE*："的"字结构，连接定语和中心词
]

#intuition[
  依存句法强调#key[词与词之间的中心—依存关系]，输出通常是有向依存树。许多句子以主要谓词为根。
]


== 句法分析方法路线

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*路线*], [*核心*], [*优缺点*]),
  [人工规则方法], [人工 CFG 规则库 + 搜索推理], [直观可解释；规则编制量大，长句和歧义处理困难],
  [统计方法], [从树库学习规则概率或打分模型], [能选择最优结构，减少主观性；依赖标注语料],
)


== PCFG\*

/ 概率上下文无关文法 (Probabilistic Context-Free Grammar, PCFG): 在 CFG 的每条产生式上附加概率，用于选择最可能的句法树。

#formula[$ P(T) = product_(r in T) P(r) $]

其中 $T$ 是一棵句法树，$r$ 是树中使用到的产生式。

#key[PCFG 特点]：
1. 每条产生式 $A -> beta$ 都有一个概率 $P(A -> beta)$
2. 对于同一个左部 $A$，所有右部的概率和为 1：$sum_(A -> beta) P(A -> beta) = 1$
3. 句法树的概率是所有使用到的产生式概率的乘积

#key[PCFG 优势]：
1. #key[消歧能力]：在存在多棵句法树时，可以根据规则概率选择最可能的结构
2. #key[鲁棒性]：对于不完全符合规则的句子，也能给出概率评分
3. #key[可学习性]：可以从树库语料中自动学习产生式概率
4. #key[定量评估]：提供了定量的句法结构评估标准

#warnbox[

  *课后习题*：简要说明 PCFG 模型特点，以及采用 PCFG 模型进行句法分析的优势。

  答题要点：
  - *特点*：在 CFG 基础上为每条产生式附加概率，句法树概率是所有使用产生式概率的乘积
  - *优势*：能够消除歧义（选择概率最大的结构）、从树库中自动学习概率、提供定量评估标准
]

== CYK 算法\*

/ CYK算法: 一种自底向上的动态规划句法分析算法，要求文法转为乔姆斯基范式（CNF）。

/ 乔姆斯基范式 (Chomsky Normal Form, CNF): 一种标准化的 CFG 形式，所有产生式只能是两种形式之一：
- $A -> B C$（二元规则：非终结符推出两个非终结符）
- $A -> a$（终结符规则：非终结符推出一个终结符）

#methodblock[
  *CNF 范式化步骤*：

  1. 消除右部长度大于 2 的规则，例如：
     - $S -> V P space A u x space N P$ 拆成：$S -> V P space X_1$，$X_1 -> A u x space N P$

  2. 消除右部既有终结符又有非终结符的规则，例如：
     - $V C -> v space a$ 保持不变（$v$ 和 $a$ 都是非终结符，已是 $A -> B C$ 形式）
     - 如果有 $A -> a B$，需拆成 $A -> X_a space B$，$X_a -> a$

  3. 消除单一非终结符规则（如 $N P -> n$）：严格 CNF 中需消除；也可保留，由 CYK 在对角线做闭包处理
]

#methodblock[
  *CYK 填表步骤*：

  1. *初始化*：构造 $n times n$ 的三角表格（$n$ 是句子长度）

  2. *填对角线*（跨度=1）：对每个词 $w_i$，找所有能推出它的非终结符
     - 例如："咬" → {v}，"死" → {a}

  3. *填上三角*（跨度=2 到 $n$）：
     - 对区间 $[i, j]$，枚举切分点 $k in [i, j-1]$
     - 查找是否存在规则 $A -> B C$，其中 $B in [i,k]$，$C in [k+1,j]$
     - 如果存在，将 $A$ 加入 $[i,j]$

  4. *判断句子合法性*：检查顶层 $[1,n]$ 是否包含开始符号 $S$

  5. *回溯构造句法树*：记录每个单元格中非终结符的来源，递归回溯
]

#warnbox[
  
  *课后习题*：采用 CYK 算法对句子进行句法结构分析，注意乔姆斯基范式化、注意多种结构树。

  CYK 题关键步骤：
  1. *先范式化*：将文法转换为 CNF
  2. *再填表*：按跨度从小到大填充三角表
  3. *记录回溯*：保存推导来源以便构造句法树
  4. *处理歧义*：如果一个单元格有多种推导，说明存在多棵结构树
]


== CYK 算法应用示例

#example(title: [CYK 算法完整示例])[
  给定文法和句子：咬 死 了 猎人 的 狗

  *原始文法*：
  - $S -> V C space N P | V P space A u x space N P$
  - $V C -> v space a | V C space u t l$
  - $N P -> n | N P space A u x space N P | N P space N P$
  - $V P -> V C space N P$
  - $v -> "咬"$，$a -> "死"$，$u t l -> "了"$，$n -> "猎人" | "狗"$，$A u x -> "的"$

  *步骤 1：CNF 范式化*

  将三元规则拆分为二元规则：
  - $S -> V C space N P$（已是二元）
  - $S -> V P space X_1$，$X_1 -> A u x space N P$
  - $V C -> v space a$（两个非终结符，已是二元规则 $A -> B C$，无需处理）
  - $V C -> V C space u t l$（已是二元）
  - $N P -> n$（单一非终结符规则，本例保留）
  - $N P -> N P space X_2$，$X_2 -> A u x space N P$
  - $N P -> N P space N P$（已是二元）
  - $V P -> V C space N P$（已是二元）

  *步骤 2：填 CYK 表*

  对角线（跨度=1）：
  - "咬"：{v}
  - "死"：{a}
  - "了"：{utl}
  - "猎人"：{n, NP}
  - "的"：{Aux}
  - "狗"：{n, NP}

  跨度=2：
  - "咬死"：{VC}（由 v + a）
  - "猎人的"：无
  - "的狗"：{X₁, X₂}（由 Aux + NP，$X_1$ 和 $X_2$ 均有 $A u x space N P$ 产生式）

  跨度=3 及以上：继续组合...

  *步骤 3：歧义分析*

  该句子有两种结构：
  1. *结构 1*：咬死了 [猎人 的 狗] — "猎人的狗"是宾语
  2. *结构 2*：[咬死了 猎人] 的 狗 — "狗"是中心词，"咬死了猎人"是修饰语

  如果顶层单元格包含 $S$，说明句子可由文法生成。
]

#warnbox[
  考试要点：
  - 必须先进行 CNF 范式化，不能直接使用原始文法
  - 填表时只能使用二元规则进行组合
  - 注意记录多种推导路径（歧义结构）
  - 可能要求画出完整的句法树
]


== 本章高频题

#exercise(title: [句法分析])[
  1. 比较句法结构分析、浅层句法分析、依存句法分析。
  2. 解释 S、VP、NP、PP；SBV、VOB、ATT、DE 的含义。
  3. 简述 PCFG 的特点和优势。
  4. 用 CYK 算法分析给定 CFG 和句子，注意乔姆斯基范式化和多棵结构树。
]
