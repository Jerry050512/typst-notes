#import "../template/preamble.typ": *

= 课后作业与习题解答

#knowtitle[本章只保留“课程作业原题 + 必会解答模板”。目标不是扩展刷题，而是保证期末复习时每一道作业题都有可直接默写的答案。]

== 作业 1：绪论

=== 1. 什么是 NLP？NLP 与 AI 的关系？

#methodblock[
  *答题模板*：自然语言处理（NLP）研究如何让计算机处理、理解、转换和生成自然语言，是人工智能的重要分支，尤其属于认知智能的重要方向。

  1. #key[处理]：分词、词性标注、句法分析、信息抽取等。
  2. #key[理解]：识别词义、句义、篇章关系和用户意图。
  3. #key[生成/转换]：机器翻译、问答、摘要、对话和文本生成。
  4. #key[与 AI 的关系]：AI 研究智能行为，NLP 让机器具备语言理解和表达能力，是实现人机自然交互的重要基础。
]

=== 2. NLP 研究内容可以划分为哪些层次？

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*层次*], [*内容*], [*例子*]),
  [应用层], [直接面向用户的 NLP 系统], [机器翻译、信息检索、问答系统、自动文摘、信息抽取、对话系统],
  [应用基础层], [支撑应用的语言分析技术], [词法分析、句法分析、语义分析、篇章分析],
  [基础层], [模型、数据和知识资源], [语言模型、语料库、语言知识库、字符集编码],
)

补充语言分析顺序：语音/文字 → 词法 → 句法 → 语义 → 语用/篇章。

=== 3. NLP 过程的主要困难是什么？请举例说明。

#methodblock[
  1. #key[歧义性]：同一语言形式有多种解释，如“咬死了猎人的狗”。
  2. #key[上下文依赖]：词义和指代要结合语境，如“苹果”可指水果或公司。
  3. #key[知识依赖]：理解语言需要常识和领域知识。
  4. #key[开放性/未知性]：新词、新义、新句式不断出现，如网络用语。
  5. #key[形式化困难]：自然语言规则有例外，不像程序语言那样严格明确。
]

=== 4. NLP 基本方法分为哪几类？

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*方法*], [*核心思想*], [*特点*]),
  [规则/理性主义方法], [人工编写语言规则和知识库], [可解释，小数据可用；规则难穷尽，维护成本高],
  [统计/经验主义方法], [从语料中学习概率和模式], [覆盖面广；依赖数据，解释性弱],
  [深度学习方法], [神经网络端到端学习表示], [效果强，可自动学习特征；训练成本高],
  [混合方法], [规则、统计、知识结合], [工程系统常用],
)

== 作业 2：词法分析

=== 1. 简述基于字分类法的分词方法原理。

#methodblock[
  字分类法把汉语分词转化为 #key[字级序列标注]：为每个字标注它在词中的位置，再根据标签还原词边界。

  常用 BIES 标签：B=词首，I=词中，E=词尾，S=单字词。
]

#example(title: [BIES])[
  “我爱自然语言处理” → 我/S 爱/S 自/B 然/I 语/I 言/E 处/B 理/E。
]

=== 2. 简述命名实体识别的概念。

/ 命名实体识别 (NER): 从文本中识别对实体的命名性指称，并标注其类别。

常见类别：人名 PER、地名 LOC、机构名 ORG；扩展类别包括时间、日期、数值、专有产品名等。

#warnbox[“中国乒乓球男队总教练”是名词性指称，“刘国梁”是命名性指称，“他”是代词性指称。NER 重点识别命名性指称。]

=== 3. 北大版词性标注集的基本词性代码。

#table(
  columns: (auto, auto, auto, auto),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 5pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*代码*], [*词性*], [*代码*], [*词性*]),
  [n], [名词], [v], [动词],
  [a], [形容词], [d], [副词],
  [m], [数词], [q], [量词],
  [r], [代词], [p], [介词],
  [c], [连词], [u], [助词],
  [t], [时间词], [s], [处所词],
  [f], [方位词], [b], [区别词],
  [i], [成语], [l], [习用语],
  [j], [简称略语], [o], [拟声词],
  [e], [叹词], [y], [语气词],
  [z], [状态词], [x], [非语素字],
  [nr], [人名], [ns], [地名],
  [nt], [机构名], [nz], [其他专名],
  [w], [标点], [], [],
)

=== 4. 如何利用 HMM 建立词性标注器？

#methodblock[
  1. #key[建模]：观测序列是词序列，隐藏状态序列是词性序列。
  2. #key[参数]：初始概率 `π`、词性转移概率 `A`、词性到词的发射概率 `B`。
  3. #key[训练]：从已标注语料统计 `π/A/B`。
  4. #key[解码]：对新句子用 Viterbi 求最可能词性序列。
  5. #key[目标]：`S* = argmax_S P(S|O) = argmax_S P(O|S) P(S)`。
]

=== 5. 正向/反向最大匹配分词。

题目词典：自动化研究所、自动化、研究所、自动、研究、取得、成就、非常、常人、能干、干到。句子：自动化研究所取得的成就非常人能干到。

#example(title: [标准答案])[
  - #key[FMM]：自动化研究所 / 取得 / 的 / 成就 / 非常 / 人 / 能干 / 到。
  - #key[BMM]：自动化研究所 / 取得 / 的 / 成就 / 非 / 常人 / 能 / 干到。

  “的、人、到、非、能”不在词典中时按单字处理。题目要求分别使用 FMM/BMM，因此两种结果都要写。
]

== 作业 3：句法分析

=== 1. 比较句法结构分析、浅层句法分析、依存句法分析。

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*类型*], [*概念*], [*输出*]),
  [句法结构分析], [判断句子是否符合语法规则，并构造完整层次结构], [短语结构树],
  [浅层句法分析], [只识别局部基本语块，不构造完整句法树], [Base NP、VP/VG、PP 等语块],
  [依存句法分析], [分析词与词之间的支配/依存关系], [依存树：中心词、依存弧、依存标签],
)

=== 2. 术语含义：S、VP、NP、PP；SBV、VOB、ATT、DE。

#table(
  columns: (auto, 1fr, auto, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 5pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*术语*], [*含义*], [*术语*], [*含义*]),
  [S], [句子], [VP], [动词短语],
  [NP], [名词短语], [PP], [介词短语],
  [SBV], [主谓关系], [VOB], [动宾关系],
  [ATT], [定中关系], [DE], [“的”字结构/结构助词“的”相关关系],
)

=== 3. PCFG 模型特点及优势。

#methodblock[
  PCFG 是在 CFG 的每条产生式上附加概率的文法；同一左部所有产生式概率之和通常为 1。一棵句法树的概率一般等于它使用的全部产生式概率之积。

  *优势*：
  1. 能处理自然语言常见的句法歧义。
  2. 多棵候选树存在时可选择概率最大的树。
  3. 规则概率可从树库统计学习，减少纯人工规则的主观性。
  4. 可与 CYK/Viterbi 动态规划结合，高效搜索最优句法树。
]

=== 4. 用 CYK 分析“咬 死 了 猎人 的 狗”。

#methodblock[
  *答题步骤*：
  1. 先将文法转为 CNF。三元规则如 `S → VP Aux NP` 可拆为 `S → VP X1, X1 → Aux NP`；`NP → NP Aux NP` 可拆为 `NP → NP X1, X1 → Aux NP`。
  2. 对每个词填入能推出它的非终结符：咬/v，死/a，了/utl，猎人/n，的/Aux，狗/n。
  3. 按跨度由短到长填 CYK 表，尝试组合 `VC → v a`、`VC → VC utl`、`NP → NP Aux NP`、`VP → VC NP`、`S → VC NP` 等。
  4. 若覆盖整句的表格单元中含 `S`，则句子可由该文法生成。
  5. 记录回溯指针，列出可能的多种结构树。
]

#example(title: [歧义结构])[
  本句至少有两种读法：

  - 咬死了 [猎人 的 狗]：宾语是“猎人的狗”。
  - [咬死了 猎人] 的 狗：中心是“狗”，前面成分作修饰。

  作答时即使不画完整大表，也要写出 CNF 化、填表、回溯和“多结构歧义”。
]

== 作业 4：语义分析

=== 1. 词义消歧概念及主要方法分类。

/ 词义消歧 (WSD): 根据上下文确定多义词在具体语境中的实际含义。

主要方法：基于规则、基于有监督学习、基于词典/知识库；扩展还包括无监督/半监督和深度学习方法。

=== 2. 基于词典的词义消歧类型及原理。

#methodblock[
  1. #key[基于词典释义]：比较上下文与候选义项释义的词语重合或语义相关程度。
  2. #key[基于义类辞典]：利用义类体系判断上下文更支持哪个语义类，如 ANIMAL/MACHINE。
  3. #key[基于双语词典]：把候选译词放入另一语言语料中统计与上下文译词的共现频率，选择共现更强的义项。
]

=== 3. 语义角色标注概念及常用语义角色。

/ 语义角色标注 (SRL): 以谓词为中心，识别句中成分与谓词之间的语义关系，回答“谁对谁做了什么、何时何地如何做”。

常用角色：施事 Agent、受事 Patient/Theme、经验者 Experiencer、工具 Instrument、时间 Time、地点 Location、方式 Manner、原因 Cause、目标 Goal、来源 Source、结果 Result。

=== 4. 短语结构树 SRL 与依存关系树 SRL 的核心差异。

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*方法*], [*候选论元单位*], [*特征来源*]),
  [基于短语结构树], [短语/成分节点], [短语类别、成分路径、谓词与短语的位置],
  [基于依存关系树], [词语节点], [谓词与候选词之间的依存弧、依存路径、依存标签],
)

#key[核心差异]：短语结构树方法依赖成分结构，关注“哪个短语承担角色”；依存树方法依赖词间关系，更直接刻画谓词-论元关系。

== 作业 7：信息检索与问答系统

=== 1. VSM 信息检索技术框架的三个关键点。

#methodblock[
  1. #key[索引项选择]：确定文档和查询用哪些词项表示，可做分词、过滤、同义归并。
  2. #key[权重计算]：计算每个词项的重要性，典型方法是 TF-IDF。
  3. #key[相似度评价]：把文档和查询表示成向量，用余弦相似度等方法计算相关度并排序返回。
]

=== 2. LSI 相对基本 VSM 的优势。

#methodblock[
  LSI 通过对词项-文档矩阵做 SVD，把高维稀疏词项空间映射到低维潜在语义空间。

  优势：
  1. 挖掘词项与文档背后的潜在语义结构。
  2. 缓解同义词/一义多词导致的漏检。
  3. 一定程度缓解一词多义导致的误检。
  4. 降维降噪，减少稀疏性，提高泛化能力。
]

=== 3. LSI 中 C、U、VT、Σ 四个矩阵含义。

#formula[$ C = U Sigma V^T $]

#table(
  columns: (auto, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*矩阵*], [*含义*]),
  [$C$], [原始词项-文档矩阵，行是词项，列是文档，元素是词项权重],
  [$U$], [词项在潜在语义空间中的表示，反映词项与语义维度关系],
  [$Sigma$], [奇异值对角矩阵，奇异值表示各潜在语义维度的重要性],
  [$V^T$], [文档在潜在语义空间中的表示，反映文档与语义维度关系],
)

=== 4. 问答系统概念及实现类型。

/ 问答系统 (QA): 能理解用户自然语言问题，并自动查找、抽取或生成答案的系统。

#table(
  columns: (auto, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*类型*], [*说明*]),
  [FAQ/模板问答], [匹配已有问答对或问题模板],
  [基于信息检索的问答], [先检索相关文档，再抽取答案片段],
  [知识库问答 KBQA], [把问题转成知识图谱查询或推理],
  [阅读理解问答 MRC], [在给定篇章中定位答案],
  [生成式问答], [用预训练语言模型或大语言模型生成答案],
)

== 额外必会计算题

#exercise(title: [HMM / 语言模型 / IR 计算速查])[
  - HMM 前向：`alpha_1(i)=pi_i b_i(o_1)`；`alpha_(t+1)(j)=[sum_i alpha_t(i)a_ij]b_j(o_(t+1))`；最后求和得 `P(O|lambda)`。
  - Viterbi：前向算法的“求和”换成“取最大”，并记录前驱 `psi` 回溯路径。
  - Bigram：`P(w_i|w_(i-1)) = count(w_(i-1),w_i)/count(w_(i-1))`。
  - 加一平滑：`P=(count+1)/(history_count+V)`。
  - 困惑度：`PP(W)=P(W)^(-1/N)`，越低越好。
  - TF-IDF：`tf-idf=tf*log(N/df)`；IDF 降低常见词权重。
  - 余弦相似度：`cos(q,d)=q·d/(|q||d|)`。
  - Precision 看“查得准”，Recall 看“查得全”，`F1=2PR/(P+R)`。
]
