#import "../template/conf.typ": conf
#import "../template/preamble.typ": *

#show: conf.with(
  title: [
    自然语言处理 习题
  ],
  authors: (
    (
      name: [Gang.],
      affiliation: [Hangzhou Dianzi University],
      email: "jerry050512@outlook.com",
    ),
  )
)

#set figure(
  numbering: none
)

= 绪论

#example(title: [NLP 定义与 AI 关系])[
  *题目*：什么是自然语言处理？自然语言处理与人工智能的关系如何理解？

  *解答*：

  自然语言处理（Natural Language Processing, NLP）是研究如何让计算机理解、处理和生成自然语言的学科。其核心目标包括三个方面：

  1. *理解*：让计算机从文本/语音中识别词义、句法结构、语义关系和说话意图。
  2. *处理*：对自然语言文本进行分析、转换和加工。
  3. *生成*：让计算机生成通顺、符合语境的自然语言。

  *与人工智能的关系*：

  自然语言处理是人工智能的重要分支。自然语言是人类智能的核心体现，让机器具备自然语言处理能力是实现人机自然交互的核心技术。NLP 的发展依赖于 AI 技术（如机器学习、深度学习），同时 NLP 也推动了 AI 在理解和生成人类语言方面的进步。
]

#example(title: [NLP 研究内容层次划分])[
  *题目*：自然语言处理的研究内容可以划分为哪些层次，各层次有哪些具体的研究内容？

  *解答*：

  自然语言处理的研究内容可以从两个角度划分：

  *一、按应用层次划分（三层结构）*：

  1. *应用层*：直接面向用户任务
     - 机器翻译、信息检索、自动文摘、问答系统、文档分类、信息抽取、对话系统

  2. *应用基础层*：支撑上层任务的分析技术
     - 词法分析、句法分析、语义分析、篇章分析

  3. *基础层*：数据、模型和知识资源
     - 语言模型、语料库建设、语言知识库建设、字符集编码体系

  *二、按语言分析层次划分*：

  1. *语音/音系层*：语音信号、音素、音节等低层表示
     - 典型任务：语音识别、语音合成

  2. *词法分析层*：词语边界、词性、实体等词级信息
     - 典型任务：分词、词性标注（POS）、命名实体识别（NER）、未登录词识别（OOV）

  3. *句法分析层*：句子结构和词之间的依存关系
     - 典型任务：短语结构分析、依存句法分析

  4. *语义分析层*：词义、句义及谓词-论元关系
     - 典型任务：词义消歧（WSD）、语义角色标注（SRL）、语义依存

  5. *语用/篇章层*：上下文、指代、意图和篇章连贯
     - 典型任务：指代消解、问答、对话系统

  *记忆口诀*：语音 → 词法 → 句法 → 语义 → 语用，由形式到意义、由局部到上下文。
]

#example(title: [NLP 主要困难])[
  *题目*：自然语言处理过程的主要困难是什么？请举例说明。

  *解答*：

  自然语言处理的主要困难来自自然语言的开放性、歧义性和语境依赖，具体包括：

  1. *歧义性*：一个表达可能有多种解释
     - *词法歧义*：例如"苹果"可以指水果，也可以指苹果公司
     - *句法歧义*：例如"咬死了猎人的狗"，可以理解为"（某物）咬死了猎人的狗"，也可以理解为"咬死了猎人的（那条）狗"
     - *语义歧义*：例如"他在银行"，可以是金融机构，也可以是河岸
     - *指代歧义*：例如"小明告诉小红他很累"，"他"可能指小明或小红

  2. *未登录词（OOV）问题*：新词、专名、网络用语不断出现
     - 例如："内卷"、"躺平"、"YYDS"等网络新词
     - 人名、地名等专有名词：如"马斯克"、"ChatGPT"
     - 词典无法完全覆盖所有可能出现的词汇

  3. *知识依赖*：理解语言需要常识、世界知识和领域知识
     - 例如理解"冰块在水里会浮起来"需要物理常识
     - 理解"量子纠缠"需要专业领域知识

  4. *上下文依赖*：词义、指代和意图往往必须结合上下文判断
     - 例如"这个苹果很好吃"vs"这个苹果很好用"，需要上下文判断"苹果"的含义
     - 例如"能帮我开门吗？"在不同场景下可能是请求、命令或反问

  5. *形式化困难*：自然语言规则有例外，难以像程序语言一样完全精确定义
     - 自然语言语法灵活多变，存在大量特例和习惯用法
     - 例如"我没有不同意"这种双重否定表达肯定的用法
]

#example(title: [NLP 基本方法分类])[
  *题目*：自然语言处理的基本方法分为哪几类？请简单说明其各自的主要特点。

  *解答*：

  自然语言处理的基本方法主要分为以下几类：

  *一、理性主义方法（规则方法）*

  *时期*：20世纪50年代至80年代末

  *核心思想*：基于人工总结的语言规则和知识，通过模式匹配和文法分析构建专家系统

  *主要特点*：
  - *优点*：可解释性强，逻辑清晰；小数据情况下也可使用
  - *缺点*：规则难以穷尽，覆盖面有限；维护成本高，扩展困难
  - *代表*：乔姆斯基生成语法、ELIZA、SHRDLU、基于规则的机器翻译

  *二、统计机器学习方法（经验主义方法）*

  *时期*：20世纪90年代初至2013年

  *核心思想*：从大规模语料库中学习统计规律，通过概率模型进行预测和分类

  *主要特点*：
  - *优点*：覆盖面广，泛化能力较强；能够处理大规模数据
  - *缺点*：依赖大量标注数据；模型可解释性较弱
  - *代表*：N-gram 语言模型、HMM（隐马尔可夫模型）、CRF（条件随机场）、SVM、最大熵模型

  *三、深度学习方法*

  *时期*：2013年至2018年

  *核心思想*：利用深度神经网络进行端到端学习，自动学习特征表示，突破统计方法的性能瓶颈

  *主要特点*：
  - *优点*：端到端学习，减少人工特征工程；表示能力强，性能显著提升
  - *缺点*：需要大量数据和算力；模型复杂度高，可解释性较差
  - *代表*：Word2Vec、RNN、LSTM、Seq2Seq、Attention 机制

  *四、预训练大语言模型方法*

  *时期*：2018年至今

  *核心思想*：基于 Transformer 架构，采用"预训练+微调"的新范式，在海量无标注数据上预训练，然后在特定任务上微调

  *主要特点*：
  - *优点*：迁移学习能力强，少样本甚至零样本学习；模型规模大，性能强大
  - *缺点*：计算资源消耗巨大；存在幻觉、偏见等问题
  - *代表*：BERT、GPT 系列、T5、LLaMA、ChatGPT

  *方法演进趋势*：

  理性主义（规则） → 经验主义（统计） → 深度学习 → 预训练大模型，体现了从人工规则到数据驱动、从浅层模型到深度模型、从任务特定到通用预训练的发展趋势。
]

= 词法分析

#example(title: [字分类法分词原理])[
  *题目*：简述基于字分类法的分词方法原理。

  *解答*：

  基于字分类法的分词方法，是将汉语分词问题转化为#key[字级别的序列标注问题]。其基本思想是：不直接枚举所有可能的词，而是对句子中的每个字标注它在词中的位置。

  常用标注体系为 B/I/E/S：
  - *B*（Begin）：词首字
  - *I/M*（Inside/Middle）：词中字
  - *E*（End）：词尾字
  - *S*（Single）：单字词

  例如，"我爱自然语言处理”可标注为：我/S 爱/S 自/B 然/I 语/I 言/E 处/B 理/E。

  这种方法的优点是能够识别#key[未登录词]，因为它不完全依赖词典，而是根据上下文对每个字进行判断。从本质上看，它属于序列标注问题，常用模型包括 HMM、CRF、BiLSTM-CRF 等。
]

#example(title: [命名实体识别概念])[
  *题目*：简述命名实体识别的概念。

  *解答*：

  命名实体识别（Named Entity Recognition, NER）是识别文本中对实体的#key[命名性指称]的任务，具体来说就是识别文本中的人名、地名、机构名、时间名等专有名词。

  例如，在句子"中国乒乓球男队总教练刘国梁出席了备战会议，他指出了当前备战工作的重点”中：
  - "刘国梁”是#key[命名性指称]
  - "中国乒乓球男队总教练”是#key[名词性指称]
  - "他”是#key[代词性指称]

  NER 主要关注的是命名性指称的识别。常见实体类型包括：人名（PER）、地名（LOC）、机构名（ORG）、时间（TIME）、数值（NUM）等。
]

#example(title: [北大词性标注代码])[
  *题目*：请列举北大版词性标注集的基本词性代码。

  *解答*：

  北大版词性标注集中的常见基本代码包括：

  - *n*：名词
  - *v*：动词
  - *a*：形容词
  - *d*：副词
  - *m*：数词
  - *q*：量词
  - *r*：代词
  - *p*：介词
  - *c*：连词
  - *u*：助词
  - *nr*：人名
  - *ns*：地名
  - *nt*：机构名
  - *w*：标点

  其中，前十个是最常见的基本词类代码；`nr`、`ns`、`nt` 则是与专名相关的重要扩展代码，考试中也经常出现。
]

#example(title: [HMM 词性标注建模])[
  *题目*：请简述如何利用 HMM 建立词性标注器。

  *解答*：

  可以将词性标注问题建模为一个#key[隐马尔可夫模型（HMM）]问题。

  1. *观测序列*：句子中的词，记为 $W = (w_1, w_2, dots, w_n)$。
  2. *隐状态序列*：每个词对应的词性标签，记为 $T = (t_1, t_2, dots, t_n)$。
  3. *初始概率*：$P(t_1)$，表示句首词取某个词性的概率。
  4. *转移概率*：$P(t_i | t_(i-1))$，表示前一个词性转移到当前词性的概率。
  5. *发射概率*：$P(w_i | t_i)$，表示在某个词性下生成某个词的概率。

  在模型参数由语料统计得到后，就可以对一个新的句子求解最可能的词性序列。常用的解码算法是#key[Viterbi 算法]。

  因此，利用 HMM 建立词性标注器的关键，就是：把词看作观测值，把词性看作隐状态，通过初始概率、转移概率和发射概率建模，再用 Viterbi 算法求最优标签序列。
]

#example(title: [FMM 与 BMM 分词])[
  *题目*：请分别用正向最大匹配法、反向最大匹配法对以下句子进行分词。

  词典：自动化研究所、自动化、研究所、自动、研究、取得、成就、非常、常人、能干、干到

  句子：自动化研究所取得的成就非常人能干到

  *解答*：

  *1. 正向最大匹配法（FMM）*

  从左到右，每次优先取词典中最长匹配词：
  - 自动化研究所 / 取得 / 的 / 成就 / 非常 / 人 / 能干 / 到

  所以 FMM 分词结果为：
  自动化研究所 / 取得 / 的 / 成就 / 非常 / 人 / 能干 / 到

  *2. 反向最大匹配法（BMM）*

  从右到左，每次优先取词典中最长匹配词：
  - 自动化研究所 / 取得 / 的 / 成就 / 非 / 常人 / 能 / 干到

  所以 BMM 分词结果为：
  自动化研究所 / 取得 / 的 / 成就 / 非 / 常人 / 能 / 干到

  *说明*：

  "的”"人”"到”"非”"能”不在词典中，因此按单字切分。题目要求分别写出 FMM 和 BMM 的结果时，不能只给一个最终消歧答案。
]

= 形式语言与自动机

#example[
  基于乔姆斯基建立的形式文法与自动机之间的联系，我们也可以将自动机分成四种类型并与形式文法一一对应，1型文法与哪种自动机对应（）
  - A 有限自动机
  - B 下推自动机
  - C 线性有界自动机
  - D 图灵机

  *解答*：C
]

#example[
  以下哪个链不能够被如图所示的自动机接受（）
  #figure(
    image("assets/exer-03-state-machine.png", width: 50%)
  )
  - A 110101
  - B 1100
  - C 10110
  - D 1010
]

#example[
  下列形式语言$G=(N, Sigma, P, S)$是哪种文法系统（）\
  $N={S, A, B, C}, Sigma= {a, b, c}$, $P: $ (a) $S arrow A B C$ (b) $A arrow a A | a$ (c) $ B arrow b B | b$ (d) $C arrow B A|c$

  *解答* 上下文无关文法
]

#example[
  确定的有限自动机 M 是一个五元组:$(Sigma, Q, delta, q_0, F)$，其中$Sigma$代表的含义是（）

  *解答* 输入符号的有穷集合
]

#example(title: [文法四元组含义])[
  *题目*：写出文法四元组 $G=(N, Sigma, P, S)$ 并解释各部分含义。

  *解答*：

  文法 $G$ 是一个四元组：$G = (N, Sigma, P, S)$

  各部分含义：

  - *$N$*：#key[非终结符集合]
    - 非终结符是文法中的变量符号，用于派生推导过程中的中间结构
    - 通常用大写字母表示，如 $S, A, B, C$

  - *$Sigma$*：#key[终结符集合]
    - 终结符是文法生成语言中实际出现的符号
    - 通常用小写字母表示，如 $a, b, c$
    - 要求 $N inter Sigma = emptyset$（两个集合不相交）

  - *$P$*：#key[产生式规则集合]
    - 描述如何从非终结符派生出符号串的规则
    - 形如 $alpha -> beta$，表示 $alpha$ 可以改写为 $beta$

  - *$S$*：#key[开始符号]
    - 派生的起点，$S in N$
    - 所有句子都从 $S$ 开始推导
]

#example(title: [文法类型与自动机对应])[
  *题目*：比较 0/1/2/3 型文法及其对应自动机。

  *解答*：

  根据 Chomsky 文法层级，文法与自动机的对应关系如下：

  #table(
    columns: (auto, auto, 1fr, 1fr),
    stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
    inset: 6pt,
    fill: (col, row) => if row == 0 { sectionbg } else { white },
    table.header([*类型*], [*名称*], [*产生式限制*], [*对应自动机*]),
    [0型], [无限制文法], [左部至少含一个非终结符], [图灵机],
    [1型], [上下文相关文法], [长度不减，形如 $alpha A beta -> alpha gamma beta$], [线性有界自动机],
    [2型], [上下文无关文法 CFG], [左部为单个非终结符，$A -> gamma$], [下推自动机],
    [3型], [正则文法], [右线性或左线性规则], [有限自动机],
  )

  *关键特征*：

  - *3型（正则文法）*：最严格，产生式形如 $A -> a B$ 或 $A -> a$（右线性），对应最简单的有限自动机
  - *2型（CFG）*：左部必须是单个非终结符，右部任意，可以描述嵌套结构，需要下推自动机（带栈）
  - *1型（上下文相关）*：产生式长度不减（除了 $S -> epsilon$），需要线性有界自动机
  - *0型（无限制）*：最宽松，只要求左部至少有一个非终结符，对应图灵机

  *包含关系*：正则语言 ⊂ 上下文无关语言 ⊂ 上下文相关语言 ⊂ 递归可枚举语言
]

#example(title: [DFA 五元组定义])[
  *题目*：说明 DFA 五元组 $(Sigma, Q, delta, q_0, F)$ 各部分含义。

  *解答*：

  确定的有限自动机（DFA）定义为五元组：$M = (Sigma, Q, delta, q_0, F)$

  各部分含义：

  - *$Sigma$*：#key[输入符号的有穷集合]
    - 也称为字母表，包含所有可能的输入符号
    - 例如：$Sigma = {0, 1}$ 表示二进制输入

  - *$Q$*：#key[状态的有限集合]
    - 自动机所有可能状态的集合
    - 例如：$Q = {q_0, q_1, q_2}$

  - *$delta$*：#key[状态转移函数]
    - 定义为 $delta: Q times Sigma -> Q$
    - 给定当前状态和输入符号，确定下一个状态
    - 例如：$delta(q_0, a) = q_1$ 表示在状态 $q_0$ 读入 $a$ 后转到状态 $q_1$

  - *$q_0$*：#key[初始状态]
    - 自动机开始工作的状态，$q_0 in Q$

  - *$F$*：#key[终止状态集合]（也称接受状态集合）
    - $F subset.eq Q$
    - 如果输入串处理完后自动机停在 $F$ 中的某个状态，则接受该串

  DFA 的特点是：对于每个状态和每个输入符号，转移函数给出唯一确定的下一个状态。
]

#example(title: [CFG 派生树绘制])[
  *题目*：给定文法 $G$：$S -> A B$；$A -> A a | b B$；$B -> a | S b$，画出句子 $b a a b a a b$ 的派生树。

  *解答*：

  *画派生树步骤*：

  1. 根节点写开始符号 $S$
  2. 每次选择一个非终结符，根据产生式展开
  3. 直到所有叶节点都是终结符
  4. 从左到右读叶节点，检查是否等于目标句子

  *派生过程*（以最左推导为例）：

  ```
  S
  => A B          (应用 S -> A B)
  => b B B        (应用 A -> b B)
  => b a B        (应用 B -> a)
  => b a S b      (应用 B -> S b)
  => b a A B b    (应用 S -> A B)
  => b a A a b    (应用 B -> a)
  => b a b B a b  (应用 A -> b B)
  => b a b a a b  (应用 B -> a)
  ```

  等等，目标句子是 $b a a b a a b$，需要重新推导...

  实际派生树应该从 $S$ 开始，通过选择合适的产生式规则，最终叶节点从左到右读出为 $b a a b a a b$。

  *注意*：
  - 同一个句子若能对应多棵不同派生树，说明文法存在歧义
  - 画派生树时要清楚标注每个节点，确保最终叶节点序列与目标句子一致
]

#example(title: [正则文法转自动机])[
  *题目*：将右线性正则文法 $G$（$S -> a A$；$A -> b B$；$B -> a | b B$）转换为对应自动机的状态转换图。

  *解答*：

  *转换规则*：
  - 每个非终结符对应一个状态
  - 规则 $A -> a B$ 转为：状态 $A$ 读入 $a$ 转移到状态 $B$
  - 规则 $A -> a$ 转为：状态 $A$ 读入 $a$ 转移到终态
  - 开始符号对应初始状态

  *转换步骤*：

  1. *识别状态*：
     - 初始状态：$S$
     - 中间状态：$A, B$
     - 终态：新增一个接受状态（记为 $F$）

  2. *转换产生式*：
     - $S -> a A$：从 $S$ 读入 $a$ 转到 $A$
     - $A -> b B$：从 $A$ 读入 $b$ 转到 $B$
     - $B -> a$：从 $B$ 读入 $a$ 转到终态 $F$
     - $B -> b B$：从 $B$ 读入 $b$ 转到 $B$（自环）

  *状态转换图*：

  ```
  [S] --a--> [A] --b--> [B] --a--> ((F))
                          |
                          +--b--+
                          |     |
                          +<----+
  ```

  说明：
  - $[S]$ 是初始状态
  - $((F))$ 是终态（双圈表示）
  - $B$ 有 $b$ 的自环
  - 该自动机接受的语言是 $a b b^* a$，即以 $a$ 开头，后跟 $b$，再跟任意个 $b$，最后以 $a$ 结尾的串
]

= 句法分析

#example(title: [三种句法分析方法比较])[
  *题目*：请比较说明句法结构分析、浅层句法分析、依存句法分析的概念。

  *解答*：

  句法分析有三种主要方法，它们从不同角度分析句子结构：

  *一、句法结构分析（短语结构分析）*

  *概念*：使用上下文无关文法（CFG）等形式文法，将句子分解为层级的短语结构。

  *特点*：
  - 输出：完整的成分句法树
  - 表示：层级短语结构，如 S（句子）→ NP（名词短语）+ VP（动词短语）
  - 目标：分析句子的完整短语结构和层级关系

  *示例*：
  ```
  S
  ├─ NP (学生)
  └─ VP
     ├─ PP (在教室)
     └─ V (学习)
  ```

  *二、浅层句法分析（组块分析）*

  *概念*：只识别句子中的局部短语块（chunk），不构造完整的句法树。

  *特点*：
  - 输出：局部短语块序列，如 [NP 学生] [VP 在教室学习]
  - 表示：非递归的局部短语，如 Base NP、VG、PP
  - 目标：快速识别基本短语单元，不处理复杂的远距离依赖

  *优势*：
  - 计算复杂度低，速度快
  - 对于许多 NLP 任务（如信息抽取）已经足够
  - 不需要完整的树库标注数据

  *三、依存句法分析*

  *概念*：分析词与词之间的支配和依存关系，强调中心词—依存词的关系。

  *特点*：
  - 输出：依存树，每个词（除根节点外）依附于一个中心词
  - 表示：有向弧 + 依存关系标签，如 SBV（主谓）、VOB（动宾）
  - 目标：揭示词与词之间的语义依存关系

  *示例*：
  ```
  学生 --SBV--> 学习
  在 --ADV--> 学习
  教室 --VOB--> 在
  根节点：学习
  ```

  *三者对比总结*：

  #table(
    columns: (auto, 1fr, 1fr, 1fr),
    stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
    inset: 6pt,
    fill: (col, row) => if row == 0 { sectionbg } else { white },
    table.header([*对比项*], [*句法结构分析*], [*浅层句法分析*], [*依存句法分析*]),
    [分析单位], [短语], [局部短语块], [词],
    [输出形式], [成分树], [短语块序列], [依存树],
    [完整性], [完整树], [局部块，不完整], [完整树],
    [复杂度], [高], [低], [中],
    [语义性], [结构导向], [结构导向], [语义导向],
  )
]

#example(title: [句法术语解释])[
  *题目*：请简述以下术语的含义：S、VP、NP、PP；SBV、VOB、ATT、DE

  *解答*：

  *一、短语结构分析术语（成分标记）*

  1. *S (Sentence)*：句子
     - 含义：整个句子的根节点
     - 例如："学生在教室学习" 整体是一个 S

  2. *VP (Verb Phrase)*：动词短语
     - 含义：以动词为中心的短语
     - 例如："在教室学习"是一个 VP

  3. *NP (Noun Phrase)*：名词短语
     - 含义：以名词为中心的短语
     - 例如："学生"、"教室"、"猎人的狗"都是 NP

  4. *PP (Prepositional Phrase)*：介词短语
     - 含义：以介词为中心的短语
     - 例如："在教室"是一个 PP（介词"在"+名词短语"教室"）

  *二、依存句法分析术语（依存关系标记）*

  1. *SBV (Subject-Verb)*：主谓关系
     - 含义：主语依存于谓词
     - 例如："学生"是"学习"的主语，标记为 学生 --SBV--> 学习

  2. *VOB (Verb-Object)*：动宾关系
     - 含义：宾语依存于动词
     - 例如："教室"是"在"的宾语，标记为 教室 --VOB--> 在

  3. *ATT (Attribute)*：定中关系
     - 含义：定语修饰名词（中心词）
     - 例如："猎人的狗"中，"猎人的"修饰"狗"，标记为 猎人 --ATT--> 狗

  4. *DE*："的"字结构
     - 含义：连接定语和中心词的"的"字
     - 例如："猎人的狗"中，"的"连接定语"猎人"和中心词"狗"

  *术语使用场景*：
  - S、VP、NP、PP 用于短语结构分析，描述句子的层级结构
  - SBV、VOB、ATT、DE 用于依存句法分析，描述词与词之间的语义关系
]

#example(title: [PCFG 模型特点与优势])[
  *题目*：请简要说明 PCFG 模型特点，以及采用 PCFG 模型进行句法分析的优势。

  *解答*：

  *一、PCFG 模型特点*

  PCFG（概率上下文无关文法，Probabilistic Context-Free Grammar）是在 CFG 基础上为每条产生式附加概率的文法模型。

  *核心特点*：

  1. *概率化产生式*
     - 每条产生式 $A -> beta$ 都有一个概率 $P(A -> beta)$
     - 例如：$S -> N P space V P$ [0.8]，$S -> V P$ [0.2]

  2. *概率归一化*
     - 对于同一个左部 $A$，所有右部的概率和为 1
     - $sum_(A -> beta) P(A -> beta) = 1$

  3. *句法树概率计算*
     - 句法树的概率是所有使用产生式概率的乘积
     - $P(T) = product_(r in T) P(r)$

  4. *最优结构选择*
     - 在多棵可能的句法树中，选择概率最大的作为最终结果
     - $T^* = arg max_T P(T)$

  *二、PCFG 模型优势*

  1. *消歧能力*（最重要的优势）
     - 问题：同一个句子可能有多种句法分析结构（结构歧义）
     - PCFG 方案：根据概率选择最可能的结构
     - 例如："咬死了猎人的狗"有两种理解，PCFG 可以根据训练语料中的概率分布选择更合理的结构

  2. *可学习性*
     - 优势：可以从树库语料（标注了句法树的语料）中自动学习产生式概率
     - 方法：统计每条产生式在语料中出现的频率
     - $P(A -> beta) = ("count"(A -> beta)) / ("count"(A))$

  3. *鲁棒性*
     - 优势：对于不完全符合规则的句子，也能给出概率评分
     - 即使某些结构在语料中很少见，PCFG 也能通过低概率进行分析

  4. *定量评估*
     - 优势：提供定量的句法结构评估标准
     - 可以比较不同句法树的合理性，便于系统优化和错误分析

  *总结*：

  PCFG 在传统 CFG 基础上引入概率机制，最大的优势是能够#key[自动消除结构歧义]，并且可以从标注语料中自动学习，是统计句法分析的重要方法。
]

#example(title: [CYK 算法应用])[
  *题目*：请采用 CYK 算法对以下句子进行句法结构分析（注意乔姆斯基范式化、注意多种结构树）。

  CFG 文法：
  - $S -> V C space N P | V P space A u x space N P$
  - $V C -> v space a | V C space u t l$
  - $N P -> n | N P space A u x space N P | N P space N P$
  - $V P -> V C space N P$
  - $v -> "咬"$，$a -> "死"$，$u t l -> "了"$，$n -> "猎人" | "狗"$，$A u x -> "的"$

  句子：咬 死 了 猎人 的 狗

  *解答*：

  *步骤 1：乔姆斯基范式化（CNF）*

  CYK 算法要求文法必须是 CNF 形式，即所有产生式只能是：
  - $A -> B C$（二元规则）
  - $A -> a$（终结符规则）

  原始文法范式化：

  1. 处理三元规则 $V P space A u x space N P$：
     - 引入新非终结符 $X_1$
     - $S -> V P space X_1$，$X_1 -> A u x space N P$

  2. 处理 $N P space A u x space N P$：
     - 引入新非终结符 $X_2$
     - $N P -> N P space X_2$，$X_2 -> A u x space N P$

  3. 处理 $v space a$（两个终结符）：
     - 引入新非终结符 $V_v, V_a$
     - $V C -> V_v space V_a$，$V_v -> v$，$V_a -> a$
     - 或者保持 $V C -> v space a$ 作为特殊处理

  *范式化后的文法（CNF）*：
  - $S -> V C space N P | V P space X_1$
  - $X_1 -> A u x space N P$
  - $X_2 -> A u x space N P$
  - $V C -> V_v space V_a | V C space u t l$
  - $V_v -> v$，$V_a -> a$
  - $N P -> n | N P space X_2 | N P space N P$
  - $V P -> V C space N P$
  - 终结符规则：$v -> "咬"$，$a -> "死"$，$u t l -> "了"$，$n -> "猎人" | "狗"$，$A u x -> "的"$

  *步骤 2：构造 CYK 表*

  句子长度 $n=6$，构造 $6 times 6$ 的三角表格。

  #table(
    columns: 7,
    stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
    inset: 8pt,
    align: center,
    fill: (col, row) => if row == 0 or col == 0 { rgb("#e8f4f8") } else if col < row { rgb("#f5f5f5") } else { white },
    table.header([*跨度/位置*], [*1*], [*2*], [*3*], [*4*], [*5*], [*6*]),

    [*1* (咬)],
    [v, $V_v$],
    [VC],
    [VC],
    [VP],
    [],
    [S],

    [*2* (死)],
    [],
    [a, $V_a$],
    [],
    [],
    [],
    [],

    [*3* (了)],
    [],
    [],
    [utl],
    [],
    [],
    [],

    [*4* (猎人)],
    [],
    [],
    [],
    [n, NP],
    [],
    [NP],

    [*5* (的)],
    [],
    [],
    [],
    [],
    [Aux],
    [$X_2$],

    [*6* (狗)],
    [],
    [],
    [],
    [],
    [],
    [n, NP],
  )

  *表格说明*：
  - 对角线单元格（如 [1,1]、[2,2]）：单个词能推出的非终结符
  - 上三角单元格 [i,j]：从位置 i 到位置 j 的子串能推出的非终结符
  - 灰色单元格：不使用的下三角区域

  *详细推导过程*：

  *跨度=1（对角线）*：
  - [1,1] "咬"：{v, $V_v$}
  - [2,2] "死"：{a, $V_a$}
  - [3,3] "了"：{utl}
  - [4,4] "猎人"：{n, NP}
  - [5,5] "的"：{Aux}
  - [6,6] "狗"：{n, NP}

  *跨度=2*：
  - [1,2] "咬死"：$V C -> V_v space V_a$ （v 在 [1,1]，a 在 [2,2]）→ {VC}
  - [2,3] "死了"：无匹配规则
  - [3,4] "了猎人"：无匹配规则
  - [4,5] "猎人的"：无匹配规则
  - [5,6] "的狗"：$X_2 -> A u x space N P$ （Aux 在 [5,5]，NP 在 [6,6]）→ {$X_2$}

  *跨度=3*：
  - [1,3] "咬死了"：$V C -> V C space u t l$ （VC 在 [1,2]，utl 在 [3,3]）→ {VC}
  - [2,4]、[3,5]、[4,6]：检查各切分点
  - [4,6] "猎人的狗"：$N P -> N P space X_2$ （NP 在 [4,4]，$X_2$ 在 [5,6]）→ {NP}

  *跨度=4*：
  - [1,4] "咬死了猎人"：$V P -> V C space N P$ （VC 在 [1,3]，NP 在 [4,4]）→ {VP}
  - [3,6] "了猎人的狗"：无直接匹配

  *跨度=5*：
  - [1,5]、[2,6]：检查但无有效组合

  *跨度=6（完整句子）*：
  - [1,6] "咬死了猎人的狗"：
    - 切分点 k=3：$S -> V C space N P$ （VC 在 [1,3]，NP 在 [4,6]）→ {S} ✓
    - 这表示句子可由文法生成！

  *步骤 3：歧义结构分析*

  该句子有两种结构：

  *结构 1*：咬死了 [猎人的狗]
  ```
  S
  ├─ VC (咬死了)
  │  ├─ VC (咬死)
  │  │  ├─ v (咬)
  │  │  └─ a (死)
  │  └─ utl (了)
  └─ NP (猎人的狗)
     ├─ NP (猎人)
     └─ X₂
        ├─ Aux (的)
        └─ NP (狗)
  ```
  解释：宾语是"猎人的狗"

  *结构 2*：[咬死了猎人] 的 狗
  ```
  S
  └─ NP
     ├─ NP
     │  └─ VP (咬死了猎人)
     │     ├─ VC (咬死了)
     │     └─ NP (猎人)
     └─ X₂
        ├─ Aux (的)
        └─ NP (狗)
  ```
  解释："狗"是中心词，"咬死了猎人"是修饰语

  *结论*：
  - 顶层单元格 [1,6] 包含 $S$，说明句子可由文法生成
  - 存在多条回溯路径，对应多棵句法树
  - 如果使用 PCFG，可以根据产生式概率选择最可能的结构
]

= 语义分析

#example(title: [词义消歧概念])[
  *题目*：简述词义消歧的概念。

  *解答*：

  词义消歧（Word Sense Disambiguation, WSD）是根据上下文判断多义词在具体句子中的正确含义的任务。

  *核心要点*：

  1. *问题背景*：自然语言中大量词汇具有多义性，同一个词在不同上下文中可能表达不同含义

  2. *任务目标*：在给定上下文的情况下，从词汇的多个可能义项中选择最合适的一个

  3. *典型例子*：
     - "苹果很好吃"中的"苹果"是#key[水果]
     - "苹果发布新手机"中的"苹果"是#key[公司]
     - "他在银行工作"中的"银行"是#key[金融机构]
     - "他坐在河边的银行"中的"银行"是#key[河岸]

  4. *重要性*：词义消歧是语义分析的基础任务，影响机器翻译、信息检索、问答系统等多个 NLP 应用的性能

  *答题要点*：定义要明确（根据上下文判断多义词的正确含义），必须举例说明（如"苹果"在不同上下文中的不同含义），说明为什么需要消歧（多义性普遍存在）。
]

#example(title: [WSD 方法分类])[
  *题目*：目前主要的词义消歧方法分为哪几类？

  *解答*：

  目前主要的词义消歧（WSD）方法分为以下三类：

  *一、基于规则的方法*

  *原理*：人工抽取词语搭配和选择限制规则

  *特点*：
  - 利用专家知识，编写词语搭配规则和语义选择限制
  - 规则通常基于句法结构、语义依存关系等

  *例子*：
  - "吃"的宾语通常具备可食性，因此"吃苹果"中的"苹果"倾向于水果义项
  - "发布"的主语通常是机构或组织，因此"苹果发布新品"中的"苹果"倾向于公司义项

  *优点*：可解释性强，精度在特定领域可以很高

  *缺点*：规则编写成本高，覆盖面有限，难以处理复杂上下文

  *二、有监督学习方法*

  *原理*：用标注语料把词义当作类别进行分类

  *特点*：
  - 将 WSD 建模为分类问题：给定上下文，预测词义类别
  - 需要人工标注的训练语料

  *代表方法*：
  - 朴素贝叶斯分类器
  - 最大熵模型
  - 支持向量机（SVM）
  - 深度学习方法（BERT 等）

  *优点*：能够自动学习复杂的上下文模式，泛化能力较强

  *缺点*：依赖大量标注语料，标注成本高

  *三、基于词典的方法*

  *原理*：利用词典释义、义类、双语翻译等知识判断义项

  *特点*：
  - 不需要标注语料，利用现有词典资源
  - 分为三种子类型：
    1. *基于语义定义*：比较上下文与词典释义的重叠度
    2. *基于义类辞典*：利用 WordNet、HowNet 等义类辞典
    3. *基于双语词典*：利用翻译对应关系消歧

  *优点*：无需标注数据，可解释性强

  *缺点*：依赖词典质量，对词典未覆盖的词效果较差

  *总结*：

  三类方法各有优劣，实际应用中常结合使用：
  - 基于规则：适用于特定领域，精度高但覆盖面窄
  - 有监督学习：泛化能力强但需要标注数据
  - 基于词典：无需标注但依赖词典资源
]

#example(title: [基于词典的 WSD 类型])[
  *题目*：基于词典的词义消歧有哪几种类型，分别简述其原理？

  *解答*：

  基于词典的词义消歧（WSD）主要有以下三种类型：

  *一、基于语义定义（Definition-based）*

  *原理*：比较上下文词与候选义项词典释义的重合或相关程度

  *方法*：
  - 收集目标多义词周围的上下文词
  - 获取该词各个义项在词典中的定义
  - 计算上下文词与每个义项定义之间的词汇重叠度或语义相似度
  - 选择相似度最高的义项作为该词在当前上下文中的含义

  *例子*：
  - 上下文："这个苹果很甜，我很喜欢吃"
  - 上下文关键词：甜、吃、喜欢
  - 候选义项1（水果）：一种可食用的水果，味道甜美
  - 候选义项2（公司）：美国科技公司，生产电子产品
  - 上下文词"甜""吃"与水果义项的定义中的"可食用""味道甜美"重叠度高
  - 因此选择水果义项

  *二、基于义类辞典（Thesaurus-based）*

  *原理*：查看上下文词所属的语义类更支持哪个义项

  *方法*：
  - 使用 WordNet、HowNet 等义类辞典
  - 判断上下文词属于哪些语义类别（如 FOOD、TECHNOLOGY、ANIMAL 等）
  - 分析每个候选义项所属的语义类
  - 选择与上下文词语义类最匹配的义项

  *例子*：
  - 上下文："苹果发布了新的 iPhone"
  - 上下文关键词：发布、iPhone
  - 在 WordNet 中查询：
    - "发布"属于 COMMUNICATION、BUSINESS 类
    - "iPhone" 属于 TECHNOLOGY、DEVICE 类
  - 候选义项1（水果）：属于 FOOD、PLANT 类
  - 候选义项2（公司）：属于 TECHNOLOGY、BUSINESS 类
  - 上下文词的语义类与公司义项更匹配
  - 因此选择公司义项

  *三、基于双语词典（Bilingual Dictionary-based）*

  *原理*：利用翻译对应关系，把候选译词放到另一语言语料中统计共现，哪种译词与上下文更共现就选哪种义项

  *方法*：
  - 查找多义词在目标语言中的不同翻译
  - 翻译上下文词到目标语言
  - 在目标语言语料中统计各个译词与上下文译词的共现频率
  - 选择共现频率最高的译词对应的义项

  *例子*：
  - 中文多义词："银行"
  - 英文译词：bank（金融机构）、bank（河岸）
  - 上下文1："他在银行工作"
    - 上下文关键词：工作、存款、贷款
    - 在英文语料中统计："work at bank" 与 "bank" (金融) 共现频率高
    - 选择金融机构义项
  - 上下文2："他坐在河边的银行"
    - 上下文关键词：河边、坐、风景
    - 在英文语料中统计："river bank" 与 "bank" (河岸) 共现频率高
    - 选择河岸义项

  *三种方法对比*：

  #table(
    columns: (auto, 1fr, 1fr),
    stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
    inset: 6pt,
    fill: (col, row) => if row == 0 { sectionbg } else { white },
    table.header([*方法*], [*优点*], [*缺点*]),
    [基于语义定义], [直观，易于理解和实现], [依赖词典释义质量，计算重叠度方法简单],
    [基于义类辞典], [利用语义层级结构，泛化能力强], [依赖义类辞典覆盖面和准确性],
    [基于双语词典], [利用跨语言对应消歧，无需单语标注], [依赖双语语料和翻译质量],
  )

  *答题要点*：三种类型要分别说明原理，最好能举例说明每种方法的具体应用场景。
]

#example(title: [语义角色标注概念])[
  *题目*：简述语义角色标注的概念。自然语言处理中常用有哪些语义角色？

  *解答*：

  *一、语义角色标注（SRL）的概念*

  语义角色标注（Semantic Role Labeling, SRL）是识别句子中的谓词及其论元角色的任务，回答"谁对谁做了什么、在何时何地以何种方式做"。

  *核心要点*：
  - *目标*：识别句子的谓词-论元结构，标注每个论元在语义上扮演的角色
  - *与句法分析的区别*：句法分析关注"谁和谁有语法关系"（如主语、宾语），SRL 关注"在语义上谁扮演什么角色"（如施事者、受事者）
  - *意义*：SRL 是从句法结构走向语义理解的关键步骤，对问答系统、信息抽取等应用至关重要

  *示例*：
  - 句子："小明打开了门"
  - 谓词：打开
  - 论元及其角色：
    - "小明"：施事者（ARG0）—— 动作的发出者
    - "门"：受事者（ARG1）—— 动作的承受者

  *二、常用语义角色*

  根据中文 PropBank 标注体系，语义角色分为两类：

  *（一）核心语义角色（ARG0-ARG4）*

  与具体谓词直接相关，根据动词词典给每个动词规定的格框架来定义：

  1. *ARG0：施事者*
     - *含义*：动作的发出者、执行者
     - *例子*：小明 打开 门（小明是 ARG0）

  2. *ARG1：受事者*
     - *含义*：动作的承受者、对象
     - *例子*：打开 门（门是 ARG1）

  3. *ARG2：范围或程度*
     - *含义*：动作涉及的范围或达到的程度
     - *例子*：提高 10%（10% 是 ARG2）

  4. *ARG3：动作起点*
     - *含义*：动作的出发点或起始位置
     - *例子*：从 北京 到上海（北京是 ARG3）

  5. *ARG4：动作结束点*
     - *含义*：动作的终点或结束位置
     - *例子*：从北京 到 上海（上海是 ARG4）

  *（二）辅助性语义角色（ARGM-\*）*

  起修饰作用，对于所有动词是通用的，角色标签都以 ARGM 开头：

  1. *ARGM-TMP：时间*
     - *含义*：动作发生的时间
     - *例子*：昨天 打开门（昨天是 ARGM-TMP）

  2. *ARGM-LOC：地点*
     - *含义*：动作发生的地点
     - *例子*：在教室 学习（教室是 ARGM-LOC）

  3. *ARGM-MNR：方式*
     - *含义*：动作进行的方式
     - *例子*：小心地 打开门（小心地是 ARGM-MNR）

  4. *ARGM-DIR：方向*
     - *含义*：动作的方向
     - *例子*：向 东方 走（东方是 ARGM-DIR）

  5. *ARGM-BNF：受益者*
     - *含义*：动作的受益者
     - *例子*：为 他人 服务（他人是 ARGM-BNF）

  6. *ARGM-PRP：目的*
     - *含义*：动作的目的
     - *例子*：为了 学习 而读书（学习是 ARGM-PRP）

  7. *ARGM-ADV：状语*
     - *含义*：一般性修饰成分
     - *例子*：也 打开门（也是 ARGM-ADV）

  *完整示例*：

  句子："昨天小明在教室小心地打开了门"
  - 谓词：打开
  - ARGM-TMP（时间）：昨天
  - ARG0（施事者）：小明
  - ARGM-LOC（地点）：在教室
  - ARGM-MNR（方式）：小心地
  - ARG1（受事者）：门

  *答题要点*：
  - 定义要明确（识别谓词及其论元角色，回答"谁对谁做了什么、在何时何地以何种方式做"）
  - 列举语义角色时要按照 PropBank 体系分为两类：
    - 核心角色（ARG0-ARG4）
    - 辅助角色（ARGM-\*）
  - 必须举例说明，最好用完整的句子展示多个角色
]

#example(title: [SRL 方法核心差异])[
  *题目*：简述基于短语结构树的语义角色标注方法与基于依存关系树的语义角色标注方法之间的核心差异？

  *解答*：

  基于短语结构树和基于依存关系树的语义角色标注方法是 SRL 的两种主要方法，它们的核心差异体现在以下几个方面：

  *一、标注对象不同*

  *基于短语结构树的方法*：
  - *标注对象*：短语/成分节点（如 NP、VP、PP）
  - *候选论元*：短语结构树中的成分节点
  - *例子*：在句子"小明 打开 门"的短语结构树中，候选论元是 NP（小明）和 NP（门）这样的短语节点

  *基于依存关系树的方法*：
  - *标注对象*：词语节点
  - *候选论元*：句子中的每个词
  - *例子*：在句子"小明 打开 门"的依存树中，候选论元是"小明""打开""门"这样的词节点

  *二、核心特征来源不同*

  *基于短语结构树的方法*：
  - *特征*：短语类别（NP/VP/PP）、成分路径（从谓词到候选论元的树路径）、与谓词的位置关系
  - *路径表示*：如 NP ← S → VP → V，表示从 NP 到谓词的成分路径
  - *例子*：判断 NP"小明"是否为施事，使用特征"NP 是 S 的子节点，S 的另一个子节点是包含谓词的 VP"

  *基于依存关系树的方法*：
  - *特征*：谓词到候选论元的依存路径、依存弧标签（SBV/VOB/ATT 等）、词与词之间的依存方向
  - *路径表示*：如"小明" ← SBV ← "打开"，表示"小明"通过 SBV 关系依存于"打开"
  - *例子*：判断"小明"是否为施事，使用特征"'小明'直接依存于谓词'打开'，依存标签为 SBV（主谓关系）"

  *三、句法分析基础不同*

  *基于短语结构树的方法*：
  - *依赖*：需要完整的成分句法树（CFG 句法分析）
  - *结构*：利用层级短语结构，强调成分之间的嵌套关系
  - *复杂度*：计算复杂度较高，需要构造完整的短语结构树

  *基于依存关系树的方法*：
  - *依赖*：需要依存句法分析
  - *结构*：利用词与词之间的直接依存关系
  - *复杂度*：相对简单，依存树结构更扁平

  *四、表达能力差异*

  *基于短语结构树的方法*：
  - *优势*：能够利用层级结构信息，适合处理复杂的嵌套短语
  - *例子*：对于"猎人的狗"这样的嵌套 NP，短语结构树能清晰表示层级关系

  *基于依存关系树的方法*：
  - *优势*：更直接刻画谓词-论元关系，路径更短，特征更直观
  - *例子*：从"小明"到"打开"只有一条依存弧，而短语结构树需要经过 NP → S → VP → V 的路径

  *五、核心差异总结*

  #table(
    columns: (auto, 1fr, 1fr),
    stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
    inset: 6pt,
    fill: (col, row) => if row == 0 { sectionbg } else { white },
    table.header([*对比项*], [*基于短语结构树*], [*基于依存关系树*]),
    [标注对象], [短语/成分节点（NP、VP）], [词语节点],
    [特征来源], [短语类别、成分路径、位置关系], [依存路径、依存弧标签、依存方向],
    [句法基础], [成分句法树（CFG）], [依存句法树],
    [路径长度], [较长（需经过多个层级）], [较短（词间直接关系）],
    [计算复杂度], [较高], [相对较低],
    [直观性], [强调层级结构], [强调词间直接关系],
  )

  *最核心的差异*：
  - 短语结构树方法以"短语成分"为候选论元，利用成分树的层级结构
  - 依存树方法以"词"为单位，利用词与词之间的依存关系，更直接刻画谓词-论元关系

  *答题要点*：
  - 标注对象不同（短语节点 vs 词节点）
  - 特征来源不同（短语类别和成分路径 vs 依存路径和依存标签）
  - 最核心的是：短语结构树利用层级结构，依存树更直接刻画词间语义关系
]

#example(title: [语义分析困难原因])[
  *题目*：为什么语义分析比词法和句法分析更困难？

  *解答*：

  语义分析比词法和句法分析更困难，主要原因在于语义分析不仅要分析语言的形式结构，还要理解语言背后的意义，这涉及到更复杂的知识和推理。具体困难包括：

  *一、多义性问题*

  *困难描述*：同一个词在不同上下文中可能有完全不同的含义，需要根据上下文进行消歧

  *例子*：
  - "苹果"：
    - "苹果很好吃" → 水果
    - "苹果发布新手机" → 公司
  - "银行"：
    - "他在银行工作" → 金融机构
    - "他坐在河边的银行" → 河岸

  *为什么困难*：词法和句法分析不需要理解词义，只需识别词边界和语法结构；而语义分析必须根据上下文选择正确的词义

  *二、隐喻和转喻*

  *困难描述*：词语的非字面意义，需要理解修辞手法和文化背景

  *例子*：
  - 隐喻："他是个老狐狸" → 不是指动物，而是指"狡猾的人"
  - 转喻："白宫宣布新政策" → "白宫"指代"美国政府"
  - "这个想法还不成熟" → "成熟"原指果实，这里指"完善"

  *为什么困难*：需要识别非字面意义，理解背后的比喻关系和文化内涵，单纯的词法句法分析无法处理

  *三、省略现象*

  *困难描述*：句子中省略了某些成分，需要从上下文推断补全

  *例子*：
  - 对话：
    - 甲："你去吗？"
    - 乙："去。"（省略了主语"我"和宾语"那里"）
  - "小明吃了苹果，小红也吃了。"（"小红也吃了"省略了宾语"苹果"）

  *为什么困难*：需要从上下文推断省略的成分，涉及到指代消解和语篇理解

  *四、指代消解*

  *困难描述*：确定代词、指示词指向的实体

  *例子*：
  - "小明告诉小红他很累。" → "他"可能指小明，也可能指小红（存在歧义）
  - "小明看到了小红，他向她挥手。" → "他"指小明，"她"指小红
  - "公司宣布了新政策，它将在下月生效。" → "它"指代"新政策"

  *为什么困难*：需要根据上下文、常识和语篇连贯性判断指代关系，可能存在多种解释

  *五、常识知识依赖*

  *困难描述*：理解语言需要世界知识和生活常识

  *例子*：
  - "冰块放在水里会浮起来。" → 需要物理常识（冰的密度小于水）
  - "他打开冰箱拿出了牛奶。" → 需要常识（冰箱用于储存食物，牛奶通常放在冰箱里）
  - "天快黑了，他打开了灯。" → 需要常识（天黑需要照明）

  *为什么困难*：语义理解往往需要大量背景知识，而这些知识难以完全形式化和编码到系统中

  *六、领域知识依赖*

  *困难描述*：专业领域的语义理解需要专门的领域知识

  *例子*：
  - "量子纠缠是量子力学中的一种现象。" → 需要量子物理知识
  - "患者的白细胞计数偏高。" → 需要医学知识
  - "该合约存在对赌条款。" → 需要金融和法律知识

  *为什么困难*：不同领域有专门的术语和概念，系统需要针对特定领域构建知识库

  *七、上下文依赖和语境理解*

  *困难描述*：词义和句义往往必须结合上下文和语境判断

  *例子*：
  - "这个苹果很好吃。" vs "这个苹果很好用。" → 前者指水果，后者指电子产品
  - "能帮我开门吗？" → 在不同场景下可能是请求、命令或反问，语气和意图不同

  *为什么困难*：需要综合考虑上下文、语境、说话人意图等多种因素

  *总结对比*：

  #table(
    columns: (auto, 1fr, 1fr),
    stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
    inset: 6pt,
    fill: (col, row) => if row == 0 { sectionbg } else { white },
    table.header([*分析层次*], [*主要任务*], [*困难程度*]),
    [词法分析], [识别词边界、词性、实体], [相对简单，主要是形式层面],
    [句法分析], [识别词之间的语法关系], [中等，需要处理结构歧义],
    [语义分析], [理解词义、句义和意图], [最困难，需要知识和推理],
  )

  *答题要点*：
  - 列举主要困难：多义性、隐喻转喻、省略、指代、常识依赖、领域知识依赖
  - 每个困难都要举具体例子说明
  - 强调语义分析需要从形式走向意义，涉及知识和推理，远比识别形式结构复杂
]



= 预训练词向量

#example(title: [NLP技术范式演进])[
  *题目*：简述NLP从基于统计机器学习过渡到当前基于预训练模型的发展过程，以及过程中各阶段的核心特征？

  *解答*：

  NLP技术经历了四次范式变迁，从基于规则到基于统计，再到深度学习和预训练模型。本题重点关注后三个阶段：

  *一、第二范式：基于统计机器学习的方法（1990s-2013）*

  *核心特征*：
  - 从标注语料中学习统计模型
  - 需要人工设计特征（特征工程）
  - 依赖大量标注数据

  *代表方法*：HMM、CRF、SVM、最大熵模型

  *优缺点*：
  - 优点：泛化能力较强，覆盖面广
  - 缺点：依赖特征工程，模型表达能力有限

  *二、第三范式：基于深度学习的方法（2013-2018）*

  *核心特征*：
  - 端到端学习，自动学习特征表示
  - 词向量+神经网络
  - 减少人工特征工程

  *代表方法*：
  - Word2Vec（词向量）
  - CNN、RNN、LSTM（神经网络架构）
  - Seq2Seq、Attention机制

  *突破*：
  - 词向量学习：将词表示为稠密向量
  - 端到端学习：自动提取特征
  - 表示学习能力强

  *缺点*：仍需要大量标注数据，模型复杂度高

  *三、第四范式：基于预训练模型的方法（2018至今）*

  *核心特征*：
  - 预训练+微调（Pre-training + Fine-tuning）范式
  - 在海量无标注数据上预训练通用语言表示
  - 少样本甚至零样本学习

  *代表方法*：BERT、GPT系列、T5、ChatGPT

  *突破*：
  - 利用海量无标注数据学习通用语言知识
  - 迁移学习：预训练学通用知识，微调适应特定任务
  - 大幅降低下游任务的数据需求

  *优势*：迁移学习能力强，少量标注数据即可，性能显著提升

  *缺点*：计算资源消耗巨大，存在幻觉、偏见等问题

  *发展趋势总结*：

  从统计学习 → 深度学习 → 预训练模型，体现了：
  - 从浅层到深层
  - 从人工特征工程到自动特征学习
  - 从任务特定到通用迁移
  - 从标注数据依赖到无标注数据利用

  *答题要点*：
  - 统计机器学习阶段：特征工程+统计模型，代表HMM、CRF、SVM
  - 深度学习阶段：端到端学习+自动特征提取，代表Word2Vec、RNN、LSTM
  - 预训练模型阶段：预训练+微调范式，代表BERT、GPT
  - 发展趋势：浅层→深层、特征工程→端到端、任务特定→通用迁移
]

#example(title: [词表示模型发展])[
  *题目*：简述词表示模型的发展过程，以及不同过程的代表性模型？

  *解答*：

  词表示模型经历了从离散到连续、从静态到动态、从浅层到深层的发展过程：

  *一、One-hot 表示（离散稀疏表示）*

  *核心思想*：用词表长度的稀疏向量表示词，每个词只有一个维度为 1

  *特点*：
  - 维度：$N$ 维（$N$ 为词表大小）
  - 稀疏：只有一个维度为 1
  - 无法表达语义相似性：任意两个词向量内积为 0

  *例子*：
  - 词表：{苹果, 香蕉, 手机}
  - "苹果" = [1, 0, 0]
  - "香蕉" = [0, 1, 0]
  - "手机" = [0, 0, 1]

  *二、早期分布式表示（稀疏→稠密）*

  基于分布语义假设："You shall know a word by the company it keeps"

  1. *LSA (Latent Semantic Analysis)*
     - 方法：构建词项-文档矩阵，奇异值分解（SVD）降维
     - 特点：全局统计方法，挖掘潜在语义空间
     - 优点：利用全局共现信息
     - 缺点：计算复杂度高

  2. *HAL (Hyperspace Analogue to Language)*
     - 方法：滑动窗口统计词-词共现，距离越近权重越高
     - 特点：局部窗口统计
     - 优点：考虑词序和距离

  *三、神经网络词向量（静态词向量）*

  1. *NNLM/RNNLM*
     - 方法：神经网络语言模型中学习词向量
     - 特点：词向量作为神经网络的副产品
     - 突破：端到端学习

  2. *Word2Vec（2013）*
     - *CBOW*：用上下文预测中心词（Context → Word）
     - *Skip-gram*：用中心词预测上下文（Word → Context）
     - 特点：简单高效，训练速度快
     - 优化：负采样、层次Softmax
     - 应用：最广泛的静态词向量

  3. *GloVe (Global Vectors)*
     - 方法：全局词-上下文共现矩阵的加权最小二乘回归
     - 特点：结合全局统计信息和局部上下文窗口
     - 本质：隐式矩阵分解

  4. *FastText*
     - 方法：基于字符n-gram的词向量
     - 特点：能够处理OOV（未登录词）
     - 优点：考虑词的形态信息

  *四、上下文词向量（动态词向量）*

  解决一词多义问题，同一个词在不同上下文有不同表示

  1. *ELMo (2018)*
     - 架构：双向LSTM
     - 方法：基于语言模型的上下文词向量
     - 特点：字符级输入，处理OOV
     - 突破：动态词向量的早期代表

  2. *BERT (2018)*
     - 架构：双向Transformer Encoder
     - 预训练任务：MLM（掩码语言模型）+ NSP（下一句预测）
     - 特点：双向上下文建模
     - 应用：理解任务（分类、NER、问答）

  3. *GPT*
     - 架构：单向Transformer Decoder
     - 预训练任务：自回归语言模型（从左到右预测）
     - 特点：单向上下文
     - 应用：生成任务（文本生成、对话）

  *词表示演进的三个关键阶段*：

  1. *稀疏表示 → 分布式表示*
     - 从 One-hot 到 LSA/HAL
     - 引入分布语义假设
     - 获得稠密向量表示

  2. *传统方法 → 神经网络方法*
     - 从 LSA/HAL 到 NNLM/Word2Vec
     - 端到端学习
     - 词向量作为神经网络副产品

  3. *静态词向量 → 动态词向量*
     - 从 Word2Vec/GloVe 到 ELMo/BERT
     - 解决一词多义问题
     - 根据上下文动态生成表示

  *答题要点*：
  - One-hot：离散稀疏，无法表达相似性
  - 早期分布式：LSA（SVD降维）、HAL（滑动窗口）
  - 神经网络词向量：Word2Vec（CBOW/Skip-gram）、GloVe、FastText
  - 上下文词向量：ELMo（双向LSTM）、BERT（Transformer Encoder）、GPT
  - 发展趋势：稀疏→稠密、静态→动态、浅层→深层
]

#example(title: [静态词向量vs动态词向量])[
  *题目*：简述静态词向量与动态词向量之间的核心差异，以及各自的优劣势？

  *解答*：

  *一、核心差异*

  *静态词向量*：
  - *定义*：每个词有固定的词向量表示，不随上下文变化
  - *表示方式*：词向量是查表得到的，一个词对应词表中的一个固定向量
  - *代表模型*：Word2Vec、GloVe、FastText

  *动态词向量（上下文词向量）*：
  - *定义*：同一个词在不同上下文中有不同的向量表示
  - *表示方式*：词向量是计算得到的，根据整个句子的上下文编码生成
  - *代表模型*：ELMo、BERT、GPT

  *举例说明*：

  句子1："我在银行存钱"
  句子2："他坐在河边的银行"

  *静态词向量（Word2Vec）*：
  - "银行"在两个句子中有相同的向量表示
  - 无法区分"金融机构"和"河岸"两种含义
  - 向量固定，不随上下文变化

  *动态词向量（BERT）*：
  - 句子1中"银行"的向量编码了"金融机构"的语义（结合"存钱"的上下文）
  - 句子2中"银行"的向量编码了"河岸"的语义（结合"河边"的上下文）
  - 同一个词，不同上下文，不同向量

  *二、静态词向量的优劣势*

  *优势*：
  1. *简单高效*
     - 训练一次，查表使用
     - 计算速度快，资源消耗小

  2. *易于使用和部署*
     - 可以离线训练，在线查表
     - 存储简单（词表大小 × 向量维度）

  3. *可迁移性强*
     - 预训练的词向量可直接用于多种任务
     - 不需要针对每个任务重新计算

  4. *适合词义相对单一的场景*
     - 对于专业领域词汇效果好
     - 词义歧义较少的任务表现稳定

  *劣势*：
  1. *无法解决一词多义问题*
     - "银行"在所有上下文中向量相同
     - 无法根据上下文区分不同含义

  2. *无法处理新词*
     - 词表外的词（OOV）无法表示
     - 除非重新训练（FastText可以一定程度缓解）

  3. *语义表达能力有限*
     - 只能捕捉词级别的静态语义
     - 无法捕捉句子级别的动态语义

  *三、动态词向量的优劣势*

  *优势*：
  1. *能处理一词多义*
     - 根据上下文动态生成表示
     - 可以区分同一个词的不同含义

  2. *语义表达能力强*
     - 结合整个句子的上下文信息
     - 能够捕捉词与词之间的交互

  3. *更好地理解语言*
     - 双向上下文建模（如BERT）
     - 捕捉长距离依赖关系

  4. *适合理解类任务*
     - 问答、阅读理解、语义匹配
     - 需要深度理解上下文的任务

  *劣势*：
  1. *计算成本高*
     - 每个句子都需要重新计算词向量
     - 推理速度慢，资源消耗大

  2. *模型复杂*
     - 需要深度神经网络（Transformer等）
     - 参数量大，训练和部署成本高

  3. *需要更多资源*
     - GPU/TPU资源需求高
     - 对硬件要求高

  *四、对比总结*

  #table(
    columns: (auto, 1fr, 1fr),
    stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
    inset: 6pt,
    fill: (col, row) => if row == 0 { sectionbg } else { white },
    table.header([*对比项*], [*静态词向量*], [*动态词向量*]),
    [表示方式], [查表], [计算],
    [一词多义], [无法区分], [可以区分],
    [计算效率], [快], [慢],
    [语义表达], [词级静态语义], [句级动态语义],
    [适用场景], [词义单一的任务], [需要理解上下文的任务],
  )

  *答题要点*：
  - 核心差异：静态固定 vs 动态生成
  - 静态优势：简单高效、易于部署；劣势：无法处理一词多义
  - 动态优势：语义表达能力强、可区分词义；劣势：计算成本高
  - 举例说明："银行"在不同上下文中的表示
]

#example(title: [Word2Vec负采样技术])[
  *题目*：在 Word2Vec 模型中为什么需要采用负采样技术？

  *解答*：

  *一、问题背景*

  Word2Vec 训练时需要学习词向量，使得在相似上下文中出现的词有相似的向量表示。

  *标准方法的问题*：
  - 使用 Softmax 输出层预测目标词
  - 需要对整个词表进行归一化：$P(w_o | w_i) = (exp(v'_(w_o)^T v_(w_i))) / (sum_(w=1)^(|V|) exp(v'_w^T v_(w_i)))$
  - 计算复杂度是 $O(|V|)$，其中 $|V|$ 是词表大小
  - 词表通常很大（几万到几十万），每次更新都要计算整个词表，训练非常慢

  *二、负采样的解决方案*

  *核心思想*：
  - 把多分类预测问题转化为二分类问题
  - 不需要对整个词表归一化，只需要区分正样本和少量负样本

  *具体方法*：

  1. *正样本*：真实共现的词对
     - 例如：上下文词"苹果"和中心词"水果"真实共现
     - 标签：1（正例）

  2. *负样本*：随机采样的不共现词对
     - 从词表中随机采样 $k$ 个词（通常 $k=5-20$）
     - 这些词与当前上下文不共现
     - 标签：0（负例）

  3. *训练目标*：
     - 最大化正样本的概率，最小化负样本的概率
     - 只需要更新正例和 $k$ 个负例的参数
     - 不需要更新整个词表

  *目标函数*：
  $
  log sigma(v'_(w_o)^T v_(w_i)) + sum_(j=1)^k E_(w_j ~ P_n(w)) [log sigma(-v'_(w_j)^T v_(w_i))]
  $

  其中：
  - $sigma$ 是 sigmoid 函数
  - $w_o$ 是正样本
  - $w_j$ 是负样本
  - $P_n(w)$ 是负采样分布

  *三、负采样策略*

  *采样分布*：
  - 按词频的 $3/4$ 次方采样：$P(w_i) = (f(w_i)^(3/4)) / (sum_j f(w_j)^(3/4))$
  - 高频词更容易被采样为负例
  - 但不会过于集中（相比直接按词频采样）

  *为什么用 $3/4$ 次方*：
  - 平衡高频词和低频词
  - 如果直接按词频采样，高频词（如"的"、"是"）会被过度采样
  - $3/4$ 次方降低高频词的采样概率，提高低频词的采样概率

  *四、负采样的优点*

  1. *训练效率大幅提高*
     - 计算复杂度从 $O(|V|)$ 降到 $O(k)$
     - 通常 $k=5-20$，远小于词表大小 $|V|$
     - 训练速度提升几十到几百倍

  2. *效果通常更好*
     - 负采样相当于给模型提供了"反例"
     - 模型不仅学习哪些词应该共现，也学习哪些词不应该共现
     - 实践中效果通常优于层次 Softmax

  3. *实现简单*
     - 随机采样，易于实现
     - 并行化友好

  *五、与层次 Softmax 的对比*

  #table(
    columns: (auto, 1fr, 1fr),
    stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
    inset: 6pt,
    fill: (col, row) => if row == 0 { sectionbg } else { white },
    table.header([*对比项*], [*负采样*], [*层次 Softmax*]),
    [计算复杂度], [$O(k)$，通常 $k=5-20$], [$O(log|V|)$],
    [实现难度], [简单，随机采样], [复杂，需要构建 Huffman 树],
    [训练效果], [通常更好], [略差],
    [适用场景], [大部分场景], [词表极大时],
  )

  *六、总结*

  Word2Vec 需要负采样技术的原因：

  1. *解决计算效率问题*
     - 标准 Softmax 对整个词表归一化太慢
     - 负采样只计算少量样本，大幅提高效率

  2. *保持甚至提升效果*
     - 提供反例学习信号
     - 实践中效果通常更好

  3. *易于实现和并行化*
     - 随机采样简单高效
     - 适合大规模训练

  *答题要点*：
  - 问题：标准 Softmax 需要对整个词表归一化，计算复杂度 $O(|V|)$ 太高
  - 解决方案：转化为二分类，只采样少量负样本（5-20个）
  - 效果：训练效率大幅提高，同时保持甚至提升词向量质量
  - 采样策略：按词频的 $3/4$ 次方采样，平衡高频和低频词
]
