#import "../template/preamble.typ": *

= 信息检索

#knowtitle[信息检索重点：VSM框架三关键点、LSI模型优势与矩阵含义、问答系统类型。主要考概念理解。]

#intuition[
  信息检索的核心是"如何衡量查询与文档的相关性"。从布尔模型的精确匹配，到向量空间模型的相似度计算，再到LSI的语义理解，体现了从形式到语义的进步。
]

== 定义

/ 信息检索 (Information Retrieval, IR): 根据用户查询，从大规模文档集合中找出相关文档并排序的过程。

== 基本模型

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*模型*], [*核心思想*], [*特点*]),
  [布尔模型], [用 AND/OR/NOT 精确匹配], [简单但不能排序],
  [向量空间模型 VSM], [把查询和文档表示成向量], [可用余弦相似度排序],
  [概率模型], [估计文档与查询相关的概率], [BM25 是经典代表],
)


== VSM 信息检索框架三关键点\*

#warnbox[

  *课后习题*：简述基于VSM信息检索技术框架的三个关键点分别是什么？

  答题要点：
  1. #key[索引项选择]：从文档中选择能代表内容的词项，可做分词、词性过滤、同义词归并
  2. #key[权重计算]：为索引项赋权，常用 TF-IDF 表示词在文档中的局部重要性和全局区分度
  3. #key[相似度评价]：把查询和文档都表示为向量，用余弦相似度等方法排序返回
]

#intuition[
  VSM把文档和查询都看作向量空间中的点，相关性就是两个点的距离/角度。关键是选什么词、如何加权、怎么算距离。
]

== LSI 隐性语义标引\*

/ LSI (Latent Semantic Indexing): 对词项-文档矩阵做奇异值分解（SVD），将词项和文档映射到低维潜在语义空间中再检索。

#formula[$ C = U Sigma V^T $]

#warnbox[

  *课后习题*：简述LSI模型中C、U、$V^T$、$Sigma$四个矩阵各自的含义？

  答题要点：
  - *$C$*：原始词项-文档矩阵
    - 行是词项，列是文档，元素是词项权重（如TF-IDF）
  - *$U$*：词项表征矩阵
    - 每行对应一个词项
    - 每列代表一个语义维度
    - 元素 $u_(i j)$ 表示词项 $i$ 与语义维度 $j$ 的关系强度
  - *$Sigma$*：奇异值矩阵
    - 对角矩阵，对角线上的奇异值按降序排列
    - 奇异值表示对应语义维度的重要性
    - 可通过保留前 $d$ 个最大奇异值实现降维
  - *$V^T$*：文档标引矩阵
    - 每列对应一篇文档
    - 每行代表一个语义维度
    - 元素 $v_(i j)$ 表示文档 $j$ 与语义维度 $i$ 的关系强度
]

#warnbox[
  *课后习题*：LSI模型应用于信息检索相对于基本的向量空间模型有哪些优势？

  答题要点：
  1. *降维降噪*：通过保留重要奇异值，过滤次要语义维度，去掉噪声信息
  2. *缓解一词多义问题*：多义词与多个语义维度相关，其作用会被自动降低
  3. *缓解同义词问题（一义多词）*：
     - 如"计算机"和"电脑"通过语义维度联系起来
     - 即使查询中只有"计算机"，包含"电脑"的文档也能被检索到
     - 解决了传统基于词匹配导致的漏检问题
  4. *语义相似度计算*：在低维语义空间中计算相似度，泛化能力更强
]

#intuition[
  LSI的核心思想：不直接比较词，而是先把词和文档都映射到"语义空间"，再比较语义相似度。就像不比较两个人说的具体词语，而是比较他们谈论的话题是否相同。
]

== TF-IDF 速记

#formula[$ "tf-idf"(t,d) = "tf"(t,d) dot "idf"(t) $]
#formula[$ "idf"(t) = log(N / "df"(t)) $]

- *TF (Term Frequency)*：词在文档中的频率，反映词在文档中的重要性
- *IDF (Inverse Document Frequency)*：逆文档频率，降低常见词权重
- $N$：文档总数；$"df"(t)$：包含词 $t$ 的文档数

#intuition[
  IDF的直觉：越少文档包含的词，区分能力越强，IDF越高。如"的"出现在几乎所有文档中，IDF很低；"量子纠缠"只出现在少数文档中，IDF很高。
]

== 问答系统\*

/ 问答系统 (Question Answering, QA): 允许用户以自然语言方式提问，系统能从大量异构数据中查找并总结出用户问题答案的信息检索系统。

#key[核心特征]：
- *输入*：自然语言问句（如"世界上最大的宫殿是什么？"）
- *输出*：准确而简洁的答案（如"紫禁城/故宫"），而非文档列表

#warnbox[

  *课后习题*：简述问答系统的概念，以及当前的几种实现类型？

  答题要点：

  *问答系统概念*：
  - 允许用户以自然语言方式提问
  - 系统从大量异构数据中查找并总结出答案
  - 返回准确简洁的答案，而非文档列表

  *几种实现类型*：

  1. *基于大规模真实文本的问答系统*
     - 从预先建立的大规模真实文本语料库中找答案
     - 覆盖度受限于语料库

  2. *基于Web的问答系统*
     - 从网络（Web）中寻找提问的答案
     - 覆盖度高
     - 最新发展是社区问答系统

  3. *基于知识库的问答系统*
     - 从预先建立的结构化知识库中找答案
     - 可以实现推理
     - 知识库构建困难

  4. *基于语言模型的理解式问答系统*
     - 语言模型是底层基础
     - 预先学习大量"问题-答案"材料
     - 利用模型的生成能力生成任意问题的答案
]

#methodblock[
  *基本框架*：

  用户提问 → 问句分析（查询项、答案类型）→ 查询&推理（从语料库/网络/知识库）→ 答案抽取 → 返回答案
]

== 评价指标速记

#key[常见评价指标]：

*Precision / Recall / F1*

#formula[$ P = "检索出的相关文档数" / "检索出的文档总数" $]
#formula[$ R = "检索出的相关文档数" / "所有相关文档总数" $]
#formula[$ F_1 = (2 P R) / (P + R) $]

- Precision：查准率，看"查得准"
- Recall：查全率，看"查得全"

*问答系统评价指标*

- *Accuracy（准确率）*：答案正确的问题数 / 全部问题数
- *MRR (Mean Reciprocal Rank)*：标准答案在系统给出的排序结果中位置的倒数的平均
- *CWS (Confidence Weighted Score)*：按答案把握性从高到低对问题排序后，计算前 $i$ 个提问中被正确回答的提问数的加权和

== 本章高频题

#exercise(title: [信息检索])[
  1. 简述基于VSM信息检索技术框架的三个关键点。
  2. LSI模型应用于信息检索相对于基本的向量空间模型有哪些优势？
  3. 简述LSI模型中C、U、$V^T$、$Sigma$四个矩阵各自的含义。
  4. 简述问答系统的概念，以及当前的几种实现类型。
]
