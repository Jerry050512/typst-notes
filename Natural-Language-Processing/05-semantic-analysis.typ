#import "../template/preamble.typ": *

= 语义分析

#knowtitle[语义分析考点集中在 WSD、SRL、语义依存与"语义分析为什么困难"。核心是从形式结构走向意义理解。]

#intuition[
  句法分析知道"谁和谁有语法关系"，语义分析进一步问"这句话到底表达了什么"。例如"苹果发布新品"和"我吃苹果"中，"苹果"的含义不同，这就是语义分析要解决的问题。
]

== 定义

/ 语义分析 (Semantic Analysis): 在词法和句法分析基础上，识别语言符号与客观世界意义之间关系的过程。

主要任务：
- 词语层面：词义消歧 WSD、同义词/反义词识别；
- 句子层面：语义角色标注 SRL、语义依存分析；
- 篇章层面：指代消解、篇章关系、上下文理解。

== 词义消歧 WSD\*

/ 词义消歧 (Word Sense Disambiguation, WSD): 根据上下文判断多义词在具体句子中的正确含义。

#example(title: [词义消歧])[
  - "苹果很好吃"中的"苹果"是#key[水果]
  - "苹果发布新手机"中的"苹果"是#key[公司]
  - "他在银行工作"中的"银行"是#key[金融机构]
  - "他坐在河边的银行"中的"银行"是#key[河岸]
]

#methodblock[
  *WSD 基本思路*：
  1. 确定目标多义词。
  2. 收集其上下文词、句法关系或语义特征。
  3. 与词典义项、知识库或训练语料中的义项进行匹配。
  4. 选择最符合上下文的义项。
]

#warnbox[

  *课后习题*：简述词义消歧的概念。

  答题要点：词义消歧（WSD）是根据上下文判断多义词在具体句子中的正确含义的任务。需要举例说明，如"苹果"在不同上下文中的不同含义。
]

=== WSD 方法分类\*

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*类别*], [*原理*], [*代表/例子*]),
  [基于规则], [人工抽取词语搭配和选择限制规则], ["吃"的宾语通常具备可食性],
  [有监督学习], [用标注语料把词义当类别分类], [朴素贝叶斯、最大熵、SVM],
  [基于词典], [利用释义、义类、双语翻译等知识判断义项], [定义重叠、义类辞典、双语词典],
)

#warnbox[
  *课后习题*：目前主要的词义消歧方法分为哪几类？

  答题要点：
  - *基于规则*：人工编写词语搭配和选择限制规则
  - *有监督学习*：用标注语料训练分类器（朴素贝叶斯、SVM等）
  - *基于词典*：利用词典释义、义类、双语翻译等知识
]

=== 基于词典的 WSD\*

#methodblock[
  *基于词典的 WSD 三种类型*：

  1. #key[基于语义定义]（Definition-based）
     - *原理*：比较上下文词与候选义项词典释义的重合或相关程度
     - *方法*：计算上下文词与义项定义之间的词汇重叠度或语义相似度
     - *例子*：若上下文有"fruit, delicious, eat"，则"苹果"的水果义项定义中也会出现类似词汇，重叠度高

  2. #key[基于义类辞典]（Thesaurus-based）
     - *原理*：查看上下文词所属语义类更支持哪个义项
     - *方法*：使用 WordNet、HowNet 等义类辞典，判断上下文词的语义类别
     - *例子*：上下文词属于 FOOD 类，则支持"苹果"的水果义项；属于 TECHNOLOGY 类，则支持公司义项

  3. #key[基于双语词典]（Bilingual Dictionary-based）
     - *原理*：把候选译词放到另一语言语料中统计共现，哪种译词与上下文更共现就选哪种义项
     - *方法*：利用翻译对应关系，通过目标语言中的翻译词选择合适义项
     - *例子*：中文"银行"可译为 bank（金融）或 bank（河岸），根据英文语料中的共现词判断
]

#warnbox[

  *课后习题*：基于词典的词义消歧有哪几种类型，分别简述其原理？

  答题要点：三种类型分别是基于语义定义、基于义类辞典、基于双语词典。每种都要说明原理和举例。
]

#intuition[

  词汇歧义可区分为：
  - *多义*（polysemy）：意义相关，如 open：开着的/公开的
  - *同形异义*（homonymy）：意义无关，如 bank：银行/河堤
  - *类别歧义*（categorical ambiguity）：跨词性，如 cook：煮/厨师

  邻近上下文常帮助确定词性，相隔较远的实词常帮助确定词义。
]


== 语义角色标注 SRL\*

/ 语义角色标注 (Semantic Role Labeling, SRL): 识别句子中的谓词及其论元角色，回答"谁对谁做了什么、在何时何地以何种方式做"。

#intuition[
  句法分析告诉我们"谁和谁有语法关系"，SRL 则进一步回答"在语义上谁扮演什么角色"。例如"小明用钥匙打开了门"，句法分析知道"小明"是主语，"门"是宾语，但 SRL 进一步标注："小明"是施事者(ARG0)，"门"是受事者(ARG1)，"钥匙"是工具。
]

#key[两类语义角色]：

根据中文 PropBank 标注体系，语义角色分为两类：

*一、核心语义角色（ARG0-ARG4）*

与具体谓词直接相关，根据动词词典给每个动词规定的格框架来定义：

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*角色标签*], [*角色描述*], [*例子*]),
  [ARG0], [施事者（动作的发出者）], [小明 打开 门（小明是 ARG0）],
  [ARG1], [受事者（动作的承受者）], [打开 门（门是 ARG1）],
  [ARG2], [范围或程度], [提高 10%（10% 是 ARG2）],
  [ARG3], [动作起点], [从 北京 到上海（北京是 ARG3）],
  [ARG4], [动作结束点], [从北京 到 上海（上海是 ARG4）],
)

*二、辅助性语义角色（ARGM-\*）*

起修饰作用，对于所有动词是通用的，角色标签都以 ARGM 开头：

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*角色标签*], [*角色描述*], [*例子*]),
  [ARGM-TMP], [时间], [昨天 打开门（昨天是 ARGM-TMP）],
  [ARGM-LOC], [地点], [在教室 学习（教室是 ARGM-LOC）],
  [ARGM-MNR], [方式], [小心地 打开门（小心地是 ARGM-MNR）],
  [ARGM-DIR], [方向], [向 东方 走（东方是 ARGM-DIR）],
  [ARGM-BNF], [受益者], [为 他人 服务（他人是 ARGM-BNF）],
  [ARGM-PRP], [目的], [为了 学习 而读书（学习是 ARGM-PRP）],
  [ARGM-ADV], [状语（一般性修饰）], [也 打开门（也是 ARGM-ADV）],
)

#warnbox[

  *课后习题*：简述语义角色标注的概念。自然语言处理中常用有哪些语义角色？

  答题要点：
  - *概念*：SRL 是识别句子中的谓词及其论元角色，回答"谁对谁做了什么、在何时何地以何种方式做"
  - *语义角色分类*：
    * 核心角色：ARG0（施事者）、ARG1（受事者）、ARG2（范围或程度）、ARG3（动作起点）、ARG4（动作结束点）
    * 辅助角色：ARGM-TMP（时间）、ARGM-LOC（地点）、ARGM-MNR（方式）、ARGM-DIR（方向）、ARGM-BNF（受益者）等
  - 需要举例说明，如"小明打开了门"中，"小明"是 ARG0，"门"是 ARG1
]

== 语义依存分析

/ 语义依存分析: 分析词语之间的语义关系，不完全受表层句法结构限制。

与依存句法相比：
- 依存句法强调语法支配关系；
- 语义依存强调实际语义关系；
- 语义依存更接近问答、信息抽取等应用需要。


=== SRL 方法比较\*

#methodblock[
  *SRL 主要方法*：

  根据句法分析的基础不同，SRL 方法可分为三类：

  1. #key[基于短语结构树的方法]
     - *标注对象*：短语/成分节点
     - *核心特征*：短语类别（NP/VP/PP）、成分路径（从谓词到候选论元的树路径）、与谓词的位置关系
     - *例如*："小明 打开 门"的短语结构树中，NP"小明"是 VP"打开门"的姐妹节点，特征为 NP-S-VP

  2. #key[基于依存关系树的方法]
     - *标注对象*：词语节点
     - *核心特征*：谓词到候选论元的依存路径、依存弧标签（SBV/VOB/ATT等）、词与词之间的依存方向
     - *例如*："小明 打开 门"的依存树中，"小明"直接依存于"打开"，依存标签为 SBV

  3. #key[基于浅层语块的方法]
     - *标注对象*：基本语块（chunk）
     - *核心特征*：语块类别（NP-chunk/VP-chunk）、语块与谓词的相对位置
     - *例如*：识别 [NP 小明] [VP 打开] [NP 门]，然后判断每个语块的角色
]

#key[核心差异总结]：

#table(
  columns: (auto, 1fr, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*方法*], [*标注对象*], [*核心特征来源*], [*优缺点*]),
  [基于短语结构树], [短语/成分节点], [短语类别、成分路径、与谓词的位置关系], [准确性高；依赖完整短语结构树，计算复杂],
  [基于依存关系树], [词语节点], [谓词到候选论元的依存路径、依存弧、依存标签], [直接刻画谓词-论元关系；依赖依存句法分析质量],
  [基于浅层语块], [基本语块], [语块类别、谓词相对位置], [速度快，不需要完整句法树；精度相对较低],
)

#intuition[
  短语结构树方法以"短语成分"（如 NP、VP）为候选论元，利用成分树的层级结构；依存树方法以"词"为单位，利用词与词之间的依存关系，更直接刻画谓词-论元关系。
]

#warnbox[

  *课后习题*：简述基于短语结构树的语义角色标注方法与基于依存关系树的语义角色标注方法之间的核心差异？

  答题要点：
  - *标注对象不同*：短语结构树方法以短语/成分节点为候选论元；依存树方法以词语节点为候选论元
  - *特征来源不同*：短语结构树方法使用短语类别、成分路径；依存树方法使用依存路径、依存弧标签
  - *核心差异*：短语结构树方法利用层级短语结构，依存树方法更直接刻画词与词之间的语义依存关系
]


== 语义分析的困难\*

#warnbox[
  语义分析难点包括：多义词、隐喻和转喻、省略、指代、常识知识依赖、领域知识依赖。考试中回答困难时要配例子。
]

#key[语义分析为什么困难]：

1. *多义词问题*
   - 一个词在不同上下文中有不同含义
   - 例如："苹果很好吃"（水果）vs"苹果发布新品"（公司）

2. *隐喻和转喻*
   - 词语的非字面意义，需要理解修辞和文化背景
   - 例如："他是个老狐狸"（隐喻：狡猾）、"白宫宣布"（转喻：美国政府）

3. *省略现象*
   - 句子中省略了某些成分，需要从上下文推断
   - 例如："你去吗？""去。"（省略了主语和宾语）

4. *指代消解*
   - 确定代词、指示词指向的实体
   - 例如："小明告诉小红他很累"（"他"可能指小明或小红）

5. *常识知识依赖*
   - 理解语言需要世界知识和生活常识
   - 例如："冰块放在水里会浮起来"（需要物理常识）

6. *领域知识依赖*
   - 专业领域的语义理解需要专门知识
   - 例如："量子纠缠"（需要量子物理知识）

#intuition[
  语义分析比词法和句法分析更困难，因为它不仅要分析语言的形式结构，还要理解语言背后的意义，这需要大量的知识和推理能力。
]

#warnbox[

  *课后习题*：为什么语义分析比词法和句法分析更困难？

  答题要点：
  - *多义性*：同一个词在不同上下文中有不同含义，需要消歧
  - *隐含信息*：省略、指代等现象需要从上下文推断
  - *知识依赖*：需要常识知识、领域知识等背景知识
  - *非字面意义*：隐喻、转喻等修辞手法增加理解难度
  - 每个难点都要举例说明
]

== 本章高频题

#exercise(title: [语义分析])[
  1. 什么是语义分析？它与句法分析有什么区别？
  2. 什么是词义消歧？请举例说明。
  3. 什么是语义角色标注？常见语义角色有哪些？
  4. 为什么语义分析比词法和句法分析更困难？
]
