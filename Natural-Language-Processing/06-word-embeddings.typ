#import "../template/preamble.typ": *

= 预训练词向量

#knowtitle[本章重点是词表示方法的演进：One-hot → 分布式表示 → Word2Vec静态词向量 → BERT/GPT上下文表示。常考比较题和发展过程题。]

#intuition[
  词向量是NLP的基础。理解词表示的演进，就是理解NLP从符号主义到分布式表示、从静态到动态、从浅层到深层的发展历程。
]

== NLP 技术范式演进\*

/ NLP 技术范式: 从基于规则到基于统计，再到基于深度学习和预训练模型的发展历程。

#key[四次范式变迁]：

*第一范式：基于规则的方法（1950s-1980s）*
- *核心特征*：人工编写规则和知识库
- *代表*：专家系统、基于规则的机器翻译
- *优点*：可解释性强
- *缺点*：规则覆盖面有限，维护成本高

*第二范式：基于统计机器学习的方法（1990s-2013）*
- *核心特征*：从标注语料中学习统计模型
- *代表*：HMM、CRF、SVM、最大熵模型
- *特点*：需要人工设计特征（特征工程），需要大量标注数据
- *优点*：泛化能力较强，覆盖面广
- *缺点*：依赖特征工程，模型表达能力有限

*第三范式：基于深度学习的方法（2013-2018）*
- *核心特征*：端到端学习，自动学习特征表示
- *代表*：Word2Vec、CNN、RNN、LSTM、Seq2Seq、Attention
- *突破*：词向量学习、端到端神经网络
- *优点*：减少特征工程，表示学习能力强
- *缺点*：需要大量标注数据，模型复杂度高

*第四范式：基于预训练模型的方法（2018至今）*
- *核心特征*：预训练+微调（Pre-training + Fine-tuning）
- *代表*：BERT、GPT 系列、T5、ChatGPT
- *突破*：在海量无标注数据上预训练通用语言表示，少样本甚至零样本学习
- *优点*：迁移学习能力强，少量标注数据即可适应新任务
- *缺点*：计算资源消耗巨大，存在幻觉和偏见问题

#warnbox[

  *课后习题*：简述NLP从基于统计机器学习过渡到当前基于预训练模型的发展过程，以及过程中各阶段的核心特征？

  答题要点：
  - *统计机器学习阶段*（第二范式）：核心特征是从标注语料学习统计模型，需要人工特征工程，代表有HMM、CRF、SVM
  - *深度学习阶段*（第三范式）：核心特征是端到端学习和自动特征提取，代表有Word2Vec、RNN、LSTM、Attention
  - *预训练模型阶段*（第四范式）：核心特征是预训练+微调范式，在海量无标注数据上预训练通用表示，代表有BERT、GPT
  - 发展趋势：从人工规则→统计学习→深度学习→预训练模型，体现了从浅层到深层、从特征工程到端到端、从任务特定到通用迁移的演进
]

== 传统词表示

=== One-hot 表示

/ One-hot表示: 用词表长度的稀疏向量表示词，每个词只有一个维度为 1。

#example(title: [One-hot 表示])[
  假设词表为 {苹果, 香蕉, 手机}，则：
  - "苹果" = [1, 0, 0]
  - "香蕉" = [0, 1, 0]
  - "手机" = [0, 0, 1]
]

#key[优缺点]：
- *优点*：简单直观、无需训练或标注数据；每个词有唯一表示
- *缺点*：
  - 维度高：词表有 $N$ 个词，向量就是 $N$ 维
  - 稀疏：只有一个维度为 1，其余全为 0
  - #key[无法表达语义相似性]：任意两个词向量的内积都是 0，"苹果"和"香蕉"的相似度与"苹果"和"手机"完全相同

=== 分布式表示\*

/ 分布式表示 (Distributed Representation): 用低维稠密向量表示词，使语义相近的词在向量空间中距离更近。

#key[核心思想]：分布语义假设（Distributional Hypothesis）

#formula["You shall know a word by the company it keeps."

  词义可以由它经常出现的上下文来刻画。]

#example(title: [分布式表示])[
  假设词向量维度为 3（实际通常为 100-300 维）：
  - "苹果" = [0.8, 0.2, 0.1]
  - "香蕉" = [0.7, 0.3, 0.1]
  - "手机" = [0.1, 0.2, 0.9]

  "苹果"和"香蕉"向量相似（都是水果），与"手机"差异大。
]

#key[优点]：
- 低维稠密：通常 100-300 维，不随词表增长
- 能表达语义相似性：相近词向量距离近
- 可以捕捉词之间的语义关系

#intuition[
  One-hot 像"身份证号"，只能区分身份，无法表达相似性；词向量像"性格画像"，能表示词之间的相似与差异。
]


== 词表示发展路线\*

#key[词表示模型演进]：

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*阶段*], [*核心思想*], [*代表模型*]),
  [One-hot], [离散稀疏表示，不能表达相似性], [传统符号主义方法],
  [LSA], [词项-文档矩阵 + SVD 降维，获得潜在语义空间], [潜在语义分析],
  [HAL], [滑动窗口统计词-词共现，距离越近权重越高], [超空间类比语言模型],
  [NNLM/RNNLM], [神经网络语言模型中学习词向量], [前馈神经网络语言模型],
  [Word2Vec/GloVe], [局部预测或全局共现学习静态词向量], [CBOW、Skip-gram、GloVe],
  [ELMo/BERT/GPT], [根据上下文动态生成词表示], [上下文词向量、预训练模型],
)

#methodblock[
  *词表示演进的三个关键阶段*：

  1. #key[稀疏表示 → 分布式表示]
     - 从 One-hot 到 LSA/HAL，开始利用共现统计获得稠密向量
     - 关键：引入分布语义假设

  2. #key[传统方法 → 神经网络方法]
     - 从 LSA/HAL 到 NNLM/Word2Vec，神经网络自动学习词向量
     - 关键：端到端学习，词向量作为神经网络的副产品

  3. #key[静态词向量 → 动态词向量]
     - 从 Word2Vec/GloVe 到 ELMo/BERT，词向量根据上下文动态生成
     - 关键：解决一词多义问题
]

#warnbox[

  *课后习题*：简述词表示模型的发展过程，以及不同过程的代表性模型？

  答题要点：
  - *One-hot 表示*：离散稀疏，无法表达语义相似性
  - *早期分布式表示*：LSA（词项-文档矩阵+SVD）、HAL（滑动窗口共现）
  - *神经网络词向量*：NNLM/RNNLM（神经语言模型中学习）、Word2Vec（CBOW/Skip-gram）、GloVe（全局共现矩阵分解）
  - *上下文词向量*：ELMo（双向LSTM）、BERT（Transformer Encoder）、GPT（Transformer Decoder）
  - 发展趋势：稀疏→稠密、静态→动态、浅层→深层
]

=== LSA、GloVe 与 ELMo 速记

- #key[LSA (Latent Semantic Analysis)]：
  - 构建词项-文档矩阵 $C$
  - 奇异值分解 $C=U Sigma V^T$
  - 降维后缓解稀疏并挖掘潜在语义
  - 属于全局统计方法

- #key[GloVe (Global Vectors)]：
  - 利用全局词-上下文共现矩阵做加权最小二乘回归
  - 本质是隐式矩阵分解
  - 结合了全局统计信息（类似LSA）和局部上下文窗口（类似Word2Vec）

- #key[ELMo (Embeddings from Language Models)]\*：
  - 基于双向 LSTM 的上下文词向量
  - 同一个词在不同句子中向量不同，解决一词多义
  - 字符级输入，有利于处理 OOV（未登录词）
  - 属于动态词向量的早期代表

#warnbox[

  简述 ELMo 词向量的核心特点？

  答题要点：
  - *动态（上下文相关）*：词的 ELMo 向量表示由其当前上下文决定，同一个词在不同句子中有不同向量
  - *鲁棒性强*：ELMo 采用字符级输入，对于未登录词（OOV）具有强鲁棒性
  - *层次表示*：ELMo 词向量由深度预训练模型中各个层次的向量表示进行加权组合
    - 第一层（底层）：Word Embedding
    - 第二层（第一层 LSTM）：编码更多句法信息
    - 第三层（第二层 LSTM）：编码更多语义信息
  - *灵活组合*：为不同的下游任务提供了较大的使用自由度，可根据任务需求调整各层权重
  - *举例说明*：如"银行"在"我在银行存钱"和"他坐在河边的银行"中有不同的向量表示
]


== Word2Vec\*

/ Word2Vec: 通过预测上下文或中心词学习静态词向量的神经网络模型，代表模型包括 CBOW 和 Skip-gram。

#key[核心思想]：通过神经网络学习词向量，使得在相似上下文中出现的词有相似的向量表示。

#table(
  columns: 3,
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*对比项*], [*CBOW (Continuous Bag of Words)*], [*Skip-gram*]),
  [预测方向], [用上下文预测中心词], [用中心词预测上下文],
  [输入], [上下文词向量的平均], [中心词向量],
  [输出], [中心词], [上下文词],
  [训练速度], [较快（一个样本一次预测）], [较慢（一个样本多次预测）],
  [低频词效果], [相对较弱], [通常更好],
  [适用场景], [大语料、常见词], [小语料、罕见词],
  [记忆口诀], [Context → Word], [Word → Context],
)

#example(title: [CBOW 与 Skip-gram])[
  句子："我 爱 自然 语言 处理"

  *CBOW*：
  - 上下文：["我", "自然"]，预测中心词："爱"
  - 上下文：["爱", "语言"]，预测中心词："自然"

  *Skip-gram*：
  - 中心词："爱"，预测上下文：["我", "自然"]
  - 中心词："自然"，预测上下文：["爱", "语言"]
]

#intuition[
  CBOW 像完形填空：给你前后文，猜中间的词。Skip-gram 像造句扩展：给你一个词，猜它可能出现在什么上下文中。
]

#warnbox[

  比较 CBOW 和 Skip-gram 模型的核心差异？

  答题要点：
  - *预测方向*：
    - CBOW：用上下文预测中心词（Context → Word）
    - Skip-gram：用中心词预测上下文（Word → Context）
  - *训练速度*：
    - CBOW：较快，一个样本只需一次预测
    - Skip-gram：较慢，一个样本需要多次预测（每个上下文词一次）
  - *适用场景*：
    - CBOW：适合大语料、常见词，训练效率高
    - Skip-gram：适合小语料、罕见词，对低频词效果更好
  - *举例说明*：如"我 爱 自然 语言 处理"，CBOW用["我","自然"]预测"爱"，Skip-gram用"爱"预测["我","自然"]
]

== 训练优化\*

Word2Vec 训练时面临的问题：标准 Softmax 需要对整个词表归一化，词表很大时计算非常慢。

常用两种优化方法：

*一、层次 Softmax (Hierarchical Softmax)*

#methodblock[
  *原理*：
  - 用 Huffman 树组织词表，每个词是叶子节点
  - 把多分类问题转为一系列二分类问题
  - 计算复杂度从 $O(|V|)$ 降到 $O(log|V|)$

  *特点*：
  - 高频词路径短，低频词路径长
  - 适合词表很大的情况
]

*二、负采样 (Negative Sampling)*

#methodblock[
  *原理*：
  - 把预测任务改为二分类：判断词对是否真实共现
  - 对每个正样本，随机采样 $k$ 个负样本（通常 $k=5-20$）
  - 只更新正例和这 $k$ 个负例的参数，不更新整个词表

  *采样策略*：
  - 按词频的 $3/4$ 次方采样
  - 高频词更容易被采样为负例，但不会过于集中

  *优点*：
  - 训练效率大幅提高
  - 效果通常优于层次 Softmax
]

#warnbox[

  *课后习题*：在 Word2Vec 模型中为什么需要采用负采样技术？

  答题要点：
  - *问题*：标准 Softmax 需要对整个词表归一化，计算复杂度是 $O(|V|)$，词表很大时（通常几万到几十万）计算非常慢
  - *负采样解决方案*：
    - 把多分类预测问题转化为二分类问题（判断词对是否真实共现）
    - 对每个正样本只采样少量负样本（通常 5-20 个）
    - 只更新正例和少量负例的参数，大幅降低计算量
  - *效果*：训练效率显著提高，同时保持甚至提升词向量质量
]

#warnbox[

  比较层次 Softmax 和负采样两种优化方法的原理？

  答题要点：
  - *层次 Softmax*：
    - 原理：用 Huffman 树组织词表，把多分类问题转为一系列二分类问题
    - 计算复杂度：从 $O(|V|)$ 降到 $O(log|V|)$
    - 特点：高频词路径短，低频词路径长
    - 适用：词表很大的情况
  - *负采样*：
    - 原理：把预测任务改为二分类（判断词对是否真实共现），对每个正样本只采样少量负样本（通常5-20个）
    - 计算复杂度：只更新正例和少量负例的参数，不更新整个词表
    - 采样策略：按词频的 $3/4$ 次方采样
    - 优点：训练效率高，效果通常优于层次 Softmax
  - *核心差异*：层次 Softmax 改变了多分类的结构（树形），负采样改变了训练目标（从多分类到二分类+采样）
]


== 静态词向量 vs 动态词向量\*

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else if calc.odd(row) { white } else { rgb("#f8f9fa") },
  table.header([*对比项*], [*静态词向量*], [*动态/上下文词向量*]),
  [定义], [每个词有固定的词向量表示], [同一个词在不同上下文有不同表示],
  [代表模型], [Word2Vec、GloVe、FastText], [ELMo、BERT、GPT],
  [一词多义], [同一个词只有一个向量，无法区分不同含义], [根据上下文动态生成，可以区分不同含义],
  [上下文依赖], [训练后固定，不依赖句子上下文], [根据上下文动态生成],
  [训练方式], [在大规模语料上预训练，得到固定词表], [预训练语言模型，使用时动态编码],
  [优点], [简单高效、计算快、易于使用], [语义表达能力强、能处理一词多义],
  [缺点], [无法解决一词多义问题], [计算成本高、模型复杂],
  [适用场景], [词义相对单一的任务], [需要理解上下文语义的任务],
)

#example(title: [静态 vs 动态词向量])[
  句子1："我在银行存钱"
  句子2："他坐在河边的银行"

  *静态词向量（Word2Vec）*：
  - "银行"在两个句子中有相同的向量表示
  - 无法区分"金融机构"和"河岸"两种含义

  *动态词向量（BERT）*：
  - 句子1中"银行"的向量编码了"金融机构"的语义
  - 句子2中"银行"的向量编码了"河岸"的语义
  - 同一个词，不同上下文，不同向量
]

#key[核心差异]：

- *静态词向量*：词向量是查表得到的，一个词对应词表中的一个固定向量
- *动态词向量*：词向量是计算得到的，根据整个句子的上下文编码生成

#warnbox[

  *课后习题*：简述静态词向量与动态词向量之间的核心差异，以及各自的优劣势？

  答题要点：
  - *核心差异*：
    * 静态词向量：每个词有固定的向量表示，不随上下文变化（如 Word2Vec、GloVe）
    * 动态词向量：同一个词在不同上下文中有不同向量表示（如 ELMo、BERT）
  - *静态词向量的优劣势*：
    * 优势：简单高效、计算快、易于使用和部署
    * 劣势：无法解决一词多义问题，"银行"在所有上下文中向量相同
  - *动态词向量的优劣势*：
    * 优势：能够根据上下文区分词义，语义表达能力强
    * 劣势：计算成本高、模型复杂、需要更多资源
]

== 预训练语言模型\*

/ 预训练语言模型: 在大规模语料上先学习通用语言表示，再迁移到下游任务的模型。

#key[预训练模型的作用]：

1. *解决数据稀缺问题*：
   - 利用海量无标注数据学习通用语言知识
   - 少量标注数据即可微调适应特定任务

2. *提供强大的特征表示*：
   - 预训练模型学到的表示包含丰富的语言知识
   - 比从零开始训练效果好得多

3. *实现迁移学习*：
   - 预训练阶段学习通用知识
   - 微调阶段适应特定任务
   - 大幅降低下游任务的数据和计算需求

#key[典型预训练模型架构]：

#table(
  columns: (auto, 2fr, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*模型*], [*架构*], [*训练目标*], [*适用任务*]),
  [BERT], [双向 Transformer Encoder], [掩码语言模型（MLM）+ 下一句预测（NSP）], [理解任务（分类、NER、问答）],
  [GPT], [单向 Transformer Decoder], [自回归语言模型（从左到右预测）], [生成任务（文本生成、对话）],
  [T5], [Encoder-Decoder], [文本到文本（统一框架）], [统一各类NLP任务],
  [XLNet], [Transformer-XL + 排列语言模型], [排列语言模型], [理解和生成],
)

#methodblock[
  *BERT vs GPT 核心差异*：

  *BERT (Bidirectional Encoder Representations from Transformers)*：
  - *架构*：Transformer Encoder（双向）
  - *预训练任务*：
    * MLM（掩码语言模型）：随机遮盖15%的词，预测被遮盖的词
    * NSP（下一句预测）：判断两个句子是否连续
  - *特点*：能够同时看到上下文，适合理解任务
  - *应用*：文本分类、NER、问答、阅读理解

  *GPT (Generative Pre-trained Transformer)*：
  - *架构*：Transformer Decoder（单向，从左到右）
  - *预训练任务*：自回归语言模型（预测下一个词）
  - *特点*：只能看到前文，适合生成任务
  - *应用*：文本生成、对话、补全

  *T5 (Text-to-Text Transfer Transformer)*：
  - *架构*：Encoder-Decoder
  - *统一框架*：把所有NLP任务都转化为文本到文本的生成任务
  - *例如*："翻译成英语：我爱你" → "I love you"
]

#intuition[
  预训练模型的核心思想：先在海量数据上学习"语言是什么"（预训练），再用少量数据学习"怎么用语言解决具体问题"（微调）。就像学生先接受通识教育，再进行专业学习。
]

== 本章高频题

#exercise(title: [词向量])[
  1. One-hot 表示有什么缺点？
  2. 什么是分布式词表示？它解决了什么问题？
  3. 比较 CBOW 和 Skip-gram。
  4. 比较静态词向量和上下文词向量。
  5. 简述 NLP 技术四次范式变迁及预训练模型的作用。
  6. Word2Vec 为什么需要负采样？
  7. 简述词表示模型的发展过程。
]
