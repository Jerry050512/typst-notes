#import "../template/preamble.typ": *

= 绪论

#knowtitle[绪论章节通常不考计算，但非常容易出名词解释、层次划分、方法比较和“为什么NLP难”的简答题。]

#intuition[
  自然语言处理要解决的问题可以理解为：把人平时“看懂一句话、判断一句话、生成一句话”的能力，拆解成计算机可以执行的步骤。它比形式语言难，因为自然语言有歧义、有上下文、有常识依赖，而且新词新用法会不断出现。
]

== NLP 定义与目标

/ 自然语言处理 (Natural Language Processing, NLP): 研究如何让计算机理解、处理和生成自然语言的学科，是人工智能的重要分支。

*核心目标*：
- #key[理解]：让计算机从文本/语音中识别词义、句法结构、语义关系和说话意图。
- #key[生成]：让计算机生成通顺、符合语境的自然语言。
- #key[交互]：让人能够用自然语言与机器沟通。

#warnbox[答“NLP定义”时不要只写“处理语言”。至少写出“理解 + 生成 + 人机交互/AI分支”。]

== NLP 研究层次

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*层次*], [*研究内容*], [*典型任务*]),
  [语音/音系], [语音信号、音素、音节等低层表示], [语音识别、语音合成],
  [词法分析], [词语边界、词性、实体等词级信息], [分词、POS、NER、OOV识别],
  [句法分析], [句子结构和词之间的依存关系], [短语结构分析、依存句法分析],
  [语义分析], [词义、句义及谓词-论元关系], [WSD、SRL、语义依存],
  [语用/篇章], [上下文、指代、意图和篇章连贯], [指代消解、问答、对话系统],
)

#key[记忆口诀]：语音 → 词法 → 句法 → 语义 → 语用，由形式到意义、由局部到上下文。

== 基本方法与范式变迁

#table(
  columns: (auto, auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else if calc.odd(row) { white } else { rgb("#f8f9fa") },
  table.header([*阶段*], [*时间*], [*核心思想*], [*代表方法*]),
  [小规模专家系统], [1950-1990], [语言学规则 + 知识库 + 推理], [ELIZA、SHRDLU、早期机器翻译],
  [语料库统计模型], [1990-2010], [从大规模语料中统计规律], [N-gram、HMM、CRF、SVM],
  [深度学习模型], [2010-2017], [神经网络端到端学习表示], [Word2Vec、RNN、LSTM、Seq2Seq],
  [大规模预训练模型], [2018-至今], [Transformer + 预训练 + 微调/提示], [BERT、GPT、T5、LLaMA],
)

=== 理性主义 vs 经验主义

#table(
  columns: (auto, 1fr, 1fr),
  stroke: (paint: rgb("#ccc"), thickness: 0.5pt),
  inset: 6pt,
  fill: (col, row) => if row == 0 { sectionbg } else { white },
  table.header([*对比项*], [*理性主义/规则方法*], [*经验主义/统计方法*]),
  [知识来源], [人工总结语言规则和知识], [从语料库中学习统计规律],
  [优点], [可解释性强，小数据可用], [覆盖面广，泛化较强],
  [缺点], [规则难穷尽，维护成本高], [依赖数据，解释性较弱],
  [代表], [乔姆斯基生成语法、专家系统], [N-gram、HMM、CRF、深度学习],
)

== NLP 的主要困难

#methodblock[
  *答题模板*：自然语言处理困难来自自然语言的开放性、歧义性和语境依赖。

  1. #key[歧义性]：一个表达可能有多种解释，如“咬死了猎人的狗”。
  2. #key[未登录词]：新词、专名、网络用语不断出现，词典无法完全覆盖。
  3. #key[知识依赖]：理解语言需要常识、世界知识和领域知识。
  4. #key[上下文依赖]：词义、指代和意图往往必须结合上下文判断。
  5. #key[形式化困难]：自然语言规则有例外，难以像程序语言一样完全精确定义。
]

== 本章高频题

#exercise(title: [绪论简答])[
  1. 什么是自然语言处理？它和人工智能是什么关系？
  2. NLP 的研究内容可以分为哪些层次？每层列举一个典型任务。
  3. NLP 面临哪些主要困难？请举例说明。
  4. 比较规则方法、统计方法和深度学习方法的特点。
]
